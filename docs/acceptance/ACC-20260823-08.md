# ACC-20260823-08 — Acceptance: wrong in both directions at once means the shape is wrong

- Target CHG: CHG-20260823-08 (the recogniser redesigned — red rules first, decidable repo paths and
  read-only VCS, operator vouching that never covers arguments)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **high** — `halt_independent`, the permanent halts.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** The redesign is present exactly as described, wired into the same engine stop
  that mutation testing shows bites, and its documented numbers are recomputed by tests rather than
  typed.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_targets.py tests/test_documented_numbers.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | Red rules are consulted **first** | `recognise` `policy.py:353-404` | `unverified` / `on_trust` then `engine.py:962`, `:971` | `test_targets.py::test_none_of_them_reaches_the_end_of_a_run` | **Pass, proven by mutation** — cutting the engine's unrecognised-target stop: **9 failed** |
| 2 | Shell composition is never ordinary | `_COMPOSED` `policy.py:276` disqualifies `> \| ; & \`$( ..` | `recognise` | `::test_shell_composition_is_never_ordinary` | **Pass** |
| 3 | A repo path and a read-only VCS command are decidable | `_REPO_PATH` `policy.py:280`; `_READ_ONLY` `policy.py:283` | `recognise` | `::test_a_traversal_guard_that_does_not_eat_ordinary_paths` | **Pass** — the guard is checked for over-refusal too, which is the half that made the previous shape wrong in both directions |
| 4 | The operator vouches for a toolchain, and vouching is consulted **last** | `ordinary_commands` `settings.py:49`, validated `:163-183` | `cli.py:676`, `cli.py:859` then `RunConfig.ordinary_commands` `engine.py:402` then `policy` at `engine.py:962`, `:971` | `::test_the_false_stop_rate_with_a_vouched_toolchain_is_zero` | **Pass** |
| 5 | Vouching never covers arguments | `_suspect` `policy.py:344-351` — `_DESTRUCTIVE_VERBS` in **subcommand position** plus `_DANGEROUS_FORMS`; applied `policy.py:397-402`; multi-word vouches refused `settings.py:178-183` | `recognise` | `::test_vouching_for_a_command_does_not_vouch_for_its_arguments` | **Pass** |
| 6 | Both seats' escaping commands now stop | `SILENT_PASSES` `test_targets.py:254-259`; `VOUCHED_BUT_DESTRUCTIVE` `:380-410`, including `cat … > /dev/sda`, `git reflog expire`, traversal | engine-level | `::test_none_of_them_reaches_the_end_of_a_run` | **Pass** — checked at the engine, not at the function |
| 7 | The documented numbers are computed | `test_documented_numbers.py` recomputes node/gate/halt counts, seat floor, work-order fields, catch rate, corpus size and rate from graph/policy/corpora and compares against every doc | the suite | `::test_this_check_would_catch_a_drifted_number`; `::test_each_category_is_still_stated_somewhere` | **Pass** — the check is proven able to fail, which is the property that makes it worth having |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Composition-aware recogniser wired | PASS | PASS, plus the engine-level mutation | **Agreed** |
| Vouching does not extend to arguments | PASS | PASS, both directions pinned | **Agreed** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

- The exact corpus counts **"27 destructive / 20 everyday"** have drifted as later changes grew the
  corpora and added `EXECUTORS` (`policy.py:296`) on top of this design. Drift, not falsity.
- `policy._SUSPECT` as the CHG names it is now the `_suspect` function plus its two constant lists —
  reshaped by later changes, same mechanism.
- The **"16 of 20 stop with no vouching"** default-cost figure was not re-measured. Its two endpoint
  claims — zero destructive commands treated as ordinary, zero ordinary commands stopped once
  vouched — are both test-enforced.
