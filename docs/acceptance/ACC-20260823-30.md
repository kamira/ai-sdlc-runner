# ACC-20260823-30 — Acceptance: the closing did not close it, and the pages said it had

- Target CHG: CHG-20260823-30 (nine fixes: the silent dry run closed in both directions, values
  checked and not only shapes, the atlas pinned, the test count made machine-independent)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **medium** — the plan now refuses several shapes it used to accept.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** This is the change that made three earlier records true, and every one of its
  nine fixes is present with a test that bites. One residue, small and named below.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_plan.py tests/test_schema_atlas.py tests/test_store.py tests/test_documented_numbers.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes. The `fable` engine ran the three files directly: **100 passed**
(store / plan / atlas) and **21 passed** (documented numbers).

## Check-details

| # | Fix | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | The silent dry run closed **both ways** | `_check_ship` `plan.py:208-221` — orphaned `acc_id` / `acc_body`, and `task` without `acc_id` | `plan.load`, then `cli.py:595` / `cli.py:800` | `test_plan.py::test_acc_id_without_a_task_is_refused`; `::test_a_task_with_no_acceptance_id_is_refused` | **Pass** — both directions, which is what CHG-28 did not do |
| 2 | The acceptance effect is reachable when the plan passes | same | — | `::test_the_acceptance_effect_is_reachable_when_the_plan_passes` | **Pass** — refusing more is only safe if the legitimate case still runs |
| 3 | Values, not only shapes | `plan.py:141-181` — `risk` against `policy.RISKS`, `autonomy` a string, three decision forms, `operations` a list of objects | `check` | `::test_a_value_the_runner_cannot_use_is_refused_at_the_door`; `::test_all_three_decision_forms_are_accepted` | **Pass** |
| 4 | Every `ship` value is a string | `plan.py:201-206` | `_check_ship` | `::test_a_ship_value_that_is_not_a_string_is_refused` | **Pass** |
| 5 | The docstring says what the code does, and keeps its retraction | `plan.py:88-95` | — | `::test_the_module_no_longer_claims_to_refuse_what_it_cannot` | **Pass — with one residue, below** |
| 6 | The atlas is pinned by tests that read it | `tests/test_schema_atlas.py`, eight tests reading `docs/schema-atlas.html` | the suite | `::test_the_footer_does_not_claim_a_test_that_does_not_exist` | **Pass** — a footer that names its own pinning tests, checked against whether they exist, is the fix for CHG-29's defect and a good idea in itself |
| 7 | The test count is machine-independent | `test_documented_numbers.py:317-352` — collected against collected | `README.md:678` | `::test_the_test_count_the_readme_states_is_the_real_one` | **Pass** — replaces CHG-29's collected-minus-skipped, which could not work |
| 8 | The false counts corrected | `README.md:484` "visits 20 of the 24 nodes"; `docs/SCHEMAS.md:407` "Seventeen routes" | — | `::test_the_readme_says_how_many_nodes_a_run_actually_visits` | **Pass** |
| 9 | Nine fixes, all wired | — | — | — | **Pass** |

## The residue

`plan.load`'s docstring (`plan.py:225`) still reads *"refuse it if this runner would not fully honour
it"* — the phrasing this change retracted. The fix and its test cover `check`'s summary line only, so
the retracted sentence survives one function along.

Small, and worth writing down for the same reason the retraction was worth making: a module that
describes a guarantee it does not provide is how the silent dry run got believed in the first place.

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| All nine fixes present and reaching the load boundary | PASS, `UNCONFIRMED: none` | PASS, three test files run green | **Agreed** |
| `plan.load`'s docstring | not examined | **found the surviving retracted phrasing** | **fable's finding stands** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

- **`plan.load`'s docstring.** A modification; needs its own CHG.
- The frozen-tree non-repetition this record states is **unverifiable from the repo** — nothing
  enforces it. Same finding as ACC-20260823-27, and this round violated it too; see the round record.
