# ACC-20260908-01 — a bounded wait that times out, and the assertion after it that takes the blame

- Project: ai-sdlc-runner
- Target CHG: CHG-20260908-01
- Date: 2026-09-10 (UTC+0)
- Reviewed by: **two independent seats**, cross-consensus, on a worktree pinned at the sha under
  review. The second seat is held by Opus rather than by the model that held it in earlier records:
  that model reached its usage limit, and the operator chose the substitution rather than dropping
  to one seat. The two seats are still not the same engine.
- Conclusion: **Pass** — on the measurements below, and after two rounds of cross-review that moved
  the record's own premise. This line is written before the review of the revision it sits in, and
  does not claim otherwise.

## What the seats moved, which is most of what is worth reading here

The build did not change direction. What changed is what the record was allowed to say, and the
largest correction is to the **premise**.

| # | what a seat refused | how it was settled |
|---|---|---|
| 1 | *"wall-clock inside an assertion: 0"*, the row the whole re-scoping rests on | re-measured by binding elapsed-time names first: **2**, both surviving into this tree, one with no message at all. The `0` came from a probe that wanted the clock call and the `assert` on one physical line |
| 2 | the `finally`'s named victim | forced the leak, ran the file: **no test fails**. The named test counts threads *after* this helper has run and closes with `<=`, so the leak helps it pass. The guard is more load-bearing than claimed, for the opposite reason |
| 3 | *"the keyword is the whole of the rule's key"* | `first.join(10)` with its guard deleted passed the rule. Both seats found this independently. Positional ceilings now count — as numeric literals only, because `sep.join(parts)` is the same syntax and counting any positional argument reported string joins as unguarded threads |
| 4 | a guard that names `is_alive` without calling it | `assert thread.is_alive` passed. The rule now wants the call |
| 5 | `wait` refused only as a bare statement | `done = gate.wait(timeout=10)` never read passed. Now refused, along with `wait_for` and `acquire` |
| 6 | a loop's joins answered for once outside the loop | constructed and confirmed. Now refused — and the one real loop join in the file guards inside a comprehension, which is what makes the two distinguishable |
| 7 | *"deleting an `assert` makes no test fail"* | a universal its own first mutation refutes. What is true is that no **behavioural** test fails; the rule does |
| 8 | *"12 `time.sleep` polling loops in five test modules"* | by AST: two in a loop and three one-shot, in one module. The 12 counted text, including `time.sleep` inside subprocess source strings and a docstring |
| 9 | the citation of `test_the_server_waits_and_the_walk_does_not` as holding `src/` | it reads one method and refuses three literal substrings, one being `".join()"` with empty parentheses — which no bounded join is spelled with |
| 10 | `_own_statements`' *"three of the waits below"* | two, and one of them is in a module-level helper, not in a test |
| 11 | `set(ran_on) == {first.name}` compares labels | thread names are not unique; it compares thread objects now |
| 12 | *"the mutation for `ran_on` is registered so the pin is known to hold"* | measured: that mutation is caught by `entered[1].wait`. The seat's proposed replacement is caught by `gave_up`, at 12.34s. **Nothing pins `ran_on`** — see below |
| 13 | `sys.exc_info()` in the `finally` | it reports what the *thread* is handling, not what this `try` raised, so a caller inside an `except` would have silenced the helper on the return path. Replaced by a flag set by the body's own `except` |

Two of these — 3 and 7 — were raised by both seats independently. The rest split evenly.

## The one finding neither seat could close, recorded rather than argued away

`ran_on` is the assertion that says the walks ran on the thread the test started. Nothing puts it
on the stand:

```
mutation                                        first line to refuse        time
if held in entered:  ->  if held == 2:          "the first walk never started"   12.64s
... wait(...)  ->  held != 1 and ... wait(...)  "walk(s) [2] were released ..."   12.34s
```

And no small mutation can, for a structural reason: a walk that runs on the attaching thread waits
there for a release only that thread can send, so the timeout it takes is always seen first by
`gave_up`. `ran_on` states the property in the test's own words and is redundant with `gave_up` for
the failure it was added for. It is kept because it names the thing directly, and the record says
it is unpinned rather than implying otherwise.

## Criteria

| # | criterion | evidence | verdict |
|---|---|---|---|
| 1 | The defect is constructed, not argued | shortened join, `31535de`: `AssertionError: the attachment was never walked` while the attachment was walked | **Pass** |
| 2 | The population is counted, and the count survives a second search | **Pass, after one refusal.** The first table said 0 wall-clock assertions and it was a search artifact; re-taken, 2 | **Pass, after one refusal** |
| 3 | Every repaired site is repaired in the shape its call needs | `join` returns nothing → a separate `is_alive()`; `wait`/`wait_for`/`acquire` return the answer → read it | **Pass** |
| 4 | A walk does not assert | `Runner` turns what a walk raises into a stopped run; both waits inside a walk record for the test thread | **Pass** |
| 5 | The `finally` asks about its own failure | a flag set by the body's `except`, not `sys.exc_info()`, which is thread-wide | **Pass, after one refusal** |
| 6 | The `finally` is not silent when it cannot raise | it warns; pytest has no `-W error` here, so the warning reaches the summary | **Pass** |
| 7 | The rule reports what it is supposed to | 0 on this tree, all seven repaired sites on `31535de`, and it does not flag the two waits that were already correct | **Pass** |
| 8 | The rule is swept, not sampled | seven constructed cases run through the shipped rule: four defect shapes refused, three correct shapes accepted (below) | **Pass** |
| 9 | The rule's limits are stated where it lives | four, in its docstring, three of them cases a seat constructed and this record chose not to close | **Pass, after two refusals** |
| 10 | Every reference names something that exists | one cited test name did not exist and was invented; every name in the change is now checked against the file's own `def`s | **Pass, after one refusal** |
| 11 | A guard that cannot be mutated says why | the four `is_alive()` guards: no *behavioural* test fails when one is deleted, which is why the rule exists. The rule is what catches it | **Pass, after one refusal** |
| 12 | The registry catches what it registers | `--only bounded-wait`, **9 of 9** caught | **Pass** |
| 13 | What the registry does not pin is named | `ran_on`, with the measurement showing why | **Pass, after one refusal** |
| 14 | The test whose arrangement never happened is fixed, not deleted | every assertion it had, plus the one it was missing; 10.21s → 0.04s | **Pass** |
| 15 | The suite still passes | `1 failed, 2357 passed, 4 skipped` at the first run — the README's test count — then green | **Pass** |

## The sweep behind criterion 8

Seven files written into a copy of `tests/`, each read by the shipped rule with nothing modified:

```
control: a guarded join                          accepted
an unguarded join                                REFUSED
an answer kept and never read                    REFUSED
an answer kept and read                          accepted
a lock acquired with a ceiling, answer dropped   REFUSED
a loop join guarded once outside the loop        REFUSED
a loop join guarded over the loop                accepted
```

## The finding this record is really about

`test_an_action_arriving_as_the_walk_decides_to_stop_is_not_stranded` was green on `31535de` and
took **10.21s** being green. Instrumented on that tree:

```
walk #1 enters      0.01     on the thread the test started
walk #2 enters      0.02     on the MAIN thread, inside runner.attach("one.md")
wait returns False 10.03     released by its own timeout
walk #3 enters     10.04
release_second.set() 10.04   after the walk it was meant to release had ended
```

`attach()` ends in `return self._advance()`, and `_advance` walks **on the calling thread** when
nothing is in flight. With the first walk returning at once, the test's own `attach` ran the second
walk, on the main thread, and stayed inside it until the timeout. So the third attachment was never
posted from inside a walk; the thread named `first` had been finished since 0.02; the count came out
at 3 anyway, and the test passed on it.

This is what a bounded wait costs when nothing asks whether it completed: not a flake, a **test that
has never run the scenario in its own docstring** while saying *"forced deterministically"*.

## Evidence

```
tools/mutation_check.py --only bounded-wait     9 of 9 CAUGHT
tools/ledger_check.py                           passed
tests/test_server.py                            155 passed
pytest -q                                       1 failed, 2357 passed, 4 skipped in 1165.80s
                                                (the README count; corrected, see the PR)
README.md                                       2361 -> 2362
test_server.py wall-clock                       67s -> 62s
the repaired test                               10.21s -> 0.04s
```

## What is still open, and belongs elsewhere

- Waits with no argument at all — a `join()` and several `wait()`s in other modules. They hang
  rather than misreport. Different failure, different remedy.
- The general arrangement defect: any test that starts a walk on a thread and then calls a mutating
  route from the main thread may have the route walk it synchronously instead. This record pins the
  one place it was found. The three `_gated_runner` tests were checked and do not have it — their
  walk blocks from the first call, so the attaching thread always finds one in flight.
- The remaining escapes from the rule, all named in its docstring: a ceiling held in a variable, one
  passed as `**{"timeout": 10}`, an answer read into a name and then ignored, and an inverted guard.
- The two wall-clock assertions themselves. One now says what its ceiling means; neither was moved,
  because `ACC-20260908-05` measured the margins and found no defect in them.
