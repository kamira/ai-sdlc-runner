# ACC-20260703-01 — Acceptance: align runner baseline to ai-sdlc v1.12.1

- Project: ai-sdlc-runner
- Target CHG: CHG-20260703-01
- Verifier: V1 (independent; ≠ implementer I1; model opus, Explore/read-only tool loadout)
- Date: 2026-07-03 (UTC+0)
- Skill: ai-sdlc v1.12.1
- Risk: medium (new vendored store version + first-run config default bump)
- Conclusion: **Pass** (after one fix → re-verify loop on docs-only criterion #5)

## Check details
| # | Criterion | Method | Result |
|---|-----------|--------|--------|
| 1 | `skills/v1.12.1/SKILL.md` frontmatter version 1.12.1; `scripts/halt_gate.py` + `references/agent-hierarchy.md` present | read frontmatter + `ls` | Pass — `metadata: version: 1.12.1`; both files present (halt_gate.py 4401B, agent-hierarchy.md 15007B). `contract.read_skill_version` (contract.py:56) supports the nested form; the fresh-run lock proves it resolves. |
| 2 | `config/runner.yaml` `contract_version: "1.12.1"` | read config | Pass — line 11. |
| 3 | Fresh project, store default (no `--skill-path`/`--contract-version`), locks to 1.12.1 via vendored store | live `runner run --backend stub --risk low` + `cat .sdlc-lock.json` | Pass — run `contract: 1.12.1`; lock `contract_version: "1.12.1"`. |
| 4 | `runner check <fresh>` reports newest 1.12.1 from `./skills` | live `runner check` | Pass — store `./skills`; versions `1.12.1, 1.1.0, 1.0.0`; newest `1.12.1`; `up_to_date`. |
| 5 | Docs synced: ARCHITECTURE §5 (three vendored versions) + §10 rows; README EN+繁中; structure/{directory,design}; ai-guideline | grep + read | **Fail → (re-fix) → Pass**. First pass: README(EN+繁中), directory.md, design.md, ai-guideline.md correct, but ARCHITECTURE §5 still said "Two versions" (v1.12.1 omitted in EN + 繁中 overview) and §10 rows were duplicated. I1 re-fixed `docs/ARCHITECTURE.md` only; re-verify confirms §5 (EN line 77–78) now lists three versions incl. `skills/v1.12.1` (current baseline), 繁中 overview includes `v1.12.1`, and §10 rows appear exactly once. |

## §7 prohibition re-check
- No governance logic duplicated in the runner: `gates.py` holds no risk matrix; every halt is delegated to the vendored skill's `scripts/halt_gate.py` via subprocess. Confirmed live: fresh run halted at `before_merge_or_release` from the skill's always-halt rule.
- No skill markdown copied into `src/` (none found). The vendored offline store (`skills/v1.12.1`) is the CHG-20260617-05 override, still bounded (verbatim `git archive` extraction, no fork).
- V1 tool-lockdown and red-line halts unchanged.

## Structure-consistency check
- [x] directory.md lists `skills/v1.12.1/`; design.md records config default 1.12.1; ARCHITECTURE §5 now lists three vendored versions — all consistent with the implementation.

## Fix → re-verify loop
- Round 1: criteria 1–4 + §7 Pass; #5 Fail (ARCHITECTURE.md §5 EN + 繁中 stale, §10 duplicated).
- Fix (I1, docs-only, `docs/ARCHITECTURE.md`): three versions in §5 EN + 繁中 overview; de-duplicated §10 rows. Suite stayed green (90 passed, 3 skipped).
- Round 2 (V1 narrow re-verify of §5 + §10): Pass.

## Summary
The store vendors v1.12.1, config default and first-run lock are 1.12.1, `check` resolves it from
the offline store, docs (including ARCHITECTURE §5 EN + 繁中 and de-duplicated §10) are synced, and
§7 governance is untouched. Full suite green (90 passed, 3 skipped). **CHG-20260703-01 Accepted.**
