# ACC-20260823-15 — Acceptance: correcting the README against what the code actually does

- Target CHG: CHG-20260823-15 (two new tests that walk the real argparse parser and the real README:
  every flag the README names exists, and global flags come before the subcommand)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** — no code changes.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass, and the two new tests were proven to bite by injected drift.** One gap the
  change did not close, and did not claim to: the diagram's *completeness* is still unchecked.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_documented_numbers.py -q -p no:randomly
# injected drift, performed and reverted by the fable engine:
#   add a `--seatz` flag to a README example              -> test_every_cli_flag_the_readme_names_actually_exists fails
#   append a post-subcommand `--config` to a README line  -> test_the_readme_puts_global_flags_before_the_subcommand fails
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Where | Test that bites | Result |
|---|---|---|---|---|
| 1 | The diagram draws every node including `review_failed` and `acceptance_failed` | `README.md:80`, `:83` — both drawn, returning to `next_module`; `feedback` branches on more/done at `:87-88` | **none for completeness** — see below | **True today, unguarded** |
| 2 | `--config` comes before the subcommand in every example | `README.md:476` and every `runner --config …` line | `test_documented_numbers.py::test_the_readme_puts_global_flags_before_the_subcommand` `:298-314` | **Pass, proven** — fails on an injected misplaced `--config` |
| 3 | Every flag the README names exists, checked by walking the parser, its subparsers and aliases | `test_documented_numbers.py:266-296`, introduced in commit `ee54cc4` — this change's own commit | `::test_every_cli_flag_the_readme_names_actually_exists` | **Pass, proven** — fails on an injected `--seatz` |

Both new tests were empirically falsified and restored. That is a stronger result than "the tests
pass": a documentation test that cannot fail is decoration, and these two can.

## The gap this change left open

**No test asserts that every graph node id appears in the README diagram.** Only the numeric "24
nodes" claims are recomputed.

The consequence is specific and worth stating: a node dropped from the drawing while the count
sentence stayed correct would pass CI. The diagram is complete today — the `fable` engine checked
every `graph.NODES` id against the section directly — but it is complete by inspection, not by
construction.

Neither engine treats this as a defect of the change: task 1's done-when is about what the diagram
draws, and it draws it. Recorded because a reader of ACC-20260823-14's "every figure is recomputed"
would otherwise assume this one is.

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| The two parser tests exist and bite | reported present; **could not confirm they bite** | **injected drift into both and watched them fail** | **fable's evidence carries it** |
| Diagram completeness | **FAIL** — "no test verifies the README diagram contains every graph node, gate, terminal and branch label as claimed" | same fact, read as an unclosed gap rather than a false claim | **Fact agreed; recorded as a gap** |
| Overall | FAIL | PASS | **PASS** — the change's own two tasks are done and proven; the completeness check was never among them |

The split here is the mirror of ACC-20260823-12's: the same fact, weighed against what the change
undertook to do. Recorded both ways so the disagreement survives for the next reader.

## Not closed by this record

**A completeness check for the README diagram.** Needs its own CHG, and would close the gap ACC-14
and this record both name.
