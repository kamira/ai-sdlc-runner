# ACC-20260823-09 — Acceptance: the two items the last change carried

- Target CHG: CHG-20260823-09 (a panel answered entirely by one backend is disclosed; declining
  high-risk mode for one run puts the seat floor back)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions, no sight of each other's findings.
- Date: 2026-08-27 (UTC+0)
- Risk: **medium** — no change to the red lines.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** Both carried items are closed, and both engines independently confirmed the
  harder half: the disclosure does not **over**-report, which is what makes it worth having.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_panel_diversity.py tests/test_settings.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | A panel answered by a single backend is detected | `_note_panel_diversity` `engine.py:1012`, appends at `:1028` | `walk` at `engine.py:1409`, `:1434`, `:1496` | `test_panel_diversity.py::test_one_backend_answering_every_seat_is_recorded` | **Pass** |
| 2 | It reaches the operator | `RunReport.single_model_panels` `engine.py:307`, serialised `:356` | CLI prints `single model:` at `cli.py:713-714` | `test_panel_diversity.py::test_the_cli_prints_it` — end-to-end through `cli.main` | **Pass** — cutting either the report field or the print fails it |
| 3 | It is **not** over-reported | `engine.py:1021` — a one-seat panel returns silently; `engine.py:1026` — only `len(kinds) == 1` | same callers | `::test_a_one_seat_panel_is_not_called_single_model`; `::test_two_of_three_seats_sharing_a_model_is_not_flagged` | **Pass** — a disclosure that fires on every panel tells the operator nothing, and this one does not |
| 4 | The CLI does not print it when seats are routed | same | `cli.py:713` | `::test_the_cli_does_not_print_it_when_seats_are_routed` | **Pass** |
| 5 | `--no-high-risk-mode` restores the floor without prompting | `cli.py:640-650` — `seats = None`, prints `opening 3`, never reaches `tui.confirm_high_risk` `cli.py:652` | `cli.main` | `test_settings.py::test_declining_puts_the_floor_back_not_merely_the_count` | **Pass** — asserts no bypass recorded, `opening 3`, and at least the floor answered |
| 6 | Declining does not prompt | as above | — | `test_settings.py::test_a_saved_bypass_can_be_declined_for_one_run` | **Pass, structurally proven** — `tui.confirm_high_risk` reads stdin via `input()`, which raises under pytest, so a prompt would fail the test |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Disclosure recorded and printed | PASS | PASS | **Agreed** |
| Not over-reported | PASS — named both silences | PASS — named both silences | **Agreed** |
| Floor restored on decline | PASS | PASS, and proved the no-prompt claim structurally | **Agreed** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

The CHG's own "Not done" — the backend-versus-model phrasing question — is disclosed there rather
than claimed, and this record does not resolve it either. Suite counts in the CHG's Evidence section
have drifted with forty-plus later changes; the two test files it rests on currently hold 56 tests
and all pass.
