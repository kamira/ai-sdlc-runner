# ACC-20260823-16 — Acceptance: the README went to the seats, and did not pass

- Target CHG: CHG-20260823-16 (the origin round-trip fix, a runnable example, the answer contract
  written into a file that runs, a Known gaps section, and both seat verdicts committed whole)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **medium** — one real code change to a security check.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass with a reservation.** Every mechanism is wired and bitten. Task 2's done-when
  said the example *"drives all 24 nodes"*, which was never true of a green run — it visits 20 — and
  the correct figure appears one line later in the same record.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_server.py tests/test_examples_run_from_anywhere.py tests/test_documented_numbers.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | An Origin is **parsed and round-tripped**, never prefix-matched | `_loopback_origin` `server.py:82-116` | `_guard` `server.py:643` | `test_server.py::test_a_lookalike_origin_is_refused` — four cases, including `http://[::1].evil.example` | **Pass** — the exact-match fix is the change's one real security correction |
| 2 | Our own origins are still accepted | same | same | `::test_our_own_origins_are_still_accepted` — four cases | **Pass** — both directions, which is what the eight tests the record claims amount to |
| 3 | A runnable example exists | `examples/minimal/{plan.json,runner.yaml,agent.py}` | — | `test_examples_run_from_anywhere.py::…[minimal]` — ran green, 10/10 in the file | **Pass** — created at the repo root by commit `49812ac`, relocated to `examples/minimal/` by CHG-20260823-36, a deliberate later move |
| 4 | The answer contract is written in a file that runs | `examples/minimal/agent.py:9-16` — verdict / modules / module / survey-versus-panel | exercised by the example run tests | same | **Pass** — a contract in an executable file cannot quietly diverge from the contract |
| 5 | Known gaps states what is **not** enforced | `README.md:636-672` — `halt_independent` `:642`, unbounded seat panel `:647`, review-before-merge as practice `:652` | — | — | **Pass** |
| 6 | Both verdicts committed whole and indexed | `docs/design/reviews/readme-codex-seat.md`, `readme-fable-seat.md`, indexed at `docs/design/reviews/README.md:16-17` | — | — | **Pass** — the verdicts that failed it are in the repo, not summarised away |
| 7 | The prose corrections landed | token exemptions `README.md:387-390`; "visits 20 of the 24 nodes" `README.md:484`; test count now stated dynamically | — | `test_documented_numbers.py::test_the_readme_says_how_many_nodes_a_run_actually_visits` | **Four of ten spot-checked — see below** |

## The reservation

Task 2's done-when says the example *"drives all 24 nodes"*. A green run visits **20 of 24** — the
record's own transcript says "visited 20 nodes" — and a later test now pins exactly that
(`test_the_readme_says_how_many_nodes_a_run_actually_visits`).

The four nodes a green run does not reach are the failure paths, which is the whole point of a green
run. So the mechanism is right and the sentence is wrong, in a record that states the right number
one line further down. Recorded because a done-when is the thing a task is ticked against, and this
one was ticked against a sentence that could not be satisfied.

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Exact loopback-origin validation, runnable example | PASS | PASS, example run green | **Agreed** |
| The prose corrections | called "documentary rather than executable" and left unverified | **spot-checked four of ten directly**, left six untraced and said so | **Partial verification, recorded as partial** |
| Task 2's "all 24 nodes" | not raised | **found it**, with the record's own contradicting line | **fable's finding stands** |
| Overall | PASS_WITH_RESERVATIONS | PASS_WITH_RESERVATIONS | **PASS_WITH_RESERVATIONS** |

## Not closed by this record

**Six of the ten prose corrections were not individually traced** to the pre-change text. Neither
engine claims them. Stated rather than rounded up to "the corrections landed".
