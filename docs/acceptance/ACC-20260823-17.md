# ACC-20260823-17 — Acceptance: every conversation stored by project, and exportable

- Target CHG: CHG-20260823-17 (an append-only conversation log per project, three pluggable backends,
  and export in the operator's chosen format)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **medium** — a new durable artefact.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Superseded in its headline, passing in its core.** Two of the three backends and the
  Mongo locality rules were **deliberately removed** by CHG-20260823-35, and the default moved from
  `file` to `sqlite` by CHG-20260823-41. Everything this change built that survived is wired and
  green.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_conversations.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details — what survived

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | An append-only conversation log | `Conversation.turn` `conversations.py:944` | `engine.ask` `engine.py:590`, `engine.answer` `:631` | `test_conversations.py` walk-level tests | **Pass** |
| 2 | An operator's decision is a turn like any other | `conversations.decision` `conversations.py:995` | `engine.py:1277`, `:1293`, `:1302`, `:1565` | `::test_the_engine_records_the_decisions_the_report_records` | **Pass** |
| 3 | A project is required, and hashed before it is ever a path | `project_id` `conversations.py:120` | `conversations.py:817`, `cli.py:517` | covered in `test_conversations.py` | **Pass** — this is what later let an attachment filename stop being able to trip the skill scanner |
| 4 | Export in the operator's format; CSV defused; fences computed | `export_conversation` `conversations.py:1079`, `_fence` `:1093`, `_defuse` `:1139` | `cmd_export` `cli.py:501-519`, parser `cli.py:1045-1056` | `::test_a_csv_cell_cannot_execute_as_a_formula` | **Pass** |
| 5 | A failed store write never fails a run, and is never silent | `_guarded` `conversations.py:1026-1047` — stderr at the moment it happens | `engine.py:1100-1105`, on **every** exit from `walk` | `::test_a_failed_store_write_never_fails_a_run_and_is_never_silent`; `::test_a_failed_store_write_reaches_stderr_at_the_moment_it_fails` | **Pass** — both halves, and the second is the one that stops "never fails a run" from meaning "swallows it" |

## What was superseded, and by what

| This change claimed | Replaced by | Where it stands now |
|---|---|---|
| Three pluggable backends: `file`, `tinydb`, `mongo` | **CHG-20260823-35** | `BACKENDS = ("sqlite", "file")` `conversations.py:80`; retired kinds refused by name `:764-767`; `test_conversations.py::test_nothing_reaches_for_the_removed_packages` asserts no `pymongo` or `tinydb` import survives |
| Mongo URI locality rules; forced `directConnection=true`; `--store-uri` / `--store-remote` | **CHG-20260823-35** | gone with the backend that needed them |
| `file` as the default store | **CHG-20260823-41** | `sqlite` is the default; the file store is the legacy and import path (`cli.py:1040`) |

None of these is a failure of this change. The CHG **itself disclaimed ever having run the Mongo
backend**, which is the disclosure that makes its later removal a tidy-up rather than a discovery.

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Logging, decisions, export, guarded writes | PASS | PASS | **Agreed** |
| The three backends | **FAIL** — "claimed TinyDB and Mongo backends are absent from the current code" | **SUPERSEDED** — named CHG-35 and CHG-41 as the changes that removed them | **fable's reading is correct** — the codex engine was working from a brief that did not yet distinguish supersession from failure, which was a gap in the brief and is recorded as one in the round record |
| Overall | FAIL | SUPERSEDED | **SUPERSEDED** |

## Not closed by this record

Nothing. A change whose surviving core is green and whose removed parts were removed on purpose is
closed; the ledger, not this record, is where the removals live.
