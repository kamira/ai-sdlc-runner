# ACC-20260827-12 - Acceptance: an acceptance record's reproduction command names the wrong group

- Target CHG: CHG-20260827-12
- Verifier: **self** (the session that built it). Permitted at this risk grade, and disclosed rather
  than dressed up: every change in this batch is low, and none touches the red lines, the gate
  matrix or the flow. The independent work that produced these changes was the acceptance round of
  2026-08-27 - two engines that had built none of the code they read. What follows is the
  implementer checking their own fix, which is worth exactly what it is worth.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** - documentation
- Conclusion: **Pass. Both commands are printed, and the correction says what was wrong.**

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python tools/mutation_check.py --only importer
PYTHONUTF8=1 PYTHONPATH=src python tools/mutation_check.py --only cli
```

Batch-wide: `pytest tests/ -q -rs` -> **1347 passed, 3 skipped**; `tools/mutation_check.py` -> **15 of 15 caught**;
`tools/ledger_check.py --repo .` -> passed, 86 changes.

## Check-details

| # | Check | Method | Result |
|---|---|---|---|
| 1 | The quoted commands reproduce all seven rows | read the block | **Pass** - `--only importer` (six) and `--only cli` (the seventh), each labelled |
| 2 | The correction is marked as one | read the appended note | **Pass** - it says what was wrong, who found it, and that the evidence itself did not change |
| 3 | The record is corrected in place, not rewritten | `git diff` | **Pass** - this repo's stated convention for a record that was actually wrong |

## Not closed by this record

Nothing. The evidence was always real - every row reported caught with byte-identical counts - and
what was wrong was the instruction for reproducing it.
