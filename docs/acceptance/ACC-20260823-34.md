# ACC-20260823-34 — Acceptance: a node spec field must say something, not merely exist

- Target CHG: CHG-20260823-34 (one definition of blank, enforced at two boundaries: when a plan loads
  and when a work order is rendered for dispatch)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **medium** — it refuses plans that used to run.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass, cleanly.** One definition, two boundaries, bitten from four directions
  including the over-refusal case. Both engines returned no reservation.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_plan.py tests/test_example_tide_spa.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | One definition of blank, whitespace included | `content_problem` `workorder.py:112`, with `_blank` `:100-108` | both boundaries below | `test_plan.py::test_one_definition_of_blank_serves_both_boundaries` — and `test_plan.py:304-309` pins **every field onto exactly one of the two lists** | **Pass** — the test that stops a field quietly belonging to neither |
| 2 | Enforced when the plan loads | `plan.py:132-135`, with `instructions` excluded via `at_load` | `plan.load` | `::test_a_blank_required_field_is_refused_at_the_door` | **Pass** |
| 3 | Enforced again at render, for all six | `workorder.py:168` inside `render` | `render` `workorder.py:150`, called from `engine.py:534` and `engine.py:1453` | `::test_render_does_refuse_a_blank_instructions_because_by_then_nothing_can_fill_it` | **Pass** — the asymmetry is deliberate and is itself tested, which is what makes it a design rather than an oversight |
| 4 | The `instructions` asymmetry survives | `own = spec.get("instructions") or ()` `engine.py:515`, `:530` — the evidence line the CHG cites | the walk | same | **Pass** — still there |
| 5 | A blank spec is refused before a single ask | the load boundary | the example driver | `test_example_tide_spa.py::test_a_blank_spec_is_refused_before_a_single_ask` | **Pass** |
| 6 | The refusal names the blank fields, and **not** the ones allowed to be blank | — | — | `::test_the_refusal_names_the_blank_fields_and_not_the_ones_allowed_to_be_blank` | **Pass** — the over-refusal direction, tested |
| 7 | The example plan survives the rule it documents | — | — | `::test_the_example_plan_survives_the_rule_it_documents` | **Pass** — a rule the shipped example would fail is a rule nobody keeps |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| One blank definition at both boundaries | PASS, `UNCONFIRMED: none` | PASS, `UNCONFIRMED: none` | **Agreed** |
| The intentional asymmetry preserved | PASS | PASS | **Agreed** |
| Overall | PASS | PASS | **PASS** |

Both engines returned `UNCONFIRMED: none`. That happened for three of the forty-seven changes in this
round, and this is one of them.

## Not closed by this record

Nothing.
