# ACC-20260827-10 - Acceptance: a retracted docstring survived one function along

- Target CHG: CHG-20260827-10
- Verifier: **self** (the session that built it). Permitted at this risk grade, and disclosed rather
  than dressed up: every change in this batch is low, and none touches the red lines, the gate
  matrix or the flow. The independent work that produced these changes was the acceptance round of
  2026-08-27 - two engines that had built none of the code they read. What follows is the
  implementer checking their own fix, which is worth exactly what it is worth.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** - a docstring
- Conclusion: **Pass. The retraction now covers the module rather than the one line somebody tested.**

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_plan.py -q -p no:randomly
```

Batch-wide: `pytest tests/ -q -rs` -> **1347 passed, 3 skipped**; `tools/mutation_check.py` -> **15 of 15 caught**;
`tools/ledger_check.py --repo .` -> passed, 86 changes.

## Check-details

| # | Check | Method | Result |
|---|---|---|---|
| 1 | `plan.load` no longer promises full honouring | read it | **Pass** - it says it reads the file and puts it through `check` |
| 2 | Every public docstring is covered | `test_no_public_docstring_in_the_module_still_makes_the_retracted_claim` | **Pass** - 52 passed |
| 3 | The original retraction test still holds | ran it | **Pass** - `check`'s summary line, and the quoted retraction it must keep |

## Not closed by this record

Nothing. The shape is worth carrying forward though: a retraction pinned by reading **one**
`__doc__` leaves every other docstring in the module free to keep making the claim, and this one
did for four days.
