# ACC-20260827-09 - Acceptance: KN-3's body still describes a module that was deleted

- Target CHG: CHG-20260827-09
- Verifier: **self** (the session that built it). Permitted at this risk grade, and disclosed rather
  than dressed up: every change in this batch is low, and none touches the red lines, the gate
  matrix or the flow. The independent work that produced these changes was the acceptance round of
  2026-08-27 - two engines that had built none of the code they read. What follows is the
  implementer checking their own fix, which is worth exactly what it is worth.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** - documentation
- Conclusion: **Pass. The body says what the index said, and a guard stops the pair drifting again.**

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_documented_numbers.py -q -p no:randomly
```

Batch-wide: `pytest tests/ -q -rs` -> **1347 passed, 3 skipped**; `tools/mutation_check.py` -> **15 of 15 caught**;
`tools/ledger_check.py --repo .` -> passed, 86 changes.

## Check-details

| # | Check | Method | Result |
|---|---|---|---|
| 1 | KN-3's body opens with a superseded blockquote | read it | **Pass** - matching KN-1's and KN-2's form |
| 2 | It says what survived | read it | **Pass** - `tui`'s selector and high-risk confirmation; the console is named as the successor design |
| 3 | It is kept, not deleted | read it | **Pass** - the curses conventions remain as the record of why they existed |
| 4 | An index row marked superseded requires a superseded body | `test_a_knowledge_entry_marked_superseded_in_the_index_says_so_in_its_body` | **Pass** - across every KN entry, not only KN-3 |

## Not closed by this record

Nothing. The claim CHG-20260823-01's task 6 made - "KN-1/2/3 marked superseded in body as well as
index" - is true for the first time today, four days after it was ticked.
