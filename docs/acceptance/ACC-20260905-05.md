# ACC-20260905-05 - a test three records call proof, and an inventory two documents disagree about

- Project: ai-sdlc-runner
- Target CHG: CHG-20260905-05
- Date: 2026-09-06 (UTC+0)
- Reviewed by: the operator's standing instruction, on the **conformance seat** of the twelfth
  review round. Two of its statements were re-measured and one of them was wrong; both are recorded.
- Conclusion: **Pass**

## Criteria

| # | criterion | evidence |
|---|---|---|
| 1 | The scan test fails for **either** wrong answer | `31 passed` -> `1 failed` under the exemption |
| 2 | The dead parameter is gone | `deploy-to-prod.md` measured `()` on both platforms |
| 3 | The module docstring says what is true of the path | leaf vs store directory, both named |
| 4 | `path_for`'s docstring describes `path_for` | the moved sentence lives on `order_paths` |
| 5 | "it cannot" is narrowed to what is enforced | identity, for anything through `add` |
| 6 | Every re-anchored citation names something that exists | 13 anchors, checked by AST |
| 7 | Both module inventories name the whole package | 20 of 20 in each |
| 8 | Every change id cited in `src/` can be followed | 104 ids, one via a link |
| 9 | Each fix has a test that fails when reverted | `--only doc-truth`: **4 of 4 CAUGHT**, 1 retired |
| 10 | The suite is green | `2204 passed, 4 skipped` |
| 11 | The README count follows the suite | `--collect-only` -> 2208 |

## Evidence

### The overclaim, before and after

Three records - `CHG-20260823-11` task 17, `ACC-20260823-11` row 17, and the test's own docstring -
say `test_both_directions_of_the_scan_survive` *"fails for either wrong answer."* With the exemption
the module docstring calls *"worse than the bug"*:

```
_TARGET_FIELDS = ("expected_outputs", "workdir")

before   pytest tests/test_attachments.py -> 31 passed
after    pytest tests/test_attachments.py -> 1 failed, 46 passed
```

The property was real and guarded - by `test_false_stops.py`, not by the test the three records
name. It is repaired rather than replaced: giving it a sibling would have left three records
pointing at the version that proves nothing.

### Two statements from the seat, re-measured

**Wrong.** The seat reported *"README:605-617 already has the correct list."*

```
src/ai_sdlc_runner holds            20 modules
docs/structure/directory.md named   10
the README's own Layout block named 12   (absent: effects, paths, probes, sandbox,
                                          settings, ship, tui, worktree)
```

Both were incomplete, and incomplete differently. The repair could not be "copy the README's list
across" - that is how the disagreement arrived. Both are completed, and the guard compares each
against `src/` rather than against the other.

**A false positive of the seat's own pattern, and it was right about that.** `store.py:3` cites
`CHG-20260823-19` with no record file. The seat called it a false positive; measured, the citation
is a markdown link to `docs/design/sqlite-only.md`, that file exists, and it carries the id. A
reader can follow it.

That finding then corrected **my own guard**: the first version asked whether
`docs/changes/<id>.md` exists and called this unresolvable. The rule's subject was the record file;
the property is whether a reader can follow the citation. The same class of error the whole round is
about, in the guard written during the round.

### Thirteen symbol anchors, and two of them were wrong when written

```
attachments.py Store.add ok    stored_name ok      engine.py _brief ok
paths.py check ok  real ok     store.py _connect MISSING    models.py save ok
settings.py save ok            server.py Operator.mint ok   Runner._walk_once ok
cli.py cmd_models MISSING      cmd_settings ok
```

`store.py::_connect` and `cli.py::cmd_models` were written from memory and name nothing. Corrected
to `store.py::connect` and `cli.py::session_factory` by reading the enclosing symbol at each cited
line. **A symbol anchor that names nothing is no better than a dead line number**, which is why the
anchors were checked by AST rather than by eye.

The citations they replace were dead at `2f26c94` and **moved again inside this round**:

```
at 2f26c94                          on this branch
attachments.py :205  a docstring    attachments.py :205  size=len(data), instruction=…
server.py      :503  out = write()  server.py      :503  self._publish()
```

### Two mutations reported NOT CAUGHT, and both were real gaps in my own guards

```
NOT CAUGHT the inventory rule reads one document instead of both
NOT CAUGHT a cited change id no longer has to be followable
```

Same cause: the live data is now clean, so narrowing the rule changes nothing observable. This is
the shape `CHG-20260904-19` retired three mutations for.

- The **resolution rule** was extracted into `unresolvable_among(cited, records, followable)` so a
  planted id can be pointed at it, and the mutation re-anchored onto that. It is caught now. Left
  inline the rule was unfalsifiable, and reported NOT CAUGHT against a rule that was working.
- The **inventory scope** mutation is **retired with its reason**. Both documents are complete, so
  dropping one from `INVENTORIES` is unobservable, and re-anchoring it onto a live gap would mean
  leaving a document wrong on purpose. What is pinned instead is `modules_missing_from`, pointed at
  planted text.

## Verdict

**Pass.** The sentence three records call proof now proves what they say it does; two documents that
disagreed about what the package is now agree with the package; thirteen dead pointers name symbols
that exist and were checked; and the two guards added here fail when they stop being true - one
after being rewritten because its first version measured the artefact instead of the property.
