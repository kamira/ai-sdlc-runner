# ACC-20260903-23 — Acceptance: a red line a person typed

- Target CHG: CHG-20260903-23
- Verifier: self (main agent = implementer)
- Found by: the **defect seat** of the CHG-20260901-15 **fifth**-draft review, findings L-24 to L-28.
- Date: 2026-09-03 (UTC+0)
- Risk: high — one of these is the red-line scan not seeing what a person typed
- Conclusion: **Pass.** Defects twenty-four through twenty-seven, and the first **P0** violation this
  review series has found.

## The one that matters

**FR-19 is P0**: *"A node's own brief — every field of it — is read against the red lines before
anything else."* Measured before the fix:

```
red prose in the PLAN's node spec      -> stopped at engineer_build
the SAME prose typed by the OPERATOR   -> ran to merge
```

Identical text, identical field, identical work order. `_order_for` merged the operator's words into
a **local copy**; `_spoken_halt` read the original. And `_spoken_halt`'s own docstring says choosing
which fields to read *"was the mistake, and it was the same mistake twice"* — this was that mistake
a **third** time, one layer out: not which fields, but which **copy**.

After: a typed red line and an attached production path both stop the run at `intake`, and the
reason names the field.

## Test-the-test

| | revert | result |
|---|---|---|
| L-26 | `_spoken_halt` reads `cfg.node_specs` again | **2 failed** — the typed line and the attachment |
| L-26 structural | — | a third reader merging its own copy fails `test_the_scan_and_the_work_order_read_one_brief` |
| L-25 | read `undecided` alone | **1 failed** — the incomplete stop is accepted as a gate |
| L-24 | pass no `held` | **1 failed** |
| L-28 | drop the tally | **1 failed** |

## Six reds, and every one was a real signal

The first full run came back `6 failed`. Not one was noise:

1. **Two tests pinned to `_finish`'s argument list** while their names are about control flow —
   *"the suspended report is returned, not yielded"*. Relaxed to check what they are named for.
2. **`test_a_declared_class_is_exported_as_the_operators_act` asserted `all(...)`** over every
   relaxation — so it said *every* relaxation is a person's act. `conversations.relaxation`'s own
   docstring says there are two kinds, and L-28's is the runner's. Narrowed to the class's act, with
   a new reverse assertion that the runner's stay the runner's.
3. **The `examples` mutation anchor moved a fourth time** — `return _Process(` became
   `process = _Process(` so its tally could be handed over, shifting the continuation lines three
   columns. Re-anchored; `mutation_check --only examples` still reports **CAUGHT**.
4. **`test_the_engine_records_the_decisions_the_report_records` refused L-24's first form.** Its
   rule is *"every place the report gains an operator **decision**, the conversation gains a turn"*,
   enforced by requiring a `conversation.decision(` beside every `confirmations.append`. My three
   lines were not decisions — they are the run reporting that a decision was **never reached** — so
   they belong in `relaxations`, which reaches the durable record by another route. A guard written
   for a different change told me which ledger I was in.
5. The README count.

## Check-details

| # | Task | Result |
|---|------|--------|
| 1 | The scan reads the brief the answerer receives | **Pass** |
| 2 | The merge lives in one place | **Pass** — `_brief`, with a structural test |
| 3 | A typed line and an attachment both stop it | **Pass** |
| 4 | An incomplete stop is neither a gate nor a tie | **Pass** — three shapes, three answers |
| 5 | Its refusal names what is waited for | **Pass** — and names `/run/instruct` and what is missing |
| 6 | All four ledgers say when they go unused | **Pass** |
| 7 | An unsandboxed run is recorded | **Pass** |
| 8 | Before the closing turn | **Pass** — measured: appended at line 36 of `_finish`, written to the conversation at 63 |
| 9 | Each fix fails without it | **Pass** |
| 10 | README follows the suite | **Pass** — 1966 → 1971 |
| — | Full suite | **Pass** — **1966 passed, 4 skipped** (1971 collected), pytest exit 0 |
| — | Ledger lint | **Pass** |

## Reservations (disclosed, not waived)

1. **Written by the implementer**, on defects somebody else measured.

2. **L-26 stops runs that used to proceed, on every platform.** The operator's instructions are
   appended to *every* ask, so one red word in a typed instruction now halts at `intake` — the first
   node that does work. That is FR-19 finally being kept and it will read as a new refusal.

3. **L-28 adds a relaxation line to nearly every run on Windows**, which has no sandbox mechanism.
   The honest reading is that this has been true all along and nothing said so; it will still look
   like a regression to whoever sees it first.

4. **L-24's entries are prose in a list nothing parses.** Four ledgers now report; a caller
   distinguishing them has to read English.

5. **L-27 is not fixed and is named in the CHG** — six snapshot keys reach no front end, and
   `RunState.error` is never cleared on a successful walk, so a finished run shows the previous
   walk's error box. The first half is CHG-20260901-15's task 24; the second is a real defect whose
   fix is in the same function, so they belong together.

6. **My first form of L-24 was wrong and a guard caught it.** Second time in two changes that a fix
   I believed correct was refused by something already written down — and both times the thing that
   refused it was a sentence, not a type error.
