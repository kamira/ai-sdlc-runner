# ACC-20260703-04 — Acceptance: resident dashboard input accepts any language (UTF-8 / CJK)

- Project: ai-sdlc-runner
- Target CHG: CHG-20260703-04
- Verifier: V1 (independent; ≠ implementer I1; model opus, Explore/read-only tool loadout)
- Date: 2026-07-03 (UTC+0)
- Skill: ai-sdlc v1.12.1
- Risk: low (single-file input-handling fix in the curses glue; no governance logic; test-covered pure helper)
- Conclusion: **Pass**

## Check details
| # | Check | Method | Result |
|---|-------|--------|--------|
| 1 | Root cause removed | grep `chr(key)`/`0 <= key < 256` in dashboard.py; inspect `run_resident` | Pass — old byte path gone; `get_wch()` at L685; `locale.setlocale(LC_ALL,"")` at L657 before `curses.wrapper`. Remaining `getch()` (L258) is in the unrelated read-only `_curses_view`, not `run_resident`. |
| 2 | `apply_key` correctness (pure, headless) | import + direct calls with CJK/mixed/Enter/Backspace/control | Pass — `("","你")→("你","edit")`; `"你好"` across 2 calls; `"a你b好c"` char-by-char (5 chars, no mojibake); Enter `\n`/`\r`→`(buf,"submit")`; Backspace `\x7f`/`\b`/`\x08` removes one whole char (`"你好"→"你"`); empty-buffer backspace→`("","edit")`; `\x01`→`(buf,"noop")`. |
| 3 | Special int-key branches intact | read `run_resident` int branch | Pass — F2, `(KEY_ENTER,10,13)`, `(KEY_BACKSPACE,127,8)` retained; arrows/other ints fall through (`continue`, no crash). |
| 4 | No governance / selector regression | `git diff main -- orchestrator.py gates.py`; tests for render_layout/parse_command/approve_decision | Pass — orchestrator.py & gates.py: zero diff. Fix confined to dashboard.py + tests (+ docs). Prior helpers' tests still green. |
| 5 | Full suite green | `pytest -q` plain and with `AI_SDLC_SKILL_PATH` | Pass — 146 passed / 6 skipped (plain); 152 passed / 0 skipped (with skill path). No regressions vs 133/6 baseline. |

## §7 prohibition re-check
- No governance/red-line logic altered: orchestrator.py and gates.py have zero diff vs main.
- HALT-gate approval path unchanged: `approve_decision` and its mapping tests untouched and green; unresolved/cancelled answers still map to reject, never implicit approve.
- Change is presentation/input-glue only, per the module's stated "no governance logic" contract.

## Structure-consistency check
- `docs/structure/logical.md` updated to note UTF-8 / wide-char resident input; README/ARCHITECTURE one-liners present in the diff. Implementation (`get_wch()` + `apply_key`) matches the documented Modification Guide (steps 1–4 all ticked and verified).

## Summary
The CJK/any-language input defect is fixed at its root: `run_resident` now reads whole decoded
characters via locale-aware `stdscr.get_wch()` and routes them through the pure, headless-tested
`apply_key`, replacing the byte-at-a-time `getch()`/`chr()` path. CJK, mixed ASCII+CJK, Enter,
character-level Backspace, and control-char handling are all verified headlessly. Special int keys
(F2/Enter/Backspace) remain; arrows are safely ignored. Governance (orchestrator/gates) is provably
untouched. Full suite green (146+6 plain / 152 with skill path). **Accepted.**
