# ACC-20260617-02 — Interactive arrow-key menu

- Project: ai-sdlc-runner
- Date: 2026-06-17
- Target: CHG-20260617-02 (interactive menu for the runner CLI)
- Verifier: V1 (independent acceptance; ≠ implementer I1)
- Risk: low (inherited from CHG-20260617-02)
- Baseline source: CHG-20260617-02 modification guide; ai-guideline §8 conventions
- Conclusion: **Pass**

## Check details
| # | Acceptance criterion | Source | Result | Evidence |
|---|----------------------|--------|--------|----------|
| 1 | Arrow-key selectable menu exists (stdlib curses) with a numbered fallback | CHG-02 goal | Pass | `tui.select` → `_curses_select` (curses ↑/↓/Enter/q) and `_numbered_select`; `_want_curses` gates on TTY + curses import |
| 2 | Zero new runtime dependencies | CHG-02 decision; NFR maintainability | Pass | AST import scan of `tui.py`: external imports = NONE (stdlib only); `pyproject` dependencies still `[]` |
| 3 | Bare `runner` opens the menu; explicit `runner menu` also works | CHG-02 (both invocations) | Pass | `main()` routes no-command to `cmd_menu`; `menu` subparser present; both exercised via piped stdin |
| 4 | Existing `run`/`migrate`/`status` behavior unchanged | CHG-02 backward-compat | Pass | `run` still prints `status: halted_for_approval`; subparsers only made non-required (additive) |
| 5 | Menu adds no governance logic; gates/red-lines still apply | CHG-02 risk control; §1.8/§7 | Pass | Menu "Run" dispatches to `cmd_run` → same orchestrator → HALTs at `before_merge_or_release` (always-halt action `release`) |
| 6 | Non-TTY / forced fallback works | CHG-02 | Pass | `AI_SDLC_NO_CURSES=1` and piped stdin drive the numbered menu end-to-end; `test_select_uses_fallback_when_forced` |
| 7 | Tests cover the new logic | §6 convention | Pass | `tests/test_tui.py` (parse-choice matrix, numbered select, cancel, EOF, empty options, prompt default); full suite **33 passed** |
| 8 | Structure docs synced | §1.2 docs-are-truth | Pass | directory.md (+tui.py), logical.md (+tui module + menu flow), design.md (+tui.select/_parse_choice/cmd_menu) updated |

## §7 prohibition re-check (unchanged by this change)
- No skill markdown copied; no hardcoded halt matrix/role table in `tui`/`cli` (menu only collects input and calls existing handlers). Pass.
- V1 tool lockdown, submodule pinning, red-line halts: untouched and still enforced. Pass.

## Unmet items & handling
None. (The curses rendering path is not unit-tested — it needs a real terminal — but the fallback that
runs in pipes/CI is fully tested, and the curses path degrades to the fallback on any error.)

## Structure consistency check
- [x] Implementation matches the updated structure docs (`tui` module, `cli → tui` dependency, menu flow).

## Summary
Deliverable. The interactive menu gives the selectable-list UX requested, with zero new dependencies
and a graceful non-TTY fallback, and — importantly — it does not weaken governance: launching a run
from the menu still stops at the red-line gate. CHG-20260617-02 set to **Accepted**.
