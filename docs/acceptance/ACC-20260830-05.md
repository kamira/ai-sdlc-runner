# ACC-20260830-05 — Acceptance: the first one not verified by the implementer

- Target CHG: CHG-20260830-05
- Verifier: ~~a four-seat review panel — `conformance`, `defect`, `risk`, `idiom` — over the six
  changes this corrects~~ **self (main agent = implementer)**, like every record before it. The
  panel verified the six changes this *corrects*; it did not read the corrections, as reservation 2
  says in the same document. Naming it here as the verifier of this change was the overstatement
  class the veto was about. Corrected in CHG-20260830-06.
- Reviewed by: a four-seat panel over `d9ad50f..47beaac`, adjudicated by
  `policy.adjudicate(voices="seats")` — the input to this change, not its verification.
- Date: 2026-08-30 (UTC+0)
- Risk: medium — three `src/` files, one of them repairing a fix shipped broken
- Conclusion: **Pass on the corrections.** The panel's own verdict on what it reviewed was
  **fail**, by veto; this change is what that verdict asked for.

## The panel's verdict, and what produced it

```
{"outcome": "fail", "reason": "veto from ['conformance']", "vetoed": ["conformance"]}
```

| seat | verdict | findings |
|---|---|---|
| conformance | **fail** | 6, one of them a veto |
| defect | **fail** | 11 |
| idiom | **fail** | 11 |
| risk | pass | 7, ranked, disclosed |

Each seat answered only its own question, none saw another's answer, and each was told that finding
nothing was an acceptable answer. Twenty-eight findings from the three failing seats; all addressed.
Risk passed and its seven are disclosures, one of which is acted on here because it names a failure
that actually occurred during the review.

## The finding that justifies the whole exercise

CHG-20260828-24 shipped a `src/` fix that **did not fix anything**, with a test that could not tell,
and an acceptance that recorded it as verified. Fourteen changes in this session carried the
reservation *"Verified by the implementer... weaker than an independent seat."* The first time an
independent seat looked, that reservation turned out to be describing a live defect in `main`.

Reading `plain_in` carefully — which I did, twice, while writing it and while writing its record —
does not surface it. The defect is in what `str(OSError)` contains, which is a fact about the
standard library rather than about the code on the page. The seat found it by constructing the
input; I had constructed a plausible one instead.

The self-verification that preceded the panel cost nothing and found none of it. What the panel
returned was one live `src/` defect, one veto on a permanent record, five tests that could not fail,
one mislabelled mutation, six overstated record claims, and a concurrency failure created while
running them. The second round, over these corrections, returned a further thirty-six — including
that four of the eleven rows below were themselves wrong (CHG-20260830-06).

## Verification of the corrections

| # | Check | Result |
|---|-------|--------|
| 1 | `plain_in` strips a real `OSError`'s prefix | **Pass** — driven by an actual raised error |
| 2 | Both spellings, longest first in each | **Pass** — four cases |
| 3 | The third recording site is fixed | **Pass** |
| 4 | The count is no longer made by hand | ~~**Pass** — an AST walk over `src/`~~ **WRONG.** The walk reached **one** of the three live sites: it matched the substring `exc` in the append's argument, so every message built into a variable first was invisible — including `conversations.py:1113`, one of the two CHG-20260828-24 actually fixed. A rule keyed on a name, in the change about rules keyed on names. Found by three seats independently; corrected in CHG-20260830-06 |
| 5 | The registry says what the change found | **Pass** — the veto's subject |
| 6 | Six tests now drive what they name | **Pass** |
| 7 | The reorder mutation reorders | **Pass** — caught for the right reason |
| 8 | A `Path` is refused by name | **Pass** |
| 9 | The stale `pragma: no cover` is gone | **Pass** |
| 10 | Six record claims corrected in place | ~~**Pass** — struck through, not rewritten~~ **WRONG.** One of the six was struck through. Two were rewritten in place with an italic aside, one had its note folded into the prose while the row above it was changed silently, and one was edited with no marker at all. Four forms for one operation, in a change about records not saying what happened. Corrected in CHG-20260830-06 |
| 11 | The harness holds a lock | ~~**Pass** — a second run is refused, and says how to recover~~ **WRONG, and this is the veto.** `main()` calls `recover()` before its first `apply()`, so a second run deleted the first one's record on the way in and then took the lock uncontended. Measured: the second run wrote the original over the first run's live mutated file, and the first run reported `NOT CAUGHT` for a mutation its tests catch. The test carrying this row called `begin()` twice in one process — the path no real second run takes. Corrected in CHG-20260830-06 |
| — | Mutations | **Pass** — store 13/13, ship 7/7, stranded 5/5, workorder 9/9 |
| — | Full suite | **Pass** |

## §7 prohibition re-check

- No skill logic re-implemented; nothing under `skills/` exists here.
- No red-line action. `GATES`, `PERMANENT_HALT_KINDS`, `CLASSES` and the graph are untouched. The
  `src/` changes are two refusal messages and one deleted comment.

## Reservations (disclosed, not waived)

1. **The panel reviewed six changes, not fourteen.** The eight before them in this session were
   never independently read, and the ratio of findings to changes here suggests that was not a safe
   place to stop.

2. **This acceptance is again written by the implementer.** The panel judged the *work being
   corrected*; nothing independent has read the corrections themselves. Under the operator's
   standing rule this change goes back to the seats, and that round is not this record.

3. **Risk's five open observations are untouched.** `reach_of` grades every single-label host
   `internal` against its own stated rationale; the secret scan reads the query string only, so a
   credential in userinfo reaches a file; two markdown formats are now pinned by test; the
   constant-time test forecloses hashed tokens. Each deserves its own change.

4. **`plain_in` remains approximate.** It rewrites driver text rather than reconstructing the
   message from `exc.filename`. A message legitimately containing those characters outside a path
   would be altered.

5. **The AST check for recording sites is narrow.** It covers `.append(...)` onto `write_errors`
   and `store_errors`. A fourth site that records an exception some other way would not be seen —
   which is the same shape as the count it replaces, one level up.
