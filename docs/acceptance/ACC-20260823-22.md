# ACC-20260823-22 — Acceptance: the HTTP API and the database schema, written down

- Target CHG: CHG-20260823-22 (`docs/API.md` and `docs/DATABASE.md`, each pinned to the code by tests
  that read the real thing)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** — no behaviour changes.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass, and it is the model for how a document should be pinned in this repo.** The
  routes are extracted from `server.py`'s own handler bodies and diffed **in both directions**; the
  DDL on the page is **executed verbatim** and its constraints are violated on purpose. Both engines
  returned no material reservation.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_api_schema.py tests/test_database_schema.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Test that bites | Result |
|---|---|---|---|---|
| 1 | Every route the server answers is on the page | `_routes` `test_api_schema.py:24-30` parses `do_GET` / `do_POST` **source**; `server.py:710`, `:757` | `::test_every_route_the_server_answers_is_on_the_page` | **Pass** |
| 2 | The page invents no route | same, other direction | `::test_the_page_invents_no_route` | **Pass** — both directions is what makes this a pin rather than a checklist |
| 3 | The guard order Host → Origin → token is the same in the page and in the code | `_guard` `server.py:~630-640` | `test_api_schema.py:116` — asserts the order in **both** the page and the source | **Pass** |
| 4 | The documented DDL actually works | `docs/DATABASE.md` §2 SQL, conversations/turns at `:106-119` | `test_database_schema.py:47`, `:71`, `:80` — **execute the page's SQL verbatim** | **Pass** |
| 5 | The documented constraints actually refuse | same | `::test_an_orphan_turn_is_refused_because_the_page_turns_foreign_keys_on` | **Pass** — the page's own `PRAGMA` line is what makes the refusal happen, so the doc is load-bearing |
| 6 | Columns removed by three review rounds cannot come back | the absent-columns section | `::test_a_column_three_review_rounds_removed_has_not_come_back` — parametrised per column, each carrying the finding that removed it | **Pass** |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| API page tied to the live handlers | PASS | PASS | **Agreed** |
| Documented SQL executed and constraint-violated | PASS | PASS, 31+ tests green | **Agreed** |
| Overall | PASS, `UNCONFIRMED: none` | PASS, `UNCONFIRMED: none` | **PASS** |

Both engines returned `UNCONFIRMED: none`. That happened for three of the forty-seven changes in this
round, and this is one of them.

## Not closed by this record

Nothing. Drift only: the stated "15 routes" is now 17+ after CHG-20260823-25, and the schema
described as unbuilt has all five tables built since CHG-20260823-41. The pinning tests measure
reality rather than the 2026-08-25 numbers, which is exactly why the drift is harmless here and
harmful elsewhere in this ledger.
