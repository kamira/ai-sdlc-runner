# ACC-20260823-37 — Acceptance: the recording, in the demos

- Target CHG: CHG-20260823-37 (a committed, scrubbed, size-capped recording of a real console-driven
  run, rendered by the shipped exporter)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** — additive; one new page.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** All six tasks have tests that bite, and the strongest of them is the one
  asserting the page is rendered by the **shipped** exporter rather than by a private copy — checked
  out of `build.py`'s syntax tree.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_demo_pages.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | Rendered by the **shipped** exporter, not a private copy | `examples/demo/build.py:131` — `conv.export_conversation(document, "playback")` | — | `test_demo_pages.py::test_the_recording_is_rendered_by_the_shipped_exporter` — asserted out of `build.py`'s **syntax tree** at `:128` | **Pass** — a demo that quietly forks the renderer is a demo of something that does not ship |
| 2 | A committed fixture of a real run | `examples/demo/conversations/379c5b0c…/815842ec….jsonl`, 306 turns | read by `build.py` via `conv.backend("file")` | `::test_the_recording_replays_a_real_console_driven_run` — asserts two INSTRUCTION turns and a DECISION turn | **Pass** — it checks the run was *driven by a person through the console*, not merely that turns exist |
| 3 | Machine paths scrubbed | `examples/demo/make_fixture.py:50-58` — JSON-escaped Windows and POSIX patterns; refusal at `:114` | — | `::test_no_absolute_path_from_the_machine_that_recorded_it_survives` — re-checked against the **committed** fixture at `:168` | **Pass** — checked against the artefact, not against the scrubber |
| 4 | Small enough that people will clone it | — | — | `::test_the_fixture_is_not_so_large_that_nobody_will_clone_it` — a 900 KB ceiling with the 902 KB cause attached | **Pass** — a limit that carries the number that caused it |
| 5 | Line endings pinned | `.gitattributes` — `examples/demo/conversations/** -text` | — | — | **Pass** |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Fixture loaded, rendered by the shipped exporter, generated into the demo set | PASS | PASS, `UNCONFIRMED: none` | **Agreed** |
| Fixture freshness | called intentionally unverified | noted the fixture was **regenerated by CHG-20260823-40** (new id, millisecond stamps) — deliberate, and every test above still bites on the current one | **fable's detail is the fuller answer** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

Nothing.
