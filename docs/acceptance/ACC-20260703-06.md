# ACC-20260703-06 — Acceptance: align runner baseline to ai-sdlc v1.16.0

- Target CHG: CHG-20260703-06
- Verifier: V1 (independent; ≠ implementer I1; model opus, Explore/read-only tool loadout)
- Date: 2026-07-03 (UTC+0)
- Skill: ai-sdlc v1.16.0
- Risk: medium
- Conclusion: **Pass**

## Check details
| # | Check | Method | Result |
|---|-------|--------|--------|
| 1 | Vendored v1.16.0 present + correct (SKILL.md `version: 1.16.0`; `scripts/halt_gate.py`, `scripts/cross_repo_check.py`, `references/agent-hierarchy.md` "Role startup spec") | read + ls + grep | **Pass** — frontmatter `metadata.version: 1.16.0`; all three integration files present; "Role startup spec" at agent-hierarchy.md:127. |
| 2 | `config/runner.yaml` `contract_version` = "1.16.0" | git diff + grep | **Pass** — line 11 `contract_version: "1.16.0"` (was 1.12.1). |
| 3 | Store offers four versions incl. 1.16.0 newest (1.0.0/1.1.0/1.12.1/1.16.0) | `runner check` + ls | **Pass** — `versions: 1.16.0, 1.12.1, 1.1.0, 1.0.0`; newest 1.16.0. |
| 4 | Live run on FRESH project (no --skill-path/--contract-version) locks 1.16.0 from store default; 4-stage loop; red-line halt | `runner run --backend stub --risk low` + cat lock | **Pass** — `contract: 1.16.0`; stages requirement_analysis→structure_design→implement→acceptance; HALT at `before_merge_or_release`; `.sdlc-lock.json` = 1.16.0. Reproduced on 2nd fresh project. Vendored halt_gate.py reproduces the HALT (exit 10). |
| 5 | Docs synced: ARCHITECTURE §5 (four versions, 1.16.0 baseline) + §10 row; README EN+繁中 four + baseline; structure/{directory,design}; ai-guideline | grep + read | **Pass** — all live docs list four versions and 1.16.0 baseline/CHG-06; no stale "three"/"1.12.1 baseline" outside frozen CHG-01/ACC-01 history. |
| 6 | §7 prohibition: no governance duplicated in runner; runner code unchanged; suite green | git diff + grep + pytest | **Pass** — working-tree `git diff -- src/ai_sdlc_runner/` EMPTY; halt delegated to skill's halt_gate.py (gates.py subprocess); no duplicate halt_gate.py outside store; 151 passed/7 skipped (plain), 158 passed (skill-path). |

## §7 prohibition re-check (governance not re-implemented in the runner)
- Runner does **not** duplicate governance: `src/ai_sdlc_runner/gates.py` calls the skill's own `scripts/halt_gate.py` via subprocess and maps its exit-code contract; no always-halt keyword logic lives in `src/`.
- Only one `halt_gate.py` exists in the repo, inside the store (`skills/*/scripts/`); none under `src/`.
- The live red-line HALT (`before_merge_or_release`, keyword `release`) is emitted by the vendored `skills/v1.16.0/scripts/halt_gate.py` (verified by direct invocation → HALT / exit 10), not by runner code.
- CHG-06 changed **no** runner code (working-tree `src/` diff empty), so no §7 relaxation beyond the already-recorded CHG-05 vendoring override was introduced.

## Structure-consistency check
- `docs/structure/directory.md`: skills/ tree lists v1.0.0/ v1.1.0/ v1.12.1/ v1.16.0/ with v1.16.0 marked current baseline (@b4d6ef3) — matches on-disk store.
- `docs/structure/design.md`: offline-store row notes v1.16.0 as the config default (CHG-06) — matches `config/runner.yaml`.
- `docs/ARCHITECTURE.md` §5 (EN + 繁中 overview) lists four vendored versions with 1.16.0 baseline; §10 has the CHG-06 row.
- `docs/ai-guideline.md`: CHG-01 baseline note marked Superseded; new "Current baseline (CHG-06)" note added.
- README (EN + 繁中): store lists four versions; current baseline 1.16.0 / CHG-06.
- No structural drift: docs, config, and the on-disk store agree.

## Summary
CHG-20260703-06 correctly vendors ai-sdlc v1.16.0 into the offline multi-version store, bumps the
config default to 1.16.0, and synchronizes all structure/architecture/README/guideline docs, without
touching runner code or duplicating governance. A fresh project with no overrides now locks to 1.16.0
from the store default and drives the four-stage loop to a red-line halt sourced from the vendored
skill's own `halt_gate.py`. Existing 1.0/1.1/1.12 locks and the migrate path are unaffected. Full test
suite green (151 passed / 7 skipped; 158 passed against the v1.16.0 skill path). Accepted.
