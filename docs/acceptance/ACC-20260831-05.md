# ACC-20260831-05 — Acceptance: the rename happened and the body did not

- Target CHG: CHG-20260831-05
- Verifier: self (main agent = implementer)
- Reviewed by: a four-seat panel over `4105344..5bf8a7e` — `conformance`, `defect`, `risk`, `idiom`
  — each in its own git worktree, none seeing another's answer. The panel is the **input** to this
  change, not its verification; see reservation 1.
- Date: 2026-08-31 (UTC+0)
- Risk: medium — the ledger's excusal rule, the harness's recovery, three `src/` refusals
- Conclusion: **Pass on the corrections.** The panel's verdict was **fail** from all four seats,
  with three vetoes; this change is what those verdicts asked for.

## Check-details

| # | Task | Result |
|---|------|--------|
| 1 | The door count is corrected | **Pass** — 4 sites, 3 with a handler, 2 changed; struck in the Risk section and the task row |
| 2 | The fourth "nine" is struck | **Pass** — `grep "nine English negations"` returns 0 unstruck |
| 3 | The excuse window is bounded in characters | **Pass** — removing it was `98 passed`; it is `1 failed` |
| 4 | `reach_of` calls `graded_by_guess` | **Pass** — `--only reach` 12/12; every reach path unchanged |
| 5 | The assignment warning names real tables | **Pass** — a nonsense name is `1 failed` |
| 6 | The ghost remedy accepts what people write | **Pass** — 11/11 phrasings, 4 verbs + the link form |
| 7 | The guard stops contradicting `--recover` | **Pass** |
| 8 | `serve` refuses `--assignment-store none` | **Pass** — it wrote a file called `none` |
| 9 | `recover` refuses non-object JSON | **Pass** — `[]`, `null`, a bare string, a non-path path |
| 10 | Every never-true spelling is pinned | **Pass** — dropping `voided` is `1 failed`; it was green |
| 11 | A whitespace-only line is a paragraph break | **Pass** — removing the check is `2 failed` |
| 12 | The span is pinned | **Pass** — `.*?` is `1 failed`; it was `98 passed` |
| 13 | The command scan sees a mid-line instruction | **Pass** — a planted `runner rollback --to ID` is `1 failed` |
| 14 | Two false assert messages are corrected | **Pass** |
| 15 | The registry door says what to do | **Pass** |
| 16 | `Bearer-` is gone, and the eighth case named | **Pass** — 7 prefixes, and the no-prefix case in prose |
| 17 | "five real vendor tokens" is eight | **Pass** |
| 18 | The `chr(92)` regex spelling is ordinary again | **Pass** — 3 constants removed, 3 raw strings |
| 19 | The duplicate unreadable-record test is folded in | **Pass** — 46 → 45, one assertion moved |
| 20 | The two-rule statement is restored | **Pass** |
| 21 | Three stale strike-throughs get their pointer | **Pass** |
| 22 | The mutation anchors are repointed | **Pass** — `reach` and `closure` |
| — | Mutations | **Pass** — `--only stranded` **12/12**, `--only closure` **9/9**, `--only reach` **12/12**; record cleared, tree clean |
| — | Full suite | **Pass** — 1855 passed, 4 skipped (1859 collected), pytest exit 0 |
| — | Ledger lint | **Pass** — 131 changes |

## The three vetoes, and what they have in common

None is against code. All three are sentences in records about work that was done:

| veto | the record said | measured |
|---|---|---|
| 1 | "all four doors … now name the file" | 4 sites, 3 with a handler, 2 changed |
| 2 | the miscount was in "all three places" | there were four |
| 3 | the window is "at most 200 characters" | 3150 characters excused |

Veto 3 is the one worth keeping: `{0,200}` looks like a character bound and is a repetition bound,
and the reservation that existed to let a reader judge an open hole understated it by fifteen times.
A disclosure is a measurement like any other.

## The rename happened and the body did not

Two rounds claimed the bare-host disclosure *"calls the rule"*, and the second wrote *"It calls it
now"* into a test docstring. The body was byte-identical to the function it was renamed from, still
carrying its own copy of `"." not in host`, and the `#:` comment above `LOCAL_SUFFIXES` said it was
*"shared with"* a function that never referenced it.

`reach_of` calls `graded_by_guess` now, and `--only reach` is 12/12 against the same six endpoints.

## §7 prohibition re-check

- No skill logic re-implemented; nothing under `skills/` exists here.
- `src/` changes: `models.py` — `reach_of`'s single-label branch now calls `graded_by_guess` instead
  of restating it (same result on every endpoint, measured); `_SECRET_PREFIXES` loses the invented
  `Bearer-`; three docstring sentences corrected. `cli.py` — **one new control-flow path**:
  `cmd_serve` refuses `--assignment-store none` and returns 2 where it previously created a store
  file of that name; plus three refusal messages reworded (the assignment warning's table names, the
  registry door's remedy, and the `run` door's note about where the escape works). `GATES`,
  `PERMANENT_HALT_KINDS`, `CLASSES` and the graph untouched.

## Reservations (disclosed, not waived)

1. **Written by the implementer.** Ten rounds, every verdict `fail`. Nine of the ten had their
   largest finding inside the previous round's fix. This round's three vetoes were all against
   sentences I wrote about work I had done.

2. **The bystander case in `_excused` is still open.** A dead test named inside a run of names that
   ends in a removal note is excused with them. Bounded by three things now, all pinned: one
   paragraph (a whitespace-only line counts), at most `EXCUSE_WINDOW` = 400 **characters** measured
   on the match, and no backticks crossed except test names. Closing it would break the list form,
   which is the same sentence to a regex.

3. **`build_factory` still has no handler**, third round running. The risk seat reached it this
   time: editing the store while `serve` is up and posting a run gives `200` with
   `"error": "ModelError: …"` in the run's own state — a failed run rather than a configuration
   refusal, with no file named and no remedy. `server.py:633` is the same gap from the library side.
   Not fixed here because the walk's error path is the engine's, not this change's. Worth its own.

4. **`isn't done` still reads as finished**, and so do `un-merged`-style forms this round did not
   generate beyond the four added. A prefix list reaches what it reaches.

5. **The `runner <word>` scan needs a `--` on the line** to see a mid-sentence instruction. An
   instruction printed with no flag at all is still invisible. Measured: that is the price of not
   reading "the runner will" as a subcommand, and it is a heuristic, not a guarantee.

6. **`_alive`'s coupling to `_owner` is still deferred**, five rounds running.

7. **`test_concurrent_config_edits_and_model_adds_do_not_deadlock` is load-sensitive**, seen red
   under concurrent pytest processes in four rounds now, green alone.
