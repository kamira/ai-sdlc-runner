# ACC-20260901-02 — Acceptance: a correction that replaced a true statement with a false one

- Target CHG: CHG-20260901-02
- Verifier: self (main agent = implementer)
- Reviewed by: a four-seat panel over `ade3fe3..d9932b7` — `conformance`, `defect`, `risk`, `idiom`
  — each in its own git worktree, none seeing another's answer. The panel is the **input** to this
  change, not its verification; see reservation 1.
- Date: 2026-09-01 (UTC+0)
- Risk: medium — the mutation harness, the harness's recovery, the ledger's excusal rule
- Conclusion: **Pass on the corrections.** The panel's verdict was **fail** from all four seats,
  with eight vetoes; this change is what those verdicts asked for.

## Check-details

| # | Task | Result |
|---|------|--------|
| 1 | The U+3000 row is restored | **Pass** — measured `excused=True` against `38f2a39^` |
| 2 | `_import_fails` uses the file's own directory | **Pass** — `--only codecs` is 9/9; it was `3 of 9 did not report CAUGHT`, exit 1 |
| 3 | Every target is probed by a test | **Pass** — reverting the path entry is `1 failed` |
| 4 | `render_cast` drops the 3.10 keyword | ~~**Pass** — parses under `feature_version=(3,9)`~~ — that evidence passes on the broken file too: the keyword is a runtime `TypeError`, not a syntax error. **Pass** on task 5's check instead. Corrected in CHG-20260901-03 |
| 5 | The tree is swept for that keyword | **Pass** — zero live calls |
| 6 | The gate decides all four fields | **Pass** — restricting `READS` to `("owner",)` is ~~`1 failed`~~ **`3 failed`**; removing the gate entirely is 9 of 11 rows. Corrected in CHG-20260901-03 |
| 7 | The gate has its own refusal | **Pass** — it names the field and does not claim the file is unreadable |
| 8 | The absence rows read the message | **Pass** |
| 9 | `149×` is gone from the source | **Pass** — `grep -rn 149 --include=*.py` returns nothing |
| 10 | The census is one rule over ~~267~~ **269** records | ~~**Pass**~~ **Pass on the rule, not the denominator** — it excluded its own two records, which is the shape it was correcting; re-measured in CHG-20260901-03 — **and excluded its own two records again there**, the fifth instance. The census names a **commit** from CHG-20260901-04 on, because "the current tree" is not something a record can measure about itself |
| 11 | The CRLF attribution is corrected | **Pass** |
| 12 | The rename phrase accepts any new name | **Pass** |
| 13 | The constants block matches the code | **Pass** |
| 14 | The bound count agrees between docstring and record | **Pass** — six, in both |
| 15 | The four disputed revert figures are corrected | **Pass** — measured 3, 6, 11, 5 |
| 16 | "the seven vetoed records" is four | **Pass** |
| — | Mutations | **Pass** — `--only codecs` **9/9**, `--only stranded` **12/12**, `--only closure` **9/9**, `--only reach` **12/12**, `--only console` **8/8**; record cleared, tree clean |
| — | Full suite | **Pass** — 1899 passed, 4 skipped (1903 collected), pytest exit 0 |
| — | Ledger lint | **Pass** — 135 changes |

## Six bounds and one widening, re-measured

Reverting each in turn, on this tree:

| removed | rows red |
|---|---|
| `_PARAGRAPH_BREAK` | 6 |
| the sentence rule | ~~6~~ **7** |
| `_SPAN`'s backtick bound | ~~3~~ **2** |
| `EXCUSE_WINDOW` | 1 |
| the change id must exist | 1 |
| the name matched case-sensitively | 1 |
| *(case-insensitivity of the verbs — a widening)* | 2 |

The previous acceptance gave `1` for the `_SPAN` bound and `7` for the sentence rule, and counted
five bounds while the docstring counted six. The bound *count* was corrected; the two figures were
not — `7` had been right on POSIX and wrong here, because a fixture wrote through `os.linesep`
translation and the paragraph rule decided two rows that the sentence rule decides on Linux. The
fixture writes the bytes it names now, so both platforms agree, and these are the figures
(CHG-20260901-03, defect and idiom seats).

## The measurement that decides the worst finding

`_excused` loaded from `38f2a39^` — the code CHG-20260831-06 was correcting — driven directly:

| break carrying | excused |
|---|---|
| U+3000 | **True** |
| a space | False |
| nothing | False |

So the original `| excused | refused |` row was right. The strike that called it "already refused"
was executed on a previous round's say-so, and my first check of it used a fixture ending in a full
stop, which the sentence bound refused before the paragraph rule was reached — producing `False` and
appearing to confirm the strike. Restored.

## §7 prohibition re-check

- No skill logic re-implemented; nothing under `skills/` exists here.
- `src/` changes: **none.** No file under `src/` is touched by this change.
- Non-`src/` hunks: `README.md`'s test count, `tools/ledger_check.py`, `tools/mutation_check.py`,
  `tools/mutation_recovery.py`, `tools/render_cast.py`, ~~two~~ **three** test files, four existing
  records, and
  the two new ones — enumerated because a round shipped four uncovered hunks two rounds ago.

## Reservations (disclosed, not waived)

1. **Written by the implementer.** Fourteen rounds, every verdict `fail`. Thirteen of the fourteen
   had their largest finding inside the previous round's fix. This round's worst finding is a
   correction I made that replaced a true statement with a false one, under a `(VETO)` label.

2. **Corrections are now the dominant defect class.** Of the last two rounds' fifteen vetoes,
   **eleven** were figures, and **six** of those were introduced while correcting other figures.
   Nothing in the ledger check reads a number.

3. **The bystander case in `_excused` is open**, bounded by six things, all pinned.

4. **The sentence rule is punctuation plus a not-a-dash, not a parser.** A bystander clause joined by
   a semicolon, comma, colon, em dash or markdown bullet is inside the window, and the risk seat
   added the shape this repository writes most: a **table row**, since `|` is not punctuation and a
   table carries no blank line. ~~90~~ **41** records put a backticked test name in a table row (84 rows);
   113 put a bare `test_` token in one. Neither is 90 — corrected in CHG-20260901-03. The leak
   is also not bounded to one row: a name in row 1 and a note five rows later is excused, since
   a whole table is one sentence to `_SENTENCE_END` and only `EXCUSE_WINDOW` stops it. No record in the tree
   exhibits the leak today — the seat checked — but it is reachable and this is the fifth
   formulation.

   ~~113~~ **110** records (374 rows). The corrected `41` was right and the number written beside
   it was not — one corrected figure and one new wrong one in a single sentence, which is the
   round's own headline reproduced inside the correction for it. Both figures are stable: 41/84
   and 110/374 come out the same under "row starts with `|`" and "row contains `|`", and the same
   at `2afec9d` and `3a49c57` (CHG-20260901-04, conformance seat).

5. **`build_factory` still has no handler**, seventh round running.

6. **Nothing checks that a mutation's `says` describes what its replacement does.**

7. **The harness is not in CI.** `.github/workflows/ci.yml` runs pytest and `ledger_check.py` only,
   so all 197 mutations — cost and regressions alike — fall to whoever remembers. That is how the
   `codecs` break shipped. The new import test moves the cheap half into the suite; the rest does
   not move.

8. **`isn't done` still reads as finished**, and `_alive`'s coupling to `_owner` is still deferred,
   nine rounds running.

9. **`test_concurrent_config_edits_and_model_adds_do_not_deadlock` is load-sensitive**, eight rounds
   now, with the defect seat's diagnosis on file.
