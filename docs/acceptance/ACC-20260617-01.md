# ACC-20260617-01 — ai-sdlc-runner initial scaffold

- Project: ai-sdlc-runner
- Date: 2026-06-17
- Target: CHG-20260617-01 (initial implementation of the runner package)
- Verifier: V1 (independent acceptance agent, ≠ implementer I1)
- Implementer / Verifier identity: I1 (lead implementer) ≠ V1 (independent verifier) — identity check satisfied
- Risk: medium (inherited from CHG-20260617-01)
- Baseline source: ai-guideline §7 Acceptance Criteria; build-guide §6; docs/structure/*.md
- Conclusion: **Pass**

## Check details
| # | Acceptance criterion | Source | Result | Evidence / gap |
|---|----------------------|--------|--------|----------------|
| 1 | Submodule `ai-skills` pinned to tag v1.0.0 (not main); version detected from SKILL.md | §6, FR-1 | Pass | `.gitmodules` pins tag v1.0.0, no `branch = main`; `contract.read_skill_version()` parses nested `metadata.version` → returns `1.0.0` against the live cache |
| 2 | First run writes `.sdlc-lock.json`; different major.minor rejected with migrate prompt; different patch passes | §6, FR-3/FR-4 | Pass | CLI proof: first run wrote lock (1.0); `1.0.9` passed; `1.1.0` and `2.0.0` rejected (exit 20, `migrate_required`) |
| 3 | `check_halt` subprocess-calls skill's `halt_gate.py`, branches on exit 0/10; no risk matrix in runner | §6, FR-8 | Pass | `gates.py` runs the script and maps 0→AUTO/10→HALT/else→error; no `always_halt_actions`/matrix duplicated in src (grep) |
| 4 | Role table parsed; `spawn("V1")` tools exclude `Agent` | §6, FR-6/FR-7 | Pass | Parsed roles `[A1,I1,I1.x,V1]`; V1 tools `[Read,Bash,Write]` (no Agent); I1 has Agent; double-enforced in spawn + orchestrator |
| 5 | Stub dry-run completes four stages and halts at a high-risk gate; `--resume` continues from checkpoint | §6, FR-10/FR-11 | Pass | medium-risk run completed 4 stages then HALTed at `before_merge_or_release`; resume re-ran only the remaining stages, not completed ones |
| 6 | `tests/test_contract.py` covers lock comparison + migrate-failure path | §6 | Pass | `pytest -q` → 17 passed; includes patch-pass, minor/major-block, migrate-failure-keeps-lock |
| 7 | Governance docs present | §6 | Pass | ai-guideline.md, structure/{directory,logical,design,data}.md, CHG-20260617-01.md all present and substantive; this ACC closes the set |

## §7 prohibition checks
| Prohibition | Result | Evidence |
|-------------|--------|----------|
| No skill markdown copied into runner | Pass | Only `docs/` + README markdown in repo; no SKILL.md/references/assets/scripts copied |
| No hardcoded halt matrix / role defs / field lists in src | Pass | Halt via subprocess; roles parsed from `agent-hierarchy.md`; only `low/medium/high` in src is an argparse `choices` list + doc comments |
| Submodule pinned to a tag, not main | Pass | `.gitmodules` pins tag v1.0.0; no floating branch |
| V1 tools exclude `Agent` | Pass | `spawn("V1")` → `[Read,Bash,Write]`; enforced in `agents.spawn` and `orchestrator.run` |

## Unmet items & handling
None. (Observation, not a defect: the first CLI `run` halts at the delivery red-line gate by design — the default approver never auto-approves deploy/release; the lock/patch logic still executes and persists before that halt.)

## Structure consistency check
- [x] Implementation consistent with docs/structure/ — modules (`cli/contract/agents/gates/state/orchestrator`), data objects (`.sdlc-lock.json`, `state.json`, `runner.yaml`), and design contracts match the structure docs authored in Stage 2. No drift.

## Summary
Deliverable. The runner genuinely references the skill (subprocess-calls `halt_gate.py`, parses the
role table, reads version from `SKILL.md`) rather than re-implementing governance, locks the contract
at major.minor per project with a validating migrate, runs four stages with shallow fan-out and gated
halts, and mechanically locks down the V1 tool layer. CHG-20260617-01 is set to **Accepted**.

The only remaining step outside this repo is the user wiring up the `ai-skills` submodule at tag
v1.0.0 (build-guide §0); a missing/incorrect tag will surface as a contract-version mismatch, by design.
