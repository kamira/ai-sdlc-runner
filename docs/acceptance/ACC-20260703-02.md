# ACC-20260703-02 — Acceptance: executor extra_args/extra_env passthrough (pure-ai-sdlc isolation)

- Project: ai-sdlc-runner
- Target CHG: CHG-20260703-02
- Verifier: V1 (independent; ≠ implementer I1; model opus, Explore/read-only tool loadout)
- Date: 2026-07-03 (UTC+0)
- Skill: ai-sdlc v1.12.1
- Risk: medium (new opt-in behavior on the command backend; api/stub unaffected)
- Conclusion: **Pass**

## Check details
| # | Criterion | Method | Result |
|---|-----------|--------|--------|
| 6 | `CommandExecutor` appends `extra_args` to argv, merges `extra_env` into env; defaults empty = no change; Api/Stub unaffected | read code + `pytest -k extra` | Pass — executors.py:75 `cmd = list(self.argv) + list(self.extra_args)`; :81 `run_env = {**os.environ, **self.env, **self.extra_env}`; both fields default empty. 7 passthrough tests pass, incl. `test_api_executor_unaffected_by_extra_fields` and `test_stub_executor_unaffected_by_extra_fields`. |
| 7 | `config/runner.yaml` documents `executor.command.extra_args: []` / `extra_env: {}` | read config | Pass — lines 37–38 with inline docs. |
| 8 | `config/pure-ai-sdlc.settings.json` disables bundled skills + four non-ai-sdlc plugins; valid JSON | `json.load` + read | Pass — valid JSON; `disableBundledSkills: true`; `everything-claude-code`, `code-review`, `feature-dev`, `claude-code-setup` all `false`. |
| 9 | Full suite green | live `pytest -q` | Pass — 90 passed, 3 skipped. |
| 10 | §7 prohibitions intact | read gates.py + grep src | Pass — no governance logic added to the executor; halt gates (gates.py → skill's `halt_gate.py`) untouched; passthrough is generic (list/dict), platform specifics kept in config (build-guide §1.7); no skill markdown in `src/`; V1 lockdown/red lines unchanged. |

## §7 prohibition re-check
- No governance logic added to the executor: `CommandExecutor` performs agent work only; halt gates and red-line stops are untouched by this change.
- Passthrough is generic (list/dict), not Claude-Code-specific: platform specifics live in `config/pure-ai-sdlc.settings.json`, referenced via config, honoring the platform-agnostic principle.
- No skill markdown copied into `src/`. V1 lockdown and red lines unchanged.

## Structure-consistency check
- [x] logical.md documents the `command` backend's `extra_args`/`extra_env` passthrough; design.md records the config surface (defaults empty, api/stub unaffected); ARCHITECTURE §6 + README (EN+繁中) document the pure-ai-sdlc recipe — all consistent.

## Summary
The `command` executor gains a generic, opt-in `extra_args`/`extra_env` passthrough that is
byte-for-byte no-op when unset; `api`/`stub` backends have no such fields and are provably
unaffected. Config documents the two fields, the pure-ai-sdlc example settings is valid JSON and
disables bundled skills plus the four non-ai-sdlc plugins, and the full suite is green (90 passed,
3 skipped). §7 intact — no governance logic touched, platform specifics kept in config.
**CHG-20260703-02 Accepted.**
