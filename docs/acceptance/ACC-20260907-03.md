# ACC-20260907-03 - a value that is not text is not a finding

- Project: ai-sdlc-runner
- Target CHG: CHG-20260907-03
- Date: 2026-09-07 (UTC+0)
- Reviewed by: found by the **defect seat** (`fable`); both engines were then asked for the repair
  in independent sessions and **took different positions**. `codex` argued for dropping; a second
  `fable` seat argued for refusing. I held a third position first and abandoned it on a
  measurement. The disagreement is laid out below rather than averaged.
- Conclusion: **Pass**

## Criteria

| # | criterion | evidence |
|---|---|---|
| 1 | Falsy is still silence | `False 0 {} [] '' '   ' None` all `[]` — CHG-20260903-36 untouched |
| 2 | Truthy non-text is refused in the scalar branch | `seat 'risk', 'problems' is bool True` |
| 3 | And in the list branch | `seat 'risk', 'unsafe'[0] is dict {'issue': 'rm -rf /'}` |
| 4 | The message says which seat, key and index | four seats aggregate; "something" is not actionable |
| 5 | Well-formed answers are unchanged | text kept, blanks dropped, `missing` resolved |
| 6 | Each half is pinned when reverted | `--only non-text-answer`: **2 of 2 CAUGHT** |
| 7 | The suite is green | `2254 passed, 4 skipped` in 394s; and two false failures below, which were mine |
| 8 | The README count follows the suite | `--collect-only` -> **2258** |

## Evidence

### The disagreement, and the measurement that settled it

Three seats answering `{"unsafe": [{"issue": "rm -rf /"}]}`, driven through the engine on
`aea1398`:

```
today                       halted=intake_review   safety={seat: ["{'issue': 'rm -rf /'}"]}   merge=False
dropping in the fallback    halted=intake_review   safety={seat: ["{'issue': 'rm -rf /'}"]}   merge=False
dropping in both branches   halted=done            safety=None                                merge=True
```

`codex` proposed the middle row. Measured, it is **inert on the documented shape** — a list is what
`docs/SCHEMAS.md` documents, the list branch is untouched, and nothing about the case it was
proposed for changes.

The bottom row is the consistent version of the same idea, and it walks three seats' unanimous
*"this deletes the disk"* to `merge`. That is `CHG-20260906-07`'s finding through another door, and
worse than the one `CHG-20260907-01` closed: that counted silence as agreement, this turns a seat
that spoke into silence first.

The top row — today, and my own first position — leaves a person deciding about a Python repr, with
`--proceed-unsafe` spending its digest against that repr.

Refusing is the only option that neither invents a finding nor loses one, and the error message
says both halves of that reason because both were somebody's objection.

### A correction I made to myself, in the message it appeared in

I told the operator that the seat's measurement refuted `codex`'s proposal. Re-measured, it refutes
the **extended** version; `codex`'s actual proposal fails differently, by doing nothing. Corrected
immediately rather than left standing, because the two failures argue for different things.

### The seat's three corrections to my brief

1. Both `str()` calls came from one commit (`ee8bfc1`). CHG-20260903-36 only touched the fallback —
   so this is **one** defect, and repairing half is what left it open. That answers the question I
   most wanted answered and shaped the repair into a single shared helper.
2. `{"problems": true}` does not stop the run; the fabricated problem reaches `report.survey` and
   no person. The one that reaches a person is `unsafe`.
3. `IntakeError` here is caught by `cli.run` and printed as one `halted:` line, not a traceback.

### Two false failures in the suite run, and why they were mine

```
FAILED test_the_test_count_the_readme_states_is_the_real_one    -> README not yet updated
FAILED test_every_change_id_the_source_cites_has_a_record       -> the record did not exist yet
```

The second is worth writing down: I launched the suite and then wrote `CHG-20260907-03.md`, so the
guard was right and I was reading a run of a tree that no longer existed. Re-run alone: `1 passed`.
An out-of-order measurement looks exactly like a defect.

## Verdict

**Pass.** The rule the module wrote down fifteen days ago and never implemented is implemented, in
both places it belongs, and the second mutation exists specifically so that repairing only one of
them cannot pass again.
