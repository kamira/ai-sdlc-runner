# ACC-20260823-43 — Acceptance: one walk at a time

- Target CHG: CHG-20260823-43 (a coalescing gate so an attachment arriving during a walk does not
  start a second one)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** — no behaviour change for a single caller.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** The `fable` engine cut the gate and watched **exactly two behavioural tests
  fail** — matching the change's own claim, which is a rarer result in this ledger than it should be.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_server.py -q -p no:randomly
# sabotage performed and reverted by the fable engine:
#   remove the coalescing gate  -> exactly 2 behavioural tests fail
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | An attachment during a walk does not start a second walk | `_walking` / `_walk_again` `server.py:436-441`, `:443-462` | `attach()` `server.py:295-310`, then `_advance()` `server.py:402` | `test_server.py::test_an_attachment_during_a_walk_does_not_start_a_second_walk` — **behavioural** | **Pass, sabotage-verified** |
| 2 | Several attachments coalesce into one further walk | same | same | `::test_several_attachments_during_one_walk_coalesce_into_one_further_walk` — **behavioural** | **Pass, sabotage-verified** |
| 3 | The gate reopens when a walk raises | the exception path | `_advance` | `::test_the_gate_reopens_when_a_walk_raises` — behavioural | **Pass** |
| 4 | `attach` is the only ungated caller | flags initialised `server.py:225-226`; read and written **only** under `self._lock` (`:436`, `:446`) | — | `::test_only_attach_reaches_advance_without_a_state_gate` — structural companion | **Pass** — this one passed even with the gate cut, and is labelled structural for that reason |

Row 4 is worth reading alongside ACC-20260823-44: a structural test that keeps passing when the
mechanism is broken is doing a different job from a behavioural one, and this change's record is
clear about which is which.

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Every server action reaches one coalescing gate | PASS | PASS, verified by cutting the gate | **Agreed** |
| The claim "exactly two tests fail" | not tested | **cut it and counted: exactly two** | **Confirmed, not merely believed** |
| Overall | PASS | PASS, `UNCONFIRMED: none` | **PASS** |

## Not closed by this record

Nothing. The record's aside that the suite takes about a minute longer is consistent with the
sabotage run: 61s against 14s for the same subset.
