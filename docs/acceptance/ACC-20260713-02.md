# ACC-20260713-02 — Acceptance: knowledge close-out (3 KN pattern entries + split-threshold note)

- Target CHG: CHG-20260713-02 (backfill `docs/knowledge/knowledge.md` INDEX with durable prior-CHG patterns + document the single-file→entries/ split threshold)
- Verifier: self (main agent = implementer; permitted by the risk table for **low** risk — docs-only, whitelist "pure documentation"; evidence is command-reproducible)
- Date: 2026-07-13 (UTC+0)
- Skill: ai-sdlc v1.17.0; autopilot: ai-sdlc-autopilot v1.0.0
- Risk: low (documentation-only; no code, no governance logic, no data)
- Conclusion: **Pass**

## Check-details (autopilot: verdict = spec-compliance + quality, from the diff)

| # | Task / check | Method | Result |
|---|--------------|--------|--------|
| 1 | KN-1 vendoring pattern present & accurate | Read `knowledge.md` + `git archive`/`skills/` facts in ai-guideline §6 | Pass — INDEX row + body; cites CHG-20260703-01/06; PRIMARY store + optional submodule fallback + §7 no-copy rule stated |
| 2 | KN-2 version-lock semantics present & accurate | Read `knowledge.md` vs FR-1/3/4/5 + CHG-20260617-01 | Pass — per-project `.sdlc-lock.json` major.minor, first-run default only, bumps don't touch locks, explicit validating migrate (patch=auto / minor-major=migrate-required) |
| 3 | KN-3 dashboard/curses conventions present & accurate | Read `knowledge.md` vs CHG-20260617-02/03, CHG-20260703-04/05 | Pass — stdlib curses + fallback, snapshot preserved, cached panels (no per-keystroke I/O), CJK `get_wch`, red lines still human-approved |
| 4 | Split threshold documented | Read header block | Pass — "when entries reach 30, split into `entries/` … keep this INDEX" |
| 5 | All cited CHG ids resolve & are Accepted | `ls docs/changes/<id>.md` ×7 | Pass — all 7 exist (CHG-20260617-01/02/03, CHG-20260703-01/04/05/06); each is Accepted per its Status line |
| 6 | Tags registered before use | `json.load(vocabulary.json)` membership | Pass — `contract`, `dashboard` already registered; no new tag → vocabulary.json unchanged |
| 7 | Doc-integrity lint green | `python3 …/ai-sdlc/scripts/doc_integrity_check.py --repo .` | Pass — "doc-integrity 檢查通過(結構同步 + CHG↔ACC + 欄位 + secrets)", exit 0 |
| 8 | Docs-only; regression green | `git status --porcelain` + pytest (pyenv `ai-sdlc-runner`, py3.12) | Pass — only `knowledge.md` (M) + this CHG/ACC (new); `pytest -q` 151 passed / 7 skipped, unchanged from baseline (no code touched) |

## §7 prohibition re-check
- No skill logic re-implemented, no skill markdown copied into the runner; `src/` untouched.
- Submodule/offline-store pinning untouched; no red-line action performed (no deploy/migration/delete/secret).
- Still single-file mode (3 entries ≪ 30); `entries/` not created.

## Structure-consistency check
- `docs/structure/directory.md` already lists `docs/knowledge/` as INDEX-first (CHG-20260706-01); entry population needs no tree change — verified no structure doc drift.
- CHG-20260713-02 header carries the v1.17-mandatory `Recurrence check` ("no recurrence") and an `Autonomy:` field (low → merge gated).

## Autopilot loop record
- Plan: CHG-20260713-02 Modification Guide (4 tasks, Global Constraints).
- Build: single docs commit (TDD N/A — pure docs; "green" = lint + accurate citations).
- Task review: read-only, from the diff → Pass (checks 1–7 above).
- Whole-branch review: lint exit 0 + regression green.
- Merge: **gated** for one human confirm (outward, hard-to-reverse), per the CHG `Autonomy` field.

## Summary
The knowledge base moved from zero entries to three grounded KN pattern entries (skill-store
vendoring, version-lock/migrate, dashboard/curses), each with an INDEX row and source-CHG citations
that all resolve, plus a documented single-file→entries/ split threshold. Docs-only; lint exit 0;
pytest 151/7 unchanged. **Accepted.**
