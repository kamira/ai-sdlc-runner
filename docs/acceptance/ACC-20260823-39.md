# ACC-20260823-39 — Acceptance: the replay was not a log

- Target CHG: CHG-20260823-39 (`--format playback` builds every card at load; the clock dims what is
  ahead rather than hiding it; click-to-seek; no blank summaries)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** — a rendering change.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** Each test checks the actual mechanism rather than the vocabulary around it —
  the load-time test asserts the payload length equals the turn count **and forbids `<= n` in the
  builder**, which is the shape the defect took.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_conversations.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | Every turn is in the page **at load**, not only after playing | `TURNS.forEach` `conversations.py:1676` in the page template | emitted by `export_conversation("playback")` | `::test_every_turn_is_in_the_page_at_load_not_only_after_playing` — payload length equals turn count, builder unbounded, **`<= n` forbidden** | **Pass** — it forbids the specific construct that caused the bug, not just the symptom |
| 2 | The clock **dims** what is ahead rather than hiding it | `.turn.ahead{opacity:.42}` `conversations.py:1575`; `classList.toggle('ahead', i > n)` `:1703` | the page | `::test_the_clock_dims_what_is_ahead_rather_than_hiding_it` | **Pass** |
| 3 | A turn can be clicked to move the clock to it | the page template | — | `::test_a_turn_can_be_clicked_to_move_the_clock_to_it` | **Pass** |
| 4 | No turn renders a blank summary | `_summary` `conversations.py:1201-1224` — verdict plus why; empty-list answers rendered as "nothing missing, problems, unsafe"; the opened turn non-blank | `_playback` | `::test_no_turn_in_a_real_run_renders_a_blank_summary`, plus the three specific `_summary` tests at `:771-788` | **Pass** — checked against a **real** run, not a constructed one |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Cards built at load, clock highlights only, click-to-seek, summaries non-blank | PASS | PASS | **Agreed** |
| The 306-card DOM count | flagged as browser-verified rather than suite-asserted | **same**, and noted the record states this itself under "Not claimed" | **Agreed — the gap is disclosed by the CHG, which is the right disposition** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

The 306-of-306 DOM count remains browser-verified only. The record says so itself; nothing here
upgrades it.
