# ACC-20260823-36 — Acceptance: a third example on the console path, and one README each

- Target CHG: CHG-20260823-36 (the `serve --settings` default fix; `weather-spa` driven against a
  live server through five scenarios; content-comparing agents; per-example READMEs; demo pages
  generated rather than kept)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **medium** — a crash fix in `serve`.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** Every mechanism has a test that fails if it is cut, and the demo pages are
  **regenerated and byte-compared** rather than trusted.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_example_weather_spa.py tests/test_demo_pages.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes. The `fable` engine ran the cited files: **80 passed, 1 skipped**.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | `serve` no longer needs `--settings` passed to it | `cli.py:993` | the parser built in `cli.main`, global flag at `cli.py:953` | `test_example_weather_spa.py::test_serve_does_not_need_settings_passed_to_it`; `::test_every_subcommand_that_takes_settings_gets_a_usable_default` — **loads each subparser's default** | **Pass** — the second test generalises the fix instead of pinning the one crash |
| 2 | Five console scenarios against a live `runner serve` | `examples/weather-spa/scenarios.py:135-269` | `tests/test_example_weather_spa.py:39-80` | one dedicated test per scenario A–E | **Pass** |
| 3 | The agents compare **content**, not the presence of a file | `examples/tide-spa/agent.py:172-186`, same fix in `weather-spa/agent.py` | driven to "nothing to do" by `examples/demo/build.py` | `test_demo_pages.py::test_the_agents_the_demos_use_do_not_pick_the_next_module_by_missing_file` — drives each shipped agent **five builds**; the fifth must answer `module: ""` | **Pass** — this is the test that later made CHG-20260823-50's defect findable |
| 4 | Demo pages are generated, not kept | `examples/demo/build.py` `generate()` | — | `::test_the_committed_page_is_the_one_the_agent_writes_today` — regenerates and **byte-compares** | **Pass** |
| 5 | One README per example, links resolving | `examples/{minimal,tide-spa,weather-spa}/README.md` | — | `::test_every_relative_link_in_an_example_readme_resolves` | **Pass** |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Serve default, console example, content agents, docs links, demo pages | PASS | PASS, `UNCONFIRMED: none` | **Agreed** |
| Tests executed | **could not run** — read-only sandbox had no writable temp dir | **ran them**, 80 passed 1 skipped | **fable carries the executed evidence** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

Nothing. The stated "1191 passed" has drifted to 1316 through later changes — allowed drift.
