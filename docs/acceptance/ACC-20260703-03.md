# ACC-20260703-03 — Acceptance: Persistent interactive dashboard (resident control console)

- Project: ai-sdlc-runner
- Target CHG: CHG-20260703-03
- Verifier: V1 (independent; ≠ implementer I1; model opus, Explore/read-only tool loadout)
- Date: 2026-07-03 (UTC+0)
- Skill: ai-sdlc v1.12.1
- Risk: medium (UI/entry-point layer; no governance logic changed)
- Conclusion: **Pass**

## Check details

| # | Requirement / check | Method | Result |
|---|---------------------|--------|--------|
| 1 | Bare `runner` (TTY) → resident dashboard; all subcommands still dispatch; off-TTY unchanged (menu/numbered fallback) | Read `cli.main`/`build_parser`; ran `_resident_available()` on a pipe (False); enumerated 8 subparsers | Pass |
| 2 | Empty start: no current project, empty panels | `ResidentApp()` → `current_project is None`; `render_layout(current_project=None)` shows `(none — use /open <path>)`; test | Pass |
| 3 | Bottom input accepts task/requirement; plain text starts a run on current project | `render_layout` input line; `dispatch` → `_start_run`; tests + demo | Pass |
| 4 | Arrow-key halt answers (cycle_index + approve_decision); never implicitly approves | `approve_decision(None/''/unknown/out-of-range) → None`; `approver = bool(approve_decision(...))`; tests | Pass |
| 5 | Menu/command entry: `parse_command` for /open,/run,/status,/check,/menu,/help,/quit, unknown, plain text; F2 menu | Demo C1 (all kinds correct); F2 bound in loop | Pass |
| 6 | 2-col bounded logs; Status + Verification always visible under overflow | Demo C2 (height=14, 200 events → top panels intact, logs tail-truncated); tests | Pass |
| 7 | Resident: no exit after run finishes/halts; only /quit exits | `dispatch` returns False only for `quit`; `_start_run` returns to idle loop; test | Pass |
| 8 | Governance untouched; red line requires explicit human Approve | `git diff main` empty for orchestrator.py + gates.py; delivery gate forced high-risk; approver-gated `_gate` | Pass |
| 9 | Test suite green | `pytest -q`: 133 passed / 6 skipped; with AI_SDLC_SKILL_PATH: 139 passed / 0 skipped | Pass |
| 10 | I1 bug-fix: skill path via store precedence, not hardcoded | `ResidentApp._resolve_skill_path` delegates to `cli._resolve_skill_path`; resolved `skills/v1.0.0`, not `./ai-skills/...` fallback | Pass |

## §7 prohibition re-check (no risk matrix in the runner; delegate all halt decisions)

- The runner still holds no halt/risk logic of its own: every decision is delegated to the skill's `scripts/halt_gate.py` via `gates.check_halt` (subprocess, branch on exit code only). Confirmed unchanged (`gates.py` diff empty).
- The resident console adds only a human `approver` and an `on_event` sink around the unchanged `orchestrator.run`; it never self-approves. Unresolved/blank/cancelled halt answers map to reject.
- Red-line gate `before_merge_or_release` still runs at `risk="high"` and halts unless a human approves.

## Structure-consistency check

- `docs/structure/logical.md` and `docs/structure/design.md` were updated to describe the resident interactive mode + command layer + interactive approver and the 2-col bounded layout / entry-point change / single-thread run integration — consistent with the implemented `dashboard.py`/`cli.py`/`tui.py`.
- Read-only snapshot path (`render_snapshot`/`view`) and all subcommands are preserved; `tui.select()`/`prompt()` APIs retained, with the arrow-key core refactored into the embeddable `_curses_select_on` reused by the resident app.

## Summary

CHG-20260703-03 delivers a resident, always-on interactive curses console as the default TTY entry
point with empty start, a bottom input for tasks/`/commands`/arrow-key halt answers, and a 2-col
bounded-height log layout that keeps Status and Verification visible under overflow. All behavior is
pure glue around the unchanged orchestrator/gates: no governance logic was added or modified
(orchestrator.py and gates.py diffs are empty), red lines still require explicit human approval, and
`approve_decision` never implicitly approves. The I1 fix routes skill-path resolution through the
store precedence rather than a hardcoded path. Full suite green (139 passed with the skill store on
the path). **Conclusion: Pass.**
