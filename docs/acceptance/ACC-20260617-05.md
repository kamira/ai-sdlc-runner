# ACC-20260617-05 — Offline local skill store

- Project: ai-sdlc-runner
- Date: 2026-06-18
- Target: CHG-20260617-05 (vendored offline multi-version skill store)
- Verifier: V1 (independent acceptance; ≠ implementer I1)
- Risk: medium (inherited; includes a deliberate guardrail override — verified below)
- Baseline source: CHG-20260617-05 modification guide; user directive (no online fetch; copy v1.0.0 + v1.1.0 into a folder as primary store; auto-select by lock; submodule as fallback)
- Conclusion: **Pass**

## Check details
| # | Acceptance criterion | Source | Result | Evidence |
|---|----------------------|--------|--------|----------|
| 1 | v1.0.0 + v1.1.0 vendored into `skills/` (verbatim, offline) | CHG-05 | Pass | `skills/v1.0.0` & `skills/v1.1.0` each a full skill root (SKILL.md/references/scripts/assets); extracted via `git archive` from local tags (no network); versions read back as 1.0.0 / 1.1.0 |
| 2 | Runner resolves skill from store, no `--skill-path`, fully offline | CHG-05 | Pass | `runner run <proj>` (no flags) ran four stages and halted at the red line — gates fired, i.e. it called the store's `halt_gate.py` |
| 3 | Version auto-selected by project lock | CHG-05 | Pass | first run (expected 1.0.0) → `skills/v1.0.0`; after `migrate --to 1.1.0`, `_resolve_skill_path` → `skills/v1.1.0` |
| 4 | `check` lists store versions + flags newer | CHG-05 | Pass | `check` prints `versions: 1.1.0, 1.0.0`; lock 1.0.0 vs newest 1.1.0 → `minor … run migrate`, exit 20 |
| 5 | Status + dashboard show store-aware skill line | CHG-05 | Pass | status `skill: newest 1.1.0 vs 1.0.0 → minor ⚠ migrate`; dashboard Status panel same |
| 6 | Submodule retained as optional fallback (not pulled) | CHG-05 | Pass | `.gitmodules` annotated "OPTIONAL FALLBACK ONLY … not pulled by default"; resolution falls back to `skill_path` only when store has no match |
| 7 | No online fetch anywhere in code | user directive | Pass | grep of `src/` for `urllib/requests/http/git clone/fetch` → none; the only URL is the fallback submodule's in `.gitmodules` |
| 8 | No duplicated governance logic (override mitigations hold) | §1.3; CHG-05 | Pass | `gates` still subprocess-calls the store's `halt_gate.py`; `agents` parses the store's `agent-hierarchy.md`; no risk matrix / role map in `src` |
| 9 | Override recorded (human-in-the-loop) | ai-sdlc override rule | Pass | CHG-05 "Deliberate guardrail override" section + Guideline §6/§8 record the §1.2/§7 relaxation and mitigations |
| 10 | Tests cover the store resolver | §6 | Pass | `tests/test_skillstore.py` (version list/ignore, resolve exact/major.minor/latest, missing/empty, detect-vs-lock); full suite **54 passed** |
| 11 | Structure docs + Guideline synced | §1.2 | Pass | directory.md (+skills/, +skillstore.py), logical.md (+skillstore, resolution flow), design.md (+skillstore, override decision), Guideline §6/§8 |
| 12 | Backward compatibility preserved | regression | Pass | `--skill-path` override still works; submodule fallback intact; existing run/migrate/status/dashboard/check behavior unchanged when store absent |

## §7 prohibition note
§7 ("don't copy skill markdown into the runner") is **deliberately overridden** here with the user's
explicit approval (recorded in CHG-05 and the Guideline). The override is bounded: verbatim offline
snapshots (not a fork), submodule fallback retained, governance logic still read from the store (not
duplicated). All other prohibitions (no hardcoded halt matrix/role table, V1 excludes Agent, red-line
halts) remain enforced.

## Unmet items & handling
None. (The cache-gated tests in `test_contract.py` skip when `AI_SDLC_SKILL_PATH` is unset; the store
path is covered by `test_skillstore.py`.)

## Structure consistency check
- [x] Implementation matches updated structure docs (`skillstore` module, store layout, lock-driven resolution, submodule fallback).

## Summary
Deliverable. The runner now runs **fully offline** from a vendored local store holding v1.0.0 and
v1.1.0, auto-selecting the version per project lock, with the submodule kept only as an optional
fallback. The reference-not-copy guardrail relaxation is explicit, bounded, and recorded; no
governance logic was duplicated and all safety gates remain intact. CHG-20260617-05 set to
**Accepted**.
