# ACC-20260901-05 — Acceptance: a capital R turned the gate off

- Target CHG: CHG-20260901-05
- Verifier: self (main agent = implementer)
- Reviewed by: a four-seat panel over `3a49c57..a845e7c` — `conformance`, `defect`, `risk`, `idiom`
  — each in its own git worktree, none seeing another's answer. The panel is the **input** to this
  change, not its verification; see reservation 1.
- Date: 2026-09-01 (UTC+0)
- Risk: medium — the ledger's excusal rule, the harness's recovery, the mutation harness
- Conclusion: **Pass on the corrections.** All four seats returned **fail**; this change is what
  those verdicts asked for.

## Check-details

| # | Task | Result |
|---|------|--------|
| 1 | The rename gate matches the verb the way the rule does | **Pass** — reverting to a case-sensitive match is `2 failed`; `Renamed` and `RENAMED TO` are refused, and both were excused at `a845e7c` |
| 2 | The phrase is built once, with the case flag single-sourced | **Pass** — dropping `(?i:` from `_case_free` is `2 failed`, one edit reaching both consumers |
| 3 | `renamed` and `to` are separated by `\s+` | **Pass** — restoring the literal space is `2 failed` |
| 4 | Attribution is adjacency, not clause order | **Pass** — `_SPAN` in place of `_ATTACHED` is `3 failed` |
| 5 | The refused true sentence is a row | **Pass** — the comma-aside row, refused and recorded as a cost |
| 6 | `recoverable` considers `mutated` | **Pass** — dropping it from the set is `1 failed`, over a file holding an unrelated edit |
| 7 | The `owner` restore is conditioned on no live run | **Pass** — an unconditional restore is `5 failed` |
| 8 | The staged-changes sentence is true | **Pass** — `git status --short` prints `M  x.py`; `git diff` is empty; the message now says which is which |
| 9 | The last refusal names a way out | **Pass** — "delete this record once the tree is reconciled, and not before" |
| 10 | The census test exercises `_SENTENCE_END` | **Pass** — the capital-only revert is `5 failed` on the new rows; the test it replaces stayed green |
| 11 | Its replacement can fail for its own reason | **Pass** — the corpus test's floors are per-shape and reachable, not a ratio dominated by punctuation |
| 12 | Task 5 of CHG-20260901-04 is struck | **Pass** — reproduced against `3a49c57`: one list, one order, and the printing branch never ran |
| 13 | The probe's cost figures are re-measured | **Pass** — 0.337s→0.861s per probe over 19 targets; the in-suite test is 16–26s; ~197 probes per full run |
| 14 | The probe's leak on a killed run is disclosed | **Pass** — named in the comment; one directory per kill, in the system temp |
| 15 | `_case_free` and `_renamed_to` are named for what they do | **Pass** — and `renamed` is `rename_allowed` |
| 16 | `compiled.match` is not the mechanism named | **Pass** — `_SPAN`'s `*?` is |
| 17 | The docstring's changelog is removed | **Pass** — the pointer to the bounds table stays |
| 18 | The `#:` block sits with the constant it documents | **Pass** |
| 19 | The `WORD_NAME` tombstone is gone | **Pass** |
| 20 | The census bucket label matches the bucket | **Pass** — `#` 1377, `*` 879, `-` 827, `\|` 117, and 2 caseless letters |
| 21 | Reservation 10 says what it measured | **Pass** — `0`, `-1`, `1`, `2147483647` all act on Windows |
| 22 | The four record inconsistencies are reconciled | **Pass** |
| 23 | Task 3's `6 failed` is corrected, and pinned for real | **Pass** — CHG-20260901-03's remedy restored verbatim is **`7 failed`** here; it was `0` at `a845e7c`, because the condemned wording contains "back by hand" in a warning against restoring |
| 24 | `_REMOVED_NO_RENAME` is attributed to the change that added it | **Pass** — `git log -S"_REMOVED_NO_RENAME" --all` returns `a845e7c` alone; `3a49c57` has one constant. Corrected in the source docstring and in both records |
| 25 | The README hunk is claimed by a task row | **Pass** — row 22 added to CHG-20260901-04, and row 31 covers this round's own bump |
| 26 | Reservation 4's table-row figures name their trees | **Pass** — 41/84 and 110/374 at `3a49c57`; 42/85 and 111/376 at `a845e7c` |
| 27 | "Three are not pinned" is six, and each row says so | **Pass** — rows 3, 5, 6, 11, 15, 16; the seventh item stays in the reservation with its reason |
| 28 | "three records" is two records, three places | **Pass** |
| 29 | "fifteen lines up" points where it points | **Pass** — replaced with "in its opening paragraph" |
| 30 | The §7 hunk command reproduces after the commit | **Pass** — `git diff 3a49c57..a845e7c --diff-filter=M \| grep -c '^@@'` is 29 |
| 31 | This round's own README bump has a row | **Pass** — `1916` → `1929` |
| — | Mutations | **Pass** — `--only closure` **9/9**, `--only codecs` **9/9**, `--only stranded` **12/12**, `--only reach` **12/12**, `--only console` **8/8**; every group exit 0, record cleared, `git status` shows only this change's own files after each |
| — | Full suite | **Pass** — **1925 passed, 4 skipped** (1929 collected), pytest exit 0, 6m43s. The first run was `1 failed, 1924 passed`: `test_documented_numbers.py` caught the README at 1916 against a suite of 1929, for the second round running — which is why the README bump is task 31 rather than a by-product |
| — | Ledger lint | **Pass** — **138** changes: 137 plus this change's own CHG. Read from the lint's output, not computed |

## Every fix, with the revert that measures it

One patch per row, on this tree, today.

| fix | revert | rows red |
|---|---|---|
| the verb is matched case-insensitively | `phrase = _renamed_to(capture=True)` | 2 |
| the case flag is single-sourced | drop `(?i:` from `_case_free` | 2 |
| `renamed\s+to` | restore the literal space | 2 |
| attribution is adjacency | `_ATTACHED` → `_SPAN` | 3 |
| the sentence rule is not the capital rule | `_SENTENCE_END` → `[.!?]\s+(?=[A-Z])` | 5 on the new rows (11 in the file) |
| `recoverable` considers `mutated` | drop it from the set | 1 |
| the `owner` restore is conditioned | make it unconditional | 5 |
| the same, measured the way the seat did | CHG-20260901-03's remedy tail, restored verbatim | 7 |

Two rows matter beyond their own task. The sentence-rule row is `5` where the test it replaces was
**green** under that same revert. And the last row is the figure the previous acceptance published
as `6` and the conformance seat measured as `0`: the guard it named was satisfied by the wording it
condemned, and it takes this round's added assertion to make that revert red at all.

## The measurement that decides the headline

Twelve sentences through the real `check_named_tests_exist`, at `a845e7c` and here:

| | at `a845e7c` | now |
|---|---|---|
| `` <the ghost> was renamed to `helper` … `` (pinned row) | refused | refused |
| the same with a capital `Renamed` | **excused** | refused |
| `Renamed to `x` … : <the ghost>.` | **excused** | refused |
| someone else's rename, ghost named **first** | **excused** | refused |
| someone else's rename joined by a comma | **excused** | refused |
| wrapped between `renamed` and `to` | **refused** | excused |
| two spaces between `renamed` and `to` | **refused** | excused |
| the five shapes that must not move | unchanged | unchanged |

Seven of twelve were wrong; none are now.

## §7 prohibition re-check

- No skill logic re-implemented; nothing under `skills/` exists here.
- `src/` changes: **none.** No file under `src/` is touched.
- Non-`src/` files and the hunk count, both read from `git` after staging, with a command that
  still reproduces after the commit — the last §7 named `--cached`, which returns 0 once committed:
  `git diff f949f8c..HEAD --diff-filter=M | grep -c '^@@'` is **29** across 11 modified files, and
  **31** across 13 with the 2 new records.

  `README.md` · `tools/ledger_check.py` · `tools/mutation_check.py` · `tools/mutation_recovery.py` ·
  `tests/test_ledger_check.py` · `tests/test_mutation_check.py` · `tests/test_mutation_recovery.py` ·
  and the records `ACC-20260901-03`, `ACC-20260901-04`, `CHG-20260901-03`, `CHG-20260901-04`, plus
  this pair.

  Every hunk is claimed by a task row, and the README's is task 31 — named because the last five
  rounds each shipped a hunk no row covered, four of them that same README line.

## Reservations (disclosed, not waived)

1. **Written by the implementer.** Seventeen rounds, every verdict `fail`. Sixteen of the seventeen
   had their largest finding inside the previous round's fix. This round has four there: two figures
   attached to reverts that do not produce them, a fix recorded against a defect that never existed,
   and a defect attributed to a round that never had it.

2. **`_ATTACHED` refuses a sentence people write.** `` `the ghost`, the halt case, was renamed to
   `X` `` is refused for its commas. That is a deliberate trade — this check refuses where it cannot
   tell, everywhere else — and it is a row rather than a discovery for the next round.

3. **The rename bound holds only in the absence of the bystander case.** Every sentence measured for
   task 4 carries no removal note; a real excuse sentence normally does, and with a bystander
   removal in the same sentence the open bystander case excuses the ghost regardless — at `3a49c57`
   and here alike. The previous acceptance's row 7 did not say so.

4. **The bystander case in `_excused` is open**, bounded by seven things, all pinned.

5. **`build_factory` still has no handler**, tenth round running.

6. **Nothing checks that a mutation's `says` describes what its replacement does.**

7. **The harness is not in CI**, and the import probe now costs roughly 200s of a ~54-minute full
   run rather than the ~84s recorded. Correctness over speed is the right trade for an outcome that
   says "the probe learned nothing"; the price is stated rather than absorbed.

8. **`isn't done` still reads as finished**, and `_alive`'s coupling to `_owner` is still deferred,
   twelve rounds running.

9. **`test_concurrent_config_edits_and_model_adds_do_not_deadlock` is load-sensitive**, eleven
   rounds.

10. **`_alive` treats every dead pid as an invitation to act, and does so differently by platform.**
    `0`, `-1`, `1` and `2147483647` all restore and delete on Windows; on POSIX `os.kill(0, 0)` and
    `os.kill(-1, 0)` both succeed, so the same records refuse there. Reasoned, not measured — there
    is no POSIX host here. This is the oldest open item in the file and it now has a second reason
    to be closed.
