# ACC-20260713-01 — Acceptance: directory.md truthfulness (list ARCHITECTURE.md + real tests/ files)

- Target CHG: CHG-20260713-01 (add `docs/ARCHITECTURE.md` and the actual `tests/` files to the `directory.md` tree)
- Verifier: self (main agent = implementer; permitted by the risk table for **low** risk — docs-only, whitelist "pure documentation"; evidence is command-reproducible)
- Date: 2026-07-13 (UTC+0)
- Skill: ai-sdlc v1.17.0
- Risk: low (documentation-only; no code, no governance logic, no data)
- Conclusion: **Pass**

## Check-details

| # | Check | Method | Result |
|---|-------|--------|--------|
| 1 | `docs/ARCHITECTURE.md` now listed in the tree | Read `docs/structure/directory.md` under `docs/` | Pass — line added ("handover-oriented architecture & feature overview") above `ai-guideline.md` |
| 2 | `tests/` block matches the real files | `ls tests/` vs the tree | Pass — tree now lists `conftest.py` + `test_contract/skillstore/executors/workspace/tui/dashboard.py`; matches `ls tests/` exactly (7 tracked files; `__pycache__` excluded) |
| 3 | Doc-integrity lint green | `python3 …/ai-sdlc/scripts/doc_integrity_check.py --repo .` | Pass — "doc-integrity 檢查通過(結構同步 + CHG↔ACC + 欄位 + secrets)" |
| 4 | Docs-only; scope contained | `git status --porcelain` | Pass — only `docs/structure/directory.md` (M) + this CHG/ACC (new); no `src/`, `tests/`, or `config/` touched |
| 5 | Regression green | pytest via project venv (pyenv `ai-sdlc-runner`, py3.12) | Pass — `pytest -q` → 151 passed / 7 skipped, identical to the CHG-20260706-01 (PR #4) baseline (no code changed) |

## §7 prohibition re-check
- No skill logic re-implemented, no skill markdown copied into the runner; `src/` untouched (git status shows no code files).
- Submodule/offline-store pinning untouched; no red-line action performed (no deploy/migration/delete/secret).

## Structure-consistency check
- `docs/structure/directory.md`: tree now indexes every tracked doc/test it purports to list — the two omissions surfaced in the PR #4 review are closed.
- No requirement/scope semantics changed; `ai-guideline.md` and the other structure docs need no edit.
- CHG-20260713-01 header carries the v1.17-mandatory `Recurrence check` field ("no recurrence").

## Summary
`directory.md`'s tree was missing `docs/ARCHITECTURE.md` and understated `tests/` (1 of 7 files).
Both are now listed truthfully; the change is documentation-only (no code paths touched), the full
suite stays green (151/7), and `doc_integrity_check.py --repo .` passes. **Accepted.**
