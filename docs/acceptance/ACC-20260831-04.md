# ACC-20260831-04 — Acceptance: the remedy deleted the only copy

- Target CHG: CHG-20260831-04
- Verifier: self (main agent = implementer)
- Reviewed by: a four-seat panel over `2510e08..4105344` — `conformance`, `defect`, `risk`, `idiom`
  — each in its own git worktree, none seeing another's answer. The panel is the **input** to this
  change, not its verification; see reservation 1.
- Date: 2026-08-31 (UTC+0)
- Risk: medium — a `src/` refusal restored, the harness's recovery, the ledger's matching
- Conclusion: **Pass on the corrections.** The panel's verdict was **fail** from all four seats,
  with one veto; this change is what those verdicts asked for.

## Check-details

| # | Task | Result |
|---|------|--------|
| 1 | `--recover` keeps a record it cannot read | **Pass** — truncated and empty; both kept byte-identical |
| 2 | It says so, and exits non-zero | **Pass** — `REFUSING …`, exit 1; it was silent with exit 0 |
| 3 | The test asserting the deletion is replaced | **Pass** — reverting the branch is `4 failed` |
| 4 | Any userinfo is refused again | **Pass** — 12 endpoints; reverting the gate is `2 failed` |
| 5 | The remedy travels with the finding | **Pass** — `key_env` absent from the bare-name remedy |
| 6 | The refusal stops echoing the value | **Pass** — `"alice" in str(...)` is `False` |
| 7 | The ghost remedy has a test | **Pass** — 7 phrasings; 4 accepted, 3 refused |
| 8 | The store handler has a test that runs it | **Pass** — `cli.main(["serve", …])` → rc 2, message read |
| 9 | The excusal is scoped to the name | **Pass** — lists excused, a dead change id is not |
| 10 | The command scan reads what is printed | **Pass** — sees 3 where it saw 2, and `emergencies` is one |
| 11 | The pair test's vacuous half is replaced | **Pass** — the refusal must name all three tuples |
| 12 | The announced-run docstring says what it holds | **Pass** |
| 13 | The other three load doors name their file | **Pass** — 3 of 4 print a path; the fourth is reservation 3 |
| 14 | Four more negation forms | **Pass** — `never merged`, `no longer done`, `not-done`, `un-merged` |
| 15 | Ten of ten, in all three places | **Pass** — measured against `2510e08`: 20 phrases, 10 words |
| 16 | `graded_by_guess` is three words | **Pass** |
| 17 | The blockquote stops breaking its table | **Pass** — 23 pipe rows, 23 rendered |
| 18 | The two docstrings stop contradicting the code | **Pass** |
| 19 | `BARE_HOST_CASES` is parametrised | **Pass** — 6 ids, 6 assertions |
| 20 | The three miscounts are struck through | **Pass** |
| 21 | The stale citations are repointed | **Pass** |
| 22 | The acceptance stops duplicating the narrative | **Pass** — 0 shared `##` headings; it was 2 |
| — | Mutations | **Pass** — `--only stranded` **12/12**, `--only closure` **9/9**, `--only reach` **12/12**; record cleared, tree clean |
| — | Full suite | **Pass** — 1835 passed, 4 skipped (1839 collected), pytest exit 0 |
| — | Ledger lint | **Pass** — 130 changes |

## The veto, and what it was against

ACC-20260831-03 said *"The tests added for all three read the message, not the return value."* The
conformance seat measured it: one of three.

The ghost-test remedy had no test at all. What held it up was that `ACC-20260831-01`'s blockquote,
written in the same commit, happened to use the one phrasing the regex accepted — the seat showed
this by replacing `excused` with `set()` and getting `2 failed`, both of them repo-wide tests that
go red on that one paragraph. Reword it and the rule was unpinned; the `closure` mutation could not
tell, because it is caught by those same two tests.

That is a green report about an instrument, one level up: a feature was "covered" by a document.

## What the guard's remedy did

| record | `--recover` before | after |
|---|---|---|
| truncated | silent, exit 0, **deleted** | `REFUSING …`, exit 1, kept |
| empty | silent, exit 0, **deleted** | `REFUSING …`, exit 1, kept |
| `owner: null` | refused, kept | refused, kept |

The guard that sends an operator there calls that file *"the original text, and the only copy"* and
says *"restore from the record by hand rather than deleting it."*

A test was asserting the deletion, with the reason attached: *"an unparseable record must not block
every later run."* Blocking every later run is exactly right — a person has to look. That sentence
is why four rounds of review of this file did not find it.

## What the shape guess let through

Eight vendor token formats, measured by two seats independently, all refused before
CHG-20260831-03 and all accepted after it:

```
gsk_  hf_  r8_  glpat-  npm_  ATATT  co-  and 16 hex characters with no prefix
```

The argument for the narrowing was about a **sentence** — `alice@host` got a remedy that did not
apply. That is fixed in the sentence. The guess picks wording now; the refusal is categorical, as
`server.py` has always had it on its own bind URL.

## §7 prohibition re-check

- No skill logic re-implemented; nothing under `skills/` exists here.
- `src/` changes: `models.py` restores the categorical userinfo refusal, returns a third element
  (the remedy) from `_secret_in_url`, renames `graded_by_the_bare_host_rule` → `graded_by_guess`,
  and stops echoing the refused value. `cli.py` **adds no new control-flow path this round** and
  changes three refusal messages: the registry door and the run door now name their file (each was
  a bare `print(f"error: {exc}")`), and the store handler's message drops a false claim and warns
  about orphaned assignments. The round-9 conformance seat's finding 9 was that
  ACC-20260831-03's §7 line did not enumerate the `except models_mod.ModelError` branch that round
  added to `cmd_serve`; it is named here. `GATES`, `PERMANENT_HALT_KINDS`, `CLASSES` and the graph
  untouched.

## Reservations (disclosed, not waived)

1. **Written by the implementer.** Nine rounds, every verdict `fail`. Eight of the nine had their
   largest finding inside the previous round's fix. This round's veto was against a sentence I wrote
   about my own testing.

2. **The bystander case in `_excused` is open, and bounded rather than closed.** A dead test named
   inside a run of names that ends in `removed in CHG-…` is excused with them. Closing it would
   break the list form, which is grammatically identical from the lint's side. The window is one
   paragraph, at most 200 characters, crosses no backticks except test names, and the change id must
   be a record that exists.

3. **`build_factory` calls `store_mod.load_registry(db)` per walk with no handler.** The fourth
   door. It runs after `serve` has started and the store can only change through the console, which
   validates — so I could not reach it. Named because the round-9 risk seat named it and I am not
   claiming it is safe, only that I did not measure it. Worth its own change.

4. **`_looks_secret` is still wrong in both directions, and now it does not matter.** It selects the
   sentence. A vendor prefix it does not know gets the milder wording and the same refusal.

5. **`isn't done` still reads as finished.** The apostrophe form negates the verb rather than the
   state, and a prefix list cannot reach it. Four more forms are covered; this one is named and not.

6. **`_alive`'s coupling to `_owner` is still deferred**, four rounds running.

7. **`test_concurrent_config_edits_and_model_adds_do_not_deadlock` is load-sensitive.** Seen red
   under concurrent pytest processes in three rounds now, green alone. Unrelated to this change.
