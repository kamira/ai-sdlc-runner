# ACC-20260831-03 — Acceptance: "cannot tell" is not "mine"

- Target CHG: CHG-20260831-03
- Verifier: self (main agent = implementer)
- Reviewed by: a four-seat panel over `3b146f1..2510e08` — `conformance`, `defect`, `risk`, `idiom`
  — each in its own git worktree, none seeing another's answer. The panel is the **input** to this
  change, not its verification; see reservation 1.
- Date: 2026-08-31 (UTC+0)
- Risk: medium — the harness's guard, the ledger's matching, two `src/` refusals
- Conclusion: **Pass on the corrections.** The panel's verdict was **fail** from every seat, with
  two vetoes; this change is what those verdicts asked for.

## Check-details

| # | Task | Result |
|---|------|--------|
| 1 | Unreadable is not owned | **Pass** — 10 states: 5 record shapes × `MUTATION_RUN` set and unset, all fail the guard |
| 2 | A skip is about a record | **Pass** — clean tree with `MUTATION_RUN=1` is ~~`28 passed, 1 skipped`~~ **`34 passed, 1 skipped`**, corrected in CHG-20260831-04; it was `22 passed, 2 skipped` |
| 3 | A skip is not an acquittal | **Pass** — reverting the owner filter is `4 failed`, reverting the skip is `1 failed`; both were green-with-skips before |
| 4 | Veto 2's rows are struck through | **Pass** — `grep "collides with nothing, which holds"` and `grep "Pass on the collision"` both return 0 unstruck |
| 5 | The refusal names where the secret is | **Pass** — userinfo → "the userinfo before @", fragment → "the fragment", query → "the query string" |
| 6 | A plain user name is not a secret | **Pass** — `https://alice@api.vendor.com/v1` → `None` |
| 7 | The ghost-test remedy is honoured | **Pass** — `ACC-20260831-01` says where the test went and the lint accepts it; `129 changes` green |
| 8 | Every command the CLI names exists | **Pass** — and reverting the comment to cite `runner models` goes red |
| 9 | The disclosure calls the rule it discloses | **Pass** — of six endpoints only `gpu-box` is named; `fd00::1` and `fe80::1` are not |
| 10 | English negations read as not-finished | **Pass** — five rows; dropping the generator is `5 failed` |
| 11 | The third matcher shares the rule | **Pass** — reverting it to `w in status` is `1 failed`; it was unpinned |
| 12 | The pair invariant is a test, both ways | **Pass** — and it is not deleted by `python -O` |
| 13 | The refusal rows assert a refusal | **Pass** — moving 未通過 into `ACC_PASS` is `1 failed`; it was green |
| 14 | `_says` is gone; `_standing` is annotated | **Pass** |
| 15 | The duplicated `IN_FLIGHT` block is written once | **Pass** — ~~15~~ **16** lines → 11, corrected in CHG-20260831-04; each block describes its own constant |
| 16 | Rows that were second copies are removed | **Pass** — 2 tests, 3 parametrised entries |
| 17 | `internal_by_guess` is ~~four~~ **three** words (corrected in CHG-20260831-04) | **Pass** — and its docstring is 6 lines, not 13 |
| 18 | The two-rule count is corrected | **Pass** — ~~29 → 15~~ **29 → 14** in both records |
| 19 | The `wrong` qualifier is restored | **Pass** |
| 20 | The stale mutation anchors are repointed | **Pass** — 3 anchors; `test_no_shipped_mutation_has_a_stale_anchor` green |
| — | Mutations | **Pass** — `--only stranded` **12/12**, `--only closure` **8/8**, `--only reach` **12/12**; record cleared, `git status` clean |
| — | Full suite | **Pass** — 1807 passed, 4 skipped, pytest exit 0 |
| — | Ledger lint | **Pass** — `python tools/ledger_check.py --repo .` on 129 changes |

## The measurement that decided veto 1

Not an argument — a table. Five record shapes × two environments, driven through the real guard
function:

| record | `MUTATION_RUN` unset | `MUTATION_RUN` set |
|---|---|---|
| `{…, "owner": 999999}` | fails | fails |
| no `owner` key | fails | fails |
| `"owner": null` | fails | fails |
| truncated JSON | fails | fails |
| zero-byte file | fails | fails |

Four cells in the left column were **green** before this change, and the left column is the CI path.
The harness could not reach them: a mutation run always sets `MUTATION_RUN` to a pid, so
`--only stranded` was 12/12 with the hole open — the one column that does not regress is the only
column the instrument can see.

## A skip is not an acquittal — measured

The story is in [CHG-20260831-03](../changes/CHG-20260831-03.md). What was measured, on the shipped
tree, reverting each half of the guard fix in turn:

| reverted | before `_guard_must_fail` | after |
|---|---|---|
| the per-path owner filter | `31 passed, 4 skipped` — green | **`4 failed`** |
| the record-not-variable skip | `34 passed, 1 skipped` — green | **`1 failed`** |

`pytest.skip` raises `Skipped`, and `pytest.raises(AssertionError)` lets it straight through. Both
reverts therefore reported green. This is the fourth round in which the instrument, not the code,
was the finding.

## The refusals, and what reads them now

The narrative is in [CHG-20260831-03](../changes/CHG-20260831-03.md); this is the evidence. Three
refusals named a remedy and none led there, and no test read any of the three messages. What was
added:

| refusal | pinned by |
|---|---|
| the secret scan's placement sentence | `test_the_refusal_names_where_it_found_the_secret` — 3 cases |
| the ghost-test lint's "say it was removed" | `test_this_repos_own_ledger_passes`, over a record that says it |
| `runner models` | `test_every_command_the_cli_tells_you_to_run_exists` — reverting the citation goes red |

All three assert on the **message**, not the return value. Duplicating the change record's prose
here is what let row 13 of the -02 pair drift from its own CHG (CHG-20260831-04, idiom seat).

## §7 prohibition re-check

- No skill logic re-implemented; nothing under `skills/` exists here.
- `src/` changes: `models.py` gains `_looks_secret`, ~~`graded_by_the_bare_host_rule`~~ `graded_by_guess`
  (renamed in CHG-20260831-04) and `LOCAL_SUFFIXES`, renames `_secret_in_query` → `_secret_in_url` and
  `graded_internal_by_a_bare_host` → `internal_by_guess`; `cli.py` names the store path in one
  refusal and trims a comment. `GATES`, `PERMANENT_HALT_KINDS`, `CLASSES` and the graph untouched.

## Reservations (disclosed, not waived)

1. **Written by the implementer.** Eight rounds: 35, 36, 33, 31, 28, 23, 31 findings and this one,
   every verdict `fail`. Seven of the eight had their largest finding inside the previous round's
   fix.

2. **`mutation_recovery.write` has no error handling, and it failed once here.** During
   `--only reach`, restoring `models.py` raised `OSError: [Errno 22] Invalid argument` and the
   process died with a bare traceback, leaving the mutated text on disk. Reconciled by hand against
   the record and verified byte-equal to `original`; the rerun is 12/12 and does not reproduce. Not
   fixed, because I have one occurrence and no cause — a retry invented against an unknown failure
   is a guess. What the guard did do is surface it, which is what it is for. Worth its own change.

3. **The conformance seat's raw output is not re-readable in this session.** Its findings are
   carried from the round-8 notification. Where a claim of mine rests on it alone, the record says
   so; the per-seat counts given are the three I can verify.

4. **A secret in a path segment is still not caught.** `https://host/v1/sk-ant-SECRET/messages` has
   no key to match on. Unchanged from CHG-20260831-02 reservation 2, and unchanged by design.

5. **`_looks_secret` is a shape heuristic and will be wrong both ways.** A 32-character user name is
   refused; a short custom-prefixed token is not. It is deliberately narrow — the alternative
   refused `https://alice@host/v1` and named a remedy that did not apply. Pinned by cases, not
   proved.

6. **`_alive`'s coupling to `_owner` is still deferred**, and this round touched `_owner`'s call
   sites for the third time. Latent; named in CHG-20260831-01 reservation 2 and unchanged.

7. **`test_concurrent_config_edits_and_model_adds_do_not_deadlock` is load-sensitive.** The idiom
   seat saw it red under three concurrent pytest processes (`URLError [WinError 10061]`) and green
   alone; `job.result(timeout=30)` cannot tell a deadlock from a loaded host. Unrelated to this
   change, seen in three consecutive rounds now, and worth its own.
