# ACC-20260703-05 — Acceptance: resident dashboard per-keystroke I/O caching

- Target CHG: CHG-20260703-05 (cache per-keystroke I/O; no typing lag)
- Verifier: V1 (independent; ≠ implementer I1; model opus, Explore/read-only tool loadout)
- Date: 2026-07-03 (UTC+0)
- Skill: ai-sdlc v1.12.1
- Risk: low (presentation-layer caching; no governance/orchestrator/gates change; snapshot path preserved)
- Conclusion: **Pass**

## Check-details

| # | Check | Method | Result |
|---|-------|--------|--------|
| 1 | Cache fields + `refresh()` present; status/verify memoized; exec_log/agent stay live | Read `dashboard.py` L68-69,81-94,126-149,170-188 | Pass — `_status_cache`/`_verify_cache` default None; `refresh()` clears both; compute-if-cold-then-cache; exec_log/agent read `self.events` uncached |
| 2 | Zero git/disk I/O per keystroke | Headless: monkeypatch `_git`/`Path.read_text` counters, 20× calls | Pass — `_git` 0→1→(refresh)2→2; ACC reads 1→(refresh)2; O(refreshes) not O(keystrokes) |
| 3 | Refresh only on real events | Read `open_project` L513-519, `_start_run` L661, `run_resident` loop L697-747 | Pass — refresh on `/open` and after `orchestrator.run`; keystroke path never refreshes |
| 4 | Snapshot path unchanged | `render_snapshot` unchanged; cold-cache tests | Pass — snapshot + progress/lock/ACC tests green |
| 5 | Governance untouched | `git diff main -- orchestrator.py gates.py` | Pass — empty diff; pure-function input handlers unchanged |
| 6 | Tests green | `pytest -q` plain + with `AI_SDLC_SKILL_PATH` | Pass — 151 passed/7 skipped; 158 passed with skill path |

## §7 prohibition re-check
- No governance logic added to the presentation layer: runs still flow through the unchanged `orchestrator.run(approver=..., on_event=...)`; halt gates unchanged (`gates.py` diff empty).
- Red-line/HALT approval semantics untouched: `approve_decision` maps unresolved/cancelled answers to no-decision (never implicit approve); `_start_run` approver wraps it with `bool(...)` so a cancelled asker rejects. Confirmed by `test_resident_app_reject_at_halt_gate_halts_the_run`.
- No orchestrator/gates/state mutation introduced by caching; `refresh()` only nulls two presentation caches.

## Structure-consistency check
- `docs/structure/design.md` updated with the resident render cache + refresh-on-open/after-run note (per CHG modification-guide checklist, ticked).
- `docs/ARCHITECTURE.md` / `README.md` one-liner on cache-backed redraw present in the branch diff.
- CHG doc status "Implemented (pending ACC-20260703-05)" — this report closes it.

## Summary
The I/O-bound panels (`status_panel`: git + state/lock; `verify_panel`: ACC glob+reads) are memoized in `DashboardModel` and invalidated only via `refresh()`, which is called on `/open` and after each run — never on the per-keystroke redraw path. Headless counters prove per-keystroke git/disk work is eliminated (O(events), not O(keystrokes)). The read-only snapshot path is unchanged (cold cache computes once). Governance surfaces (orchestrator, gates, halt-approval mapping) are byte-identical to main. Full suite green (151/7; 158 with skill path). **Accepted.**
