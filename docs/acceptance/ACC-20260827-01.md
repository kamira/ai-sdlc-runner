# ACC-20260827-01 - Acceptance: the retired-backend refusal reaches the person who typed it

- Target CHG: CHG-20260827-01
- Verifier: **self** (the session that built it). Permitted at this risk grade, and disclosed rather
  than dressed up: every change in this batch is low, and none touches the red lines, the gate
  matrix or the flow. The independent work that produced these changes was the acceptance round of
  2026-08-27 - two engines that had built none of the code they read. What follows is the
  implementer checking their own fix, which is worth exactly what it is worth.
- Date: 2026-08-27 (UTC+0)
- Risk: **low**
- Conclusion: **Pass. The refusal a user sees is now the one the change wrote, and the check that proves it drives the CLI.**

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m ai_sdlc_runner.cli export --store mongo --format json
PYTHONUTF8=1 PYTHONPATH=src python -m ai_sdlc_runner.cli export --store nope --format json
PYTHONUTF8=1 PYTHONPATH=src python -m ai_sdlc_runner.cli export --help
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_conversations.py -q -p no:randomly
```

Batch-wide: `pytest tests/ -q -rs` -> **1347 passed, 3 skipped**; `tools/mutation_check.py` -> **15 of 15 caught**;
`tools/ledger_check.py --repo .` -> passed, 86 changes.

## Check-details

| # | Check | Method | Result |
|---|---|---|---|
| 1 | The defect reproduces before the fix | ran the CLI on `origin/main` | **Pass** - `runner export: error: argument --store: invalid choice: 'mongo' (choose from 'sqlite', 'file')`, exit 2 |
| 2 | The refusal now reaches the operator | same command, after | **Pass** - `the mongo store was removed in CHG-20260823-35; the model registry lives in SQLite ...` |
| 3 | An unknown store still reads as unknown | `--store nope` | **Pass** - `unknown store 'nope'; one of sqlite, file`. The two cases stayed distinguishable, which is the whole reason the retired list exists |
| 4 | `--help` does not advertise a retired backend | `export --help` | **Pass** - `[--store {sqlite,file}]`, unchanged |
| 5 | A test drives the CLI, not `backend()` | `test_the_person_who_types_a_removed_backend_is_the_one_who_sees_the_refusal` | **Pass** - subprocess, parametrised over both retired names, and it asserts `invalid choice` is **absent** |
| 6 | The declaration cannot quietly go back | `test_every_store_option_goes_through_the_one_checker` | **Pass** - reads `cli.py`'s syntax tree and refuses `choices=` on `--store` |
| 7 | One vocabulary, not two | read `_store_kind` | **Pass** - it consults `conv_mod.RETIRED` and `conv_mod.BACKENDS`; the wording is not copied |

## Why the existing test could not have caught this

`test_a_removed_backend_is_refused_by_name_and_told_what_happened_to_it` calls `conv.backend()`
directly. It passed before the fix, passes after it, and would pass with the CLI wire cut - because
it never touches the CLI. That is not a criticism of the test; it checks the vocabulary and does it
well. It is the reason a second test exists at a different altitude, and the reason the round's
brief asked verifiers for *the test that fails when the wire is cut* rather than *a test*.

The comment above the flag said `mongo` and `tinydb` "are refused **by name**" for four days while
they were not. A comment is not a mechanism either.

## Not closed by this record

Nothing. The `type=` callable covers every `--store` in the parser - asserted structurally rather
than by counting the one that exists today.
