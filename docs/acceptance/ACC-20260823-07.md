# ACC-20260823-07 — Acceptance: a blacklist finding nothing had been read as "safe"

- Target CHG: CHG-20260823-07 (unknown becomes its own target state, and it is not the safe one;
  resume reads the journal)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **high** — `halt_independent`, the permanent halts and the identity of a run.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** Both P0 fixes are wired, and both were proven by mutation: cutting the
  unrecognised-target stop turns nine engine-level tests red; cutting the journal read turns four red.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_targets.py tests/test_resume.py tests/test_nothing_is_unwired.py -q -p no:randomly
# mutations performed and reverted:
#   cut the unrecognised-target stop out of engine.py   -> 9 failed
#   replace the journal read with {}                    -> 4 failed
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | A target has **three** states, not two | `recognise` `policy.py:353` returns red / ordinary / **unrecognised**; `unrecognised` `policy.py:411`; `unverified` `policy.py:490` | `engine.py:962-969` — stops under the default `refuse`, naming the targets | `test_targets.py::test_a_target_this_runner_cannot_place_is_not_treated_as_safe` | **Pass, proven by mutation** — cutting the stop: **9 failed** |
| 2 | None of them reaches the end of a run | same | `walk` | `::test_none_of_them_reaches_the_end_of_a_run` | **Pass** |
| 3 | `on_trust` means *unverified*, not *untargeted* | `policy.on_trust` `policy.py:502` — true when ordinary with no targets **or** with unrecognised ones | `engine.py:971-980`, recorded in `report.on_trust` | `::test_on_trust_is_about_verification_not_about_having_said_something`; `::test_an_operation_whose_targets_are_recognised_is_not_recorded_as_trusted` | **Pass** — the disclosure was previously keyed on *having named a target*, which is the bug KN-15 records |
| 4 | Resume reads the journal and skips answered work | `AskJournal.answers` `engine.py:142`; `RunConfig.resume` `engine.py:406`; `walk` reads it `engine.py:1146-1153` and refuses resume without one | `--resume` `cli.py:1031`, `cli.py:678` | `test_resume.py::test_a_resumed_run_does_not_open_a_session_for_answered_work`; `::test_a_resumed_ask_never_touches_the_factory` | **Pass, proven by mutation** — journal read replaced with `{}`: **4 failed**. Sessions are **counted**, not taken on trust from a name |
| 5 | A journal that exists changes nothing without `--resume` | same | `walk` | `::test_without_resume_a_journal_that_exists_changes_nothing`; `::test_resume_without_a_journal_is_refused` | **Pass** — both directions |
| 6 | The unwired-symbol check stops vouching by coincidence | `_qualified_uses` `test_nothing_is_unwired.py:81` — `subprocess.run` no longer vouches for a local `run` | the suite | `::test_the_check_catches_a_planted_orphan` `test_nothing_is_unwired.py:168` | **Pass** |
| 7 | `--no-high-risk-mode` declines a saved bypass for one run | `cli.py:1022`; `cli.py:640-648` resets seats to the floor | `cmd_run` | `test_settings.py:373-415` | **Pass** — the CHG notes this had no test when written; it has several now |
| 8 | The five silent-pass commands are pinned verbatim | `SILENT_PASSES` `test_targets.py:254-259` | — | same | **Pass** |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Three-state recognition wired and stopping | PASS | PASS, plus a 9-test mutation | **Agreed** |
| Resume reads the journal | PASS | PASS, plus a 4-test mutation | **Agreed** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

The CHG's six documentation corrections were **spot-checked, not exhaustively traced** — the `fable`
engine verified ARCHITECTURE.md §8 and the four-layer description, and left the rest unverified
rather than claiming them. Recorded as such.
