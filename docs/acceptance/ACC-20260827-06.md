# ACC-20260827-06 - Acceptance: the replay's second script payload is escaped and unguarded

- Target CHG: CHG-20260827-06
- Verifier: **self** (the session that built it). Permitted at this risk grade, and disclosed rather
  than dressed up: every change in this batch is low, and none touches the red lines, the gate
  matrix or the flow. The independent work that produced these changes was the acceptance round of
  2026-08-27 - two engines that had built none of the code they read. What follows is the
  implementer checking their own fix, which is worth exactly what it is worth.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** - tests only
- Conclusion: **Pass, proven by cutting the call and watching the new test fail while the old one passed.**

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_conversations.py -q -p no:randomly -k script_block
# sabotage, performed and reverted:
#   __RAIL__ -> raw json.dumps  ->  the rail test fails, the __TURNS__ test still passes
```

Batch-wide: `pytest tests/ -q -rs` -> **1347 passed, 3 skipped**; `tools/mutation_check.py` -> **15 of 15 caught**;
`tools/ledger_check.py --repo .` -> passed, 86 changes.

## Check-details

| # | Check | Method | Result |
|---|---|---|---|
| 1 | A hostile node name cannot end the script block | `test_a_node_name_reaching_the_rail_cannot_end_the_script_block` | **Pass** |
| 2 | It bites | replaced `_script_json(rail)` with `json.dumps` | **Pass** - the new test failed; restored and re-verified clean |
| 3 | The existing test could not have caught it | ran it under the same sabotage | **Pass** - it passed. It puts hostile text in an *answer*, and the rail carries node names, so the payloads never met |
| 4 | The name is still shown, escaped | assert `alert(1)` survives in the rail | **Pass** - a rail that "fixed" this by dropping the node would satisfy the safety assertions and show the reader nothing |

## Not closed by this record

Nothing. Exposure was always low - the rail carries node ids and clock strings, not model prose -
and low exposure is a reason to rank it below the `__TURNS__` hole, not a reason to leave a payload
unchecked in the change whose subject was unchecked payloads.
