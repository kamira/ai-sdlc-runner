# ACC-20260925-01 — every order says how to answer it, and a backend gets an hour

- Project: ai-sdlc-runner
- Target CHG: CHG-20260925-01
- Date: 2026-09-25 (UTC+0)
- **Measured against `origin/main` at `5216941`**, the tree this branch is cut from.
- Reviewed by: a four-seat panel — two `fable` seats and two `opus` seats, each in its own session
  (DIR-1: `codex` could not be reached) — four times: on the design before the build, on the
  implementation at `0b8119a`, on its fixes at `c89fe8e`, and on the verification round's fixes at
  `30ddf02`. The first three rounds were independent and then cross-read; the fourth
  was too. The implementer is none of them: on a high-risk change the verifier is not the
  implementer. Probe paths in the verdicts below are a session scratch directory (`<scratch>/`),
  not this tree.
- Conclusion: **Pass** — on the measurements and verdicts below. The final check found no blocker;
  its residue — chiefly the one-walk cut — is answered in the commit this record lands with, by the
  implementer, with tests and mutations, and **no seat has read that commit**. This line does not
  claim otherwise; a conclusion is written before its own review.

## What the design panel said, and what answered it

Final verdicts after cross-reading (the model field is given as the seat's label; the rest verbatim):

```
[verdict] conformance-fable | fable | concern | probes/conformance_cross.py §i (prose `pm_confirm` reused on resume, 0 dispatched) and §j (no-frontier `{"module":"","error":…}` → `finished`); phase-1 probes B/C | The field, its computation site, the resume mechanism and the timeout are sound; before build the record must be amended, not redesigned: reword the `unattended` "in the terms `keys` gives you" clause and give `module`/`error`/`undecided`/the intake lists their `means`, write out `format` and the descriptor strings, exclude the fixed text from the reuse compare, and correct §3's false "accepted by the walk" claim.
[verdict] defect-fable | fable | fail | <scratch>/probes/defect_phase2_probe.py (output above) with defect_probe.py, and the cross-tree runs against probes/sim | The `unattended` clause "say so in the terms `keys` gives you" is measured to turn an engineer's failure into `finished` (list config) or into an error that blames the backend (frontier), the model-facing `format`/descriptor text the change consists of is not written in the record, and §3 rests on the false claim that journaled answers were accepted by the walk — each must be fixed in the design before anything is built.
[verdict] idiom-opus | opus | fail | CHG-20260925-01.md:64,72-76,118-121 + probes/idiom_cross.py (A2, C2-C5, H), idiom_cross2.py (39 engineer asks to the step guard), idiom_readers.py | The model-facing text is unwritten, and the design as sketched points a literal model at wrong answers: it tells the model not to wait for approval with nothing about `permanent_halts`, it has no presence or empty rules, and its failure paths either walk on or loop to the step guard. The resume rule also rests on a false "accepted" claim.
[verdict] risk-opus | opus | fail | probes/p2_poisoned.py, probes/p4_cross.py, probes/risk_cross.py, probes/risk_cross_walk.py (all under <scratch>/probes/) | The fixed `unattended` sentence and the unwritten descriptor text steer models into answers the readers act on wrongly: `module:""` read as done, `module` with `error` recorded as built, `unsafe:["None"]` suspending the run, and no stop left for a question about a red line. The resume rule also rests on the false claim that journaled answers were accepted. The text must be written and checked against the readers before building.
```

| objection (seats) | answered by |
|---|---|
| the model-facing text was not written (all four) | CHG §2 quotes `REPLY_FORMAT` and `REPLY_UNATTENDED` as shipped, and §1 every schema's descriptions |
| the vocabulary could not say "leave this out" or "an empty list means none" (idiom, defect, risk) | a closed JSON Schema subset: `required`, `additionalProperties: false`, `description` on every property |
| "say so in the terms `keys` gives you" steered a failing engineer into "nothing left", or into an error blaming the backend (all four) | the sentence is reworded; a reported failure stops at the engineer's own ask (`_stop_on_failed_build`), a named module with `error` stops too, and the message no longer blames the backend |
| nothing about `permanent_halts` beside "do not wait for approval" (risk, idiom) | `REPLY_UNATTENDED`: "Nothing in this order permits performing any of `permanent_halts` against a real system …" |
| the resume rule rested on a false "accepted by the walk" claim (risk, defect, idiom) | the claim is withdrawn; a journaled answer is reused only if its own reader would take it (`_heard`, and the ask's `accept`) |
| tuning fixed wording would re-ask every journaled node (risk, idiom) | `_comparable` compares the schema's structure only — no fixed sentence, no property description |
| a closed-count test goes hollow under the new required keyword (risk) | narrowed to `WorkOrderError` "outside the contract" |
| `why` hides intake findings in the log line (risk) | `_summary` reads `why` last |
| the risk grade (risk) | graded high |

## The implementation, at `0b8119a`

Final verdicts after cross-reading:

```
[verdict] conformance-fable | fable | fail | docs/SCHEMAS.md:157-220, README.md:545-556, docs/structure/design.md:26,40 vs workorder.py:244-253 / engine.py:903,1156-1177; probes2/p1_failed_build_resume.py (4/4 replay, 0 re-asked) | The code ships the amended design (schema field, both sentences verbatim, both failed-build stops, `_conforms`/`_comparable`, 3600), but the three public contract pages still publish the rejected `reply.keys`/`_reads` draft, and the new failed-build stop cannot be resumed past although README and `AskJournal.refuse` promise exactly that; both must be fixed before merge.
[verdict] defect-fable | fable | fail | docs/SCHEMAS.md:157-220, README.md:545-547, docs/structure/design.md:26,40 vs `workorder.reply` output; probes2/df_mutsim.py | the code answers the design panel's objections and every seat's behavioural claims checked out, but README, the closed schema doc and design.md still publish the rejected `reply.keys`/`_reads`/unconditional-resume draft, and the record's mutation claim is contradicted by measurement (adjacent same-size mutants alias) — fixable in docs and one env line, but not merged as is.
[verdict] idiom-opus | opus | fail | docs/SCHEMAS.md:157-220, README.md:545-547, docs/structure/design.md:26,40; engine.py:3150 + probes2/p1_failed_build_resume.py; probes2/idiom2/mutloop.py | The code answers the design panel's objections, but the three public contract pages describe the rejected `reply.keys` draft, and a failed build is a stop that no resume can get past: it is journaled `answered` with no `accept=`, 4 of 4 stopped again with 0 re-asked. Separately, the reply-contract mutation group's stability claim is wrong: 6 of 12 sequences aliased.
[verdict] risk | risk-opus | fail | <scratch>/probes2/{p1_failed_build_resume.py,p3_fixpass.py,xc5_empty_module.py} + docs/SCHEMAS.md:157-220, README.md:545-556, docs/structure/design.md:26,40 | A failed build replays its stop on every resume (4 of 4, 0 re-asked) and even after a passed fix pass, an engineer that follows the `module` text alone produces a false `finished`, and all four seats found that SCHEMAS, README and design.md publish the rejected `reply.keys` draft.
```

| finding (seats) | disposition, at `c89fe8e` |
|---|---|
| README, SCHEMAS and `design.md` still published the rejected draft (all four) | rewritten from the code; `test_no_current_document_describes_the_rejected_draft` guards it |
| a failed build replayed its stop on every resume, stopped only after two more asks, and survived a fix pass (all four) | the stop moved to the ask (`accept=_stop_on_failed_build`), journaled `refused`; a resume asks the engineer again |
| `_conforms` stricter than the readers — old `engineer_build` answers re-asked (defect, risk) | replaced by `_heard`, which consults the readers themselves |
| `voices` open (conformance) | closed (`VOICES`), an unknown path raises |
| the log line hid `error` and `risk` (all four) | `_summary` leads with them |
| the reply-contract mutations aliased a stale `.pyc` (defect, idiom) | no `after` the size of its `before`, no same delta twice in a row on one file |
| the model-facing wording (idiom) and the example agent's extra keys (idiom, risk) | reworded; `examples/minimal/agent.py` answers only named keys |

## The verification round, at `c89fe8e`

Final verdicts after cross-reading:

```
[verdict] conformance-fable | fable | concern | probes3/cf_serve_restart.py, probes3/risk/fb.sh (WRITER=5216941 fail_empty list; plan_empty frontier), probes3/risk/conv_probe.py; engine.py:979-980, :1213-1215, :1281-1292 vs CHG-20260925-01.md:100,183,316 | Both round-1 blockers are fixed and verified (docs match the code page for page bar four nits, a failed build stops at its ask, journals `refused` and resumes under `--resume` and under serve's `start`), but three should-fix items remain: the new `modules` clause invites an answer that wedges the journal one node earlier than the class this round closed, the accept-refusal path drops the engineer's `why` from the conversation (a regression against 0b8119a), and a pre-change journal's failed build is rebuilt and merged on the old attempt's reviews; none blocks the merge if the record states them and the ACC lands with it.
[verdict] defect-fable | fable | fail | probes3/df_resume_refusals.py cases 18–20, probes3/idiom3/emptyplan.py, probes3/risk/fb.sh (WRITER=old fail_empty/fail_named), probes3/idiom3/convlog2{,_old}.py vs src/ai_sdlc_runner/engine.py:975-977,1155-1181,1213-1215,1286-1292 and examples/*/plan.json:4 | every round-1 defect is fixed and verified by probe, and all four seats agree on every fact; I hold fail on severity alone because the fix's own `modules` text — in the order every shipped example's frontier configuration renders — prescribes an answer that the runner then punishes with a non-resumable stop or a 135-ask loop, contradicting the record's "exactly as strict as the walk", and the same change turned an upgrade-path stuck resume into a merge on stale reviews while dropping the failure's `why` from the conversation; three small fixes (a `pm_plan` accept, stop reuse after a declined entry, log the refused result), each needing a test.
[verdict] idiom-opus | opus | concern | <scratch>/probes3/{idiom4/stale4.py,idiom4/bf.py,idiom4/allmut.py,idiom3/emptyplan.py,idiom3/convlog2.py,cf_serve_restart.py,risk/fb.sh,risk/gate_reject.py} | Both round-1 blockers are fixed, and a failed build resumes under both `--resume` and serve (POST /run). Should-fix items remain, and I hold them at should-fix rather than fail: the new `modules` text leads to a late stop that replays on resume, with `_heard` looser than `_frontier` despite the "exactly as strict" claim; a pre-change journal's failed build is rebuilt and then reuses stale reviews; a failed build's `why` is lost from the conversation; and `error: true` beside a named module is recorded as built.
[verdict] risk | opus | concern | <scratch>/probes3/risk/{fb.sh,stale.py,conv_probe.py,gate_reject.py} + probes3/cf_serve_restart.py + probes3/idiom3/emptyplan.py | Both round-1 blockers are fixed and measured, including a serve restart, but three should-fix risks remain: the new `[] if you could not plan` text on the frontier config every example ships leads to a stop that replays on every resume, a pre-change journal's failed build is rebuilt and merged on stale reviews, and a failed build's `why` is lost from both the conversation and, after a resume, the journal; none must block the merge, but the first is a one-sentence fix that belongs in this change.
```

Every seat found both round-1 blockers fixed. The findings, and what was done with each at
`30ddf02`:

| finding (seats) | disposition |
|---|---|
| the new `modules` text sent a planner that could not plan into a late stop that replayed on every resume, or a 66-lap loop (all four; `fail` from defect) | `pm_plan` carries `_stop_on_empty_plan` under a `"frontier"` decision: an empty list, or none where no earlier plan gave one, stops at the ask, journaled `refused`, and a resume asks for the plan again; the text now says the run stops *at this order*; `_frontier`'s backstop no longer blames `pm_plan` |
| a failed build's `why` and full `error` reached nothing a person reads (all four) | `_build_failed` carries both whole in the stop's message, which is the text of the conversation's `unanswered` turn |
| an older runner's journal past a failed build resumed onto reviews of the failed attempt, through `merge` (all four) | once a same-question entry is declined for reuse, `_ask` clears the rest of the journal for that walk; a changed question still does not |
| `_heard` "exactly as strict as the walk" was false both ways (conformance, idiom, risk) | the looser side is closed by `_accept_for`; the stricter side (an answer an after-gate's rejection makes unread) is recorded in the docstring and §4, not changed |
| `error: true` beside a named module recorded as built; whitespace `error` stopping (defect, idiom) | `true` stops, a blank string does not |
| "journaled as refused" said by the backstops too (defect, idiom) | said only from the ask's own refusal |
| a missing `module` with a failure key now stops — new, not "predating" (defect, idiom, risk) | kept, and stated as new in §3 and README |
| ACC cited in the present tense (all four) | this record lands with the change |
| `pending|answered` in SCHEMAS §7 and `design.md` (all four) | `pending|answered|refused` |
| README "no other key but `why`" (idiom, risk) | "no key it does not name" |
| the CHG's `missing` quote; `_comparable`'s docstring (conformance, defect, idiom) | corrected |
| three dead guard needles (conformance, idiom) | replaced by the draft's own spellings, each found in that tree |
| the `.pyc` invariant held by hand (conformance, defect) | pinned for the group by `test_the_reply_contract_group_cannot_import_a_stale_pyc`; the record scopes it to the group |
| the `module` description's two conditions for `""`; the approvals sentence beside `halt` (idiom) | reworded |
| the `missing` dash clause; "exactly as the plan names it" (idiom) | not changed: the cross-read found the `missing` text doing its job, and the order does not name the modules (recorded under "What this does not do") |
| `tide-spa`/`weather-spa` agents answer `wrote`/`note` (all four) | listed under "Left as they are" |
| the atlas chip beside a closed contract (conformance, idiom) | the chip says which side it describes |
| `serve` after a stop (risk) | recorded: `POST /run` walks against the journal again (two seats measured it); the gate routes need a suspension |
| stuck resumes past the step guard; `check_decisions` admitting `"frontier"` in a list (defect, idiom, risk) | pre-existing; recorded under "What this does not do" |

## The check of those fixes, at `30ddf02`

```
[verdict] conformance-fable | fable | concern | probes4/cf6_serve_stale_tail.py (serve `start` -> suspended at merge; `approve merge` 0 asked, 9/9 stale reused incl. `merge`), probes4/ro/ro_merge.sh (real 5216941 writer, R2 0 asked), probes4/df4_two_step_resume.py (9/9, 9/9, 8/8) vs engine.py:1238,:2615,:241-242,:123-127 and CHG-20260925-01.md:212,283-285 + commit message | every verification-round finding is fixed or recorded for a single uninterrupted walk, but the clear-on-decline is in-memory and the next walk (serve's ordinary gate-approval walk, the CLI's `--confirm`-per-gate flow, a second failure, a crash) reuses the failed attempt's reviews and the old run's `merge` while the record and commit state the finding closed; limited to pre-change journals and no worse than c89fe8e, so a small durable cut plus a two-walk test or a scoped claim, not a merge blocker; the rest is wording.
[verdict] defect-fable | fable | concern | probes4/df4_two_step_resume.py A1–A3, probes4/fp1_stale_tail.py, probes4/df5_clear_edges.py (g2)/(h), io5/replan.py and io5/plan_conv.py re-run on both trees vs src/ai_sdlc_runner/engine.py:1230-1238,:241-242,:1584-1586 and docs/changes/CHG-20260925-01.md:212-217,:283-285 | Every verification-round finding is fixed or recorded and each fix does what it says on one uninterrupted walk (empty plan stops and resumes, `why`/`error` reach the conversation, bool `error`, every quoted line), and all four seats agree on the residue; but the reuse clear is in-memory and skips `refused` entries, so a pre-change journal whose first resume is interrupted — a second failure, a crash, a gate suspension, every `serve` gate — still merges the rebuild on the failed attempt's reviews (9/9, 12/12) and reuses the empty plan's confirmations (3/3) while the record and commit present the finding as closed; a small persisted cut plus two-resume tests, plus docstring/doc lags (`_heard`, `_build_failure`, "whole", data.md, ARCHITECTURE.md) — not a merge blocker.
[verdict] idiom-opus | opus | concern | <scratch>/probes4/io6/{fail_twice.py,plan_twice.py,nojournal.py,nonmap.py}, probes4/io5/{clear_merge.py,clear_durable2.py,clear_siblings.py,plan_conv.py} vs src/ai_sdlc_runner/engine.py:1230-1238,:241-242 and docs/changes/CHG-20260925-01.md:212-217,:283-285,:374-377 | Every verification-round finding is fixed or recorded except the upgrade-path one. The clear-on-decline is in memory, and a `refused` entry never triggers it, so after an interrupted first resume (the engineer fails again, a gate suspends, a crash) an older runner's journal reuses 9 of 9 (list) and 12 of 12 (frontier) reviews of the failed attempt, or the empty plan's confirmations, while the record, the Review section and the commit call it answered. That is a small durable cut plus a two-resume test, or narrower claims; it is not a merge blocker, and I disagree with conformance-fable's "fixed".
[verdict] risk-opus | opus | concern | probes4/ro/{ro_gates.sh,ro_merge.sh,ro_durable.sh,ro_durable2.sh,ro_panel.py,ro_replan.py,ro_conv.py}, probes4/ro2/{pv.py,nonmap.py}, probes4/io5/plan_conv.py (re-run on both trees) against engine.py:1231-1238,:2615,:241-242 and CHG-20260925-01.md:212-217,:283-285 | Every verification-round finding is fixed or recorded, but the fix for reusing the failed attempt's reviews lasts only one walk: the next walk after a gate suspension, a second failure or a crash reuses the old runner's reviews, QA or `merge` (three seats measured this independently), the record says the finding is closed, and a refused plan's prose now reaches no one after a resume. These are should-fix, not merge blockers, because they only affect journals an older runner wrote.
```

All four held `concern`, none `fail`, and all four agreed that every verification finding was fixed
or recorded. The findings, and what the commit this record lands with does with each:

| finding (seats) | disposition |
|---|---|
| the cut lasted one walk: it cleared the walk's copy only, and a `refused` entry never triggered it, so a first resume interrupted by the engineer failing again, a gate suspending or a crash let the next walk reuse 9 of 9 reviews of the failed attempt (12 of 12 under `"frontier"`), the old run's `merge`, or an empty plan's confirmations; the record and the commit called it closed (defect, idiom, risk measured it; conformance agreed in the cross-read) | the cut is on disk: `AskJournal.forget_after` drops every ask numbered after the declined one; `test_the_cut_outlasts_a_resume_that_was_interrupted` (engineer fails again; gate suspends before `merge`) fails without it; two mutations |
| a refused answer's own words reached nobody — a planner's prose, a CLI's `stderr` (idiom, risk, defect) | the unanswered turn carries the answer as given, up to 4000 characters; a test and a mutation |
| the cut is by position, so a panel's other voices are re-asked (conformance, defect) | kept, and said in §4 and SCHEMAS §7: re-asking costs asks, the reuse it replaces cost a merge |
| "whole" was a 2000-character cap, and `error` was said twice (conformance, defect) | "up to 2000 characters"; the long form only where the failure line cut it short |
| "journaled as refused" printed with no journal (conformance, defect, idiom) | "Where the run keeps an ask journal, …" |
| `_stop_on_empty_plan` "exactly as strict as the reader" — stricter where a re-plan replaced `[]` before a frontier decision (conformance, defect) | docstring and §3 say where it is stricter and that the order's text promises it |
| `_heard` "no stricter than the walk"; `_build_failure` "predates this change" (conformance, defect) | both docstrings match §3 and §4 |
| `docs/structure/data.md` and `docs/ARCHITECTURE.md` without `refused` (conformance, defect, idiom) | both name it; ARCHITECTURE's resume sentence says what is and is not reused |
| the approvals sentence named 2 of the 4 verdicts (`auto`, `halt_independent` absent) (conformance, defect) | "Whatever `policy_verdict` says, any gate it names is the runner's to take …" |
| `_summary` printed `error: false` as the line (defect, risk) | skipped as the key left out; a test row and a mutation |
| §5 motivated `error`-first by the stopping ask, now an unanswered turn (conformance, defect) | reworded |
| a non-object answer under an `accept` raised inside `refuse`, leaving the entry `pending` (conformance, defect, idiom; pre-existing) | `refuse` keeps `{"answer": repr(...)}`; a test and a mutation |
| `POST /run` after a stop starts a new run, so its gates ask again (conformance, idiom) | one clause in §4 |
| the `missing` dash wording; a whitespace-only `module` or `error` beside an empty `module` (conformance, defect) | not changed, listed under "Left as they are" (the `module` half is how `_frontier` has always read it) |
| the ACC absent while cited (conformance, defect) | this record |

## Criteria

| # | Criterion | How | Verdict |
|---|---|---|---|
| 1 | Every order carries `reply`, and its schema is what that ask's reader acts on, on every dispatch path | `test_reply` walks answer from `reply` alone: the default flow, a `"frontier"` decision, every model-panel node on two models, the option ask, survey and review seats — each finishes or is accepted | Pass |
| 2 | The two fixed sentences are the ones the CHG quotes | §2's blockquotes equal `REPLY_FORMAT` and `REPLY_UNATTENDED`; checked by the seats each round | Pass |
| 3 | A failed build stops at its own ask, journaled `refused`, and a resume asks the engineer again | `test_a_failed_build_stops_at_its_own_ask`, `…_resume_after_a_failed_build…`, `…older_runner…`; mutations | Pass |
| 4 | An empty plan under a `"frontier"` decision stops at its own ask, and a resume asks for the plan again | `test_an_empty_plan_under_a_frontier_decision_stops_at_its_own_ask` and three more; mutations | Pass |
| 5 | Resume: a journal from before `reply` is reused where its readers accept it; a refused answer is asked again; nothing after it is reused, in this walk or a later one | `test_a_journal_written_before_reply…`, `…old_journal_in_the_shapes…`, `…nothing_said_after_a_refused_answer…`, `…cut_outlasts…` (two interruptions); mutations | Pass |
| 6 | Rewording the runner's own text re-asks nothing; a changed schema is asked again | `test_the_runners_own_wording…`, `…changed_schema…`; mutations | Pass |
| 7 | `agent_timeout` defaults to 3600 s, reaches the process, and README and `runner.yaml` say so | `tests/test_agent_timeout.py`; mutations | Pass |
| 8 | No current page describes the rejected draft | `test_no_current_document_describes_the_rejected_draft`, needles from the draft's own tree | Pass |
| 9 | Every wire above is pinned | mutation group `reply-contract`, 35 of 35 caught; the affected groups re-run (below) | Pass |
| 10 | The suite and the ledger pass on every CI platform | CI at `30ddf02`: ledger and pytest on ubuntu and windows × py3.9 and py3.13, all green; the head this record lands with is checked by the same CI before merge | Pass |

## What this does not do

See the CHG's section of the same name: nodes whose answers nothing reads, a voice's `why` not
carried into rework, the send-back reason not scanned for red lines (queued for the user as its own
change), no deadline in the order, no kill of a timed-out backend's children, no module list in the
build order, stops past the step guard that still replay on resume.

## The run

```
full suite, at the head this record lands with
  2421 passed, 6 skipped (2427 collected; README says 2427)
tools/ledger_check.py
  ledger check passed (244 changes)
tools/mutation_check.py --only reply-contract
  all 35 caught
every other group that targets a changed file, re-run on the same tree
  ask-in-flight 13, bounds 6, bypass 4, claim-referent 3, clock 1, contradiction 3, decisions 6,
  doc-anchors 3, effect-state 1, effects 12, emergency 7, finish 2, frontier 4, grading 3,
  importer 7, modules 13, planning 11, propagation 3, record 3, record-lifecycle 3,
  refusal-routing 3, refused-answer 2, risk 13, routing 6, two-views 2, unsafe-stop 5, voice 3,
  workorder 9 — all caught
  (grading's first run read BASELINE RED on test_ledger_check: the CHG said Done before this
  record was in the tree; with it, 3 of 3)
  classes (1) and codecs (2) fail identically on the pre-change tree and are not this change's
CI at 30ddf02
  ledger; pytest ubuntu-latest py3.9, py3.13; windows-latest py3.9, py3.13 — all success
```
