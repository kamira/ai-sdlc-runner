# ACC-20260823-02 — Acceptance: the red lines stopped being a word list and became a declaration

- Target CHG: CHG-20260823-02 (an operation declares its kind; silence is refused by default; the
  word list survives only as a backstop)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions, no sight of each other's findings.
- Date: 2026-08-27 (UTC+0)
- Risk: **high** — `halt_independent`, and the CHG's own round-2 verdicts were Fail (codex-seat) and
  Pass-with-reservations (fable-seat) on the work this record closes.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md) for what
  that means and for the machine evidence quoted below.
- Conclusion: **Pass.** Both engines confirmed independently; the declaration layer is wired ahead of
  dispatch, cutting it turns eighteen tests red, and the one claim that no longer reads true today
  was true at this change's own commit and moved only because a later governed change narrowed it.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_flow.py tests/test_nothing_is_unwired.py -q -p no:randomly
git show 89c5df8:tests/test_flow.py     # the mis-declared test, as it stood at this change
# mutation performed and reverted by the fable engine:
#   remove the declared-kind branch from policy.classify, re-run  ->  18 failed
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | An operation declares its kind, from a closed set | `policy.classify` `policy.py:434-487` | `engine._permanent_halt` `engine.py:957`, from `walk` `engine.py:1242` | `test_flow.py::test_a_declared_red_line_stops_however_it_is_worded` | **Pass, proven by mutation** — removing the declared-kind branch: **18 failed** |
| 2 | Silence is refused, not assumed safe | `engine.py:938-951`; `RunConfig.undeclared="refuse"` `engine.py:398` | `--undeclared` `cli.py:974`, `cli.py:1017` | `test_flow.py::test_a_node_that_does_work_and_declares_nothing_is_refused`; `::test_refusing_is_the_default` | **Pass** — the default is a refusal, so the failure now needs a wrong declaration rather than an unusual phrasing |
| 3 | The `pr` gate can actually fire | `policy.py:120` — `"pr"` high = CONFIRM | the gate table | covered by the gate-matrix tests | **Pass** |
| 4 | `feasibility_confirmed` holds the lead's assessment instead of stopping in front of it | `graph.py:149-150` — `lead_assess`, `gate_when="after"` | `engine.py:1261` | `test_flow.py::test_a_gate_that_confirms_someones_judgement_waits_for_the_judgement` | **Pass** — this was `fable-seat`'s round-2 finding and it is closed |
| 5 | `--risk` / `--seats` / `--seat-model` reach the walk | `cli.py:973`, `cli.py:1000-1004` | `engine.walk` via `cli.py:698-699` | `test_cli.py` parser tests | **Pass** |
| 6 | The probes are called by something | `ship.py:78`, `:110`, `:118` | `cli.effects_provider` | `test_plan.py:84` | **Pass** |
| 7 | One confirmation covers one stop | `engine.py:1137-1160`, `:1076-1078` | the gate check | `test_flow.py::test_one_confirmation_covers_one_stop`; `::test_confirming_twice_covers_two_stops` | **Pass** |
| 8 | An unreached confirmation is reported, not dropped | `pending()` reader, `cli._report_pending` `cli.py:413` | called on **both** the exception path `cli.py:702` and the normal path `cli.py:705` | `test_settings.py::test_a_confirmation_the_run_never_reached_is_reported_not_dropped` | **Pass** — a report only correct when the run succeeds is not a report, and this one is on every exit |
| 9 | Nothing is left unwired | AST orphan scan `test_nothing_is_unwired.py:29`, `:180` | the suite | `::test_the_check_catches_a_planted_orphan` | **Pass** — the check is proven able to fail |

## The claim that drifted, and the proof it was true when made

The CHG says *"all twelve bypass sentences now stop, both when declared and when mis-declared
`ordinary`."* Today the mis-declared half is a computed subset (`BACKSTOP_CATCHES`,
`test_flow.py:448`) rather than all twelve.

`git show 89c5df8:tests/test_flow.py` shows the mis-declared test parametrised over the whole
`BYPASS` corpus at this change's own commit. The narrowing is **CHG-20260823-06's**, made
deliberately to cut a false-stop rate of two jobs in three, and pinned there. The declaration layer
— the thing this change is actually about — still stops all eighteen sentences
(`test_flow.py:942`).

Documented drift, not a claim that was never true.

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Declaration wired ahead of dispatch | PASS | PASS, plus an 18-test mutation | **Agreed: pass** |
| Mis-declared coverage | not raised | raised, then dated to `89c5df8` and attributed to CHG-06 | **Drift, disclosed above** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

**The declaration is only as strong as the planner** — the CHG says so itself and it remains true.
An operation declared `ordinary` and described in words that give nothing away is dispatched unless
a target gives it away. That is what CHG-20260823-04 went on to address, and it is why the two
records should be read together.

