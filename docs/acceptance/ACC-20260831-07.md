# ACC-20260831-07 — Acceptance: the guard that turned a traceback into an overwrite

- Target CHG: CHG-20260831-07
- Verifier: self (main agent = implementer)
- Reviewed by: a four-seat panel over `e50c1e7..934cef1` — `conformance`, `defect`, `risk`, `idiom`
  — each in its own git worktree, none seeing another's answer. The panel is the **input** to this
  change, not its verification; see reservation 1.
- Date: 2026-08-31 (UTC+0)
- Risk: medium — the harness's recovery, the ledger's excusal rule, the mutation harness
- Conclusion: **Pass on the corrections.** The panel's verdict was **fail** from all four seats,
  with nine vetoes; this change is what those verdicts asked for.

## Check-details

| # | Task | Result |
|---|------|--------|
| 1 | A record whose owner cannot be read is refused | **Pass** — `"owner": "nope"` keeps file and record; it restored and deleted both |
| 2 | `_alive`'s comment says what it does | **Pass** |
| 3 | The paragraph break is pinned | **Pass** — disabling it is `6 failed`; it was `0` |
| 4 | CRLF is the case that mattered | **Pass** — a CRLF break is a row of its own |
| 5 | The break matches U+200B and U+FEFF | **Pass** — both refused; both were excused |
| 6 | The verbs are case-insensitive | **Pass** — reverting is `2 failed` |
| 7 | `renamed to the new name in CHG-…` is accepted | **Pass** |
| 8 | Ordinary punctuation is not a sentence end | **Pass** — 5 rows; reverting the rule is `1 failed` |
| 9 | Every markdown link form is accepted | **Pass** — inline, reference-style, anchor |
| 10 | The href claim is withdrawn | **Pass** — and the row that asserted it is gone |
| 11 | The scan starts where a match can | **Pass** — 69 KB: 2.24s → **0.02s** |
| 12 | The four recovery guards are pinned | **Pass** — reverting them is `5 failed`, `1 failed`, `1 failed` |
| 13 | A collection error is not a catch | **Pass** — `BROKE`, returns False |
| 14 | A directory gets its own refusal | **Pass** — it names the path and does not claim an edit |
| 15 | A file that is not UTF-8 gets one too | **Pass** |
| 16 | The duplicated comment is written once | **Pass** |
| 17 | The `AttributeError` comment stops citing it | **Pass** |
| 18 | The dead `if pid is None` is gone | **Pass** |
| 19 | `serve --help` says it refuses `none` | **Pass** |
| 20 | The `none` comment stops saying "only the first" | **Pass** |
| 21 | The five miscounted or fabricated figures are struck | **Pass** |
| 22 | The half-met row is marked half-met | **Pass** — `~~**[x]**~~ **[~]**` |
| — | Mutations | **Pass** — `--only stranded` **12/12**, `--only closure` **9/9**, `--only reach` **12/12**, `--only console` **8/8**; record cleared, tree clean |
| — | Full suite | **Pass** — 1881 passed, 4 skipped (1885 collected), pytest exit 0 |
| — | Ledger lint | **Pass** — 133 changes |

## The measurement that decides the worst finding

`recover()` on a record with `"owner": "nope"`, over a victim holding the mutated text:

| | before this change | after |
|---|---|---|
| what it said | `restored target.py, which a killed run left mutated` | `REFUSING to act on rec.json: …` |
| the file | overwritten with the record's `original` | left as it was |
| the record | **deleted** | kept |

Before *last* round it raised `ctypes.ArgumentError` and touched nothing. So the sequence is: a
traceback, then a silent overwrite introduced by the guard written to remove the traceback, then a
refusal. The comment shipped with the middle state said it was already the last one.

## Six bounds, each pinned by rows that go red without it

| bound removed | rows red |
|---|---|
| `_PARAGRAPH_BREAK` | **6** (it was 0) |
| the sentence rule | 1 |
| `EXCUSE_WINDOW` | 1 |
| the change id must exist | 8 |
| case-insensitivity | 2 |
| the `_SPAN` backtick bound | 1 |

Thirty-two rows. The previous round's three whitespace rows were replaced because all three were
decided by the sentence bound before the paragraph rule was consulted.

## §7 prohibition re-check

- No skill logic re-implemented; nothing under `skills/` exists here.
- `src/` changes: `cli.py` only — one help string (`serve --assignment-store` now says it refuses
  `none`) and one comment corrected. **No new control-flow path**, no behaviour change.
  `models.py`, `server.py`, `policy.py`, `engine.py` untouched. `GATES`, `PERMANENT_HALT_KINDS`,
  `CLASSES` and the graph untouched.

## Reservations (disclosed, not waived)

1. **Written by the implementer.** Twelve rounds, every verdict `fail`. Eleven of the twelve had
   their largest finding inside the previous round's fix. This round's worst finding was a guard I
   added last round that converted a traceback into data loss, under a comment asserting the
   opposite; three seats found it independently.

2. **The bystander case in `_excused` is open**, bounded by six things, all pinned. A live test
   named inside a run of names that ends in a removal note is excused with them, because a list and
   a bystander are the same sentence to a regex.

3. **The sentence rule is punctuation plus a capital, not a parser.** A bystander clause joined by a
   semicolon, a comma, or a markdown bullet is still inside the window — measured by the defect and
   conformance seats. It is narrower than a paragraph and wider than a sentence, and this is the
   third formulation in three rounds.

4. **`build_factory` still has no handler**, fifth round running.

5. **The command scan reads line starts and backticks only.** An instruction printed mid-sentence
   and unbackticked is invisible. The risk seat enumerated every `runner <word>` in a non-docstring
   literal that is neither: ten hits, all prose, so the gap is currently empty.

6. **Nothing checks that a mutation's `says` describes what its replacement does.** Unchanged from
   last round, and it is the class the `console` inversion belonged to. The `BROKE` outcome added
   here closes a different one.

7. **`isn't done` still reads as finished**, and `_alive`'s coupling to `_owner` is still deferred,
   seven rounds running.

8. **`test_concurrent_config_edits_and_model_adds_do_not_deadlock` is load-sensitive**, seen red
   under concurrent pytest processes in six rounds now. The defect seat measured it: 16 jobs across
   8 threads against a `ThreadingHTTPServer` with the stdlib's default `request_queue_size` of 5,
   failing with `URLError [WinError 10061]` — connection refused, not a deadlock. That is a real
   diagnosis and it is not this change's; worth its own.
