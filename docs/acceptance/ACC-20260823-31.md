# ACC-20260823-31 — Acceptance: a real SPA, and the defect building it found

- Target CHG: CHG-20260823-31 (a resumed conversation whose store has been deleted is reopened under
  the journal marker's existing identity)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** — one narrow recovery path.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass on the code claim, in full.** The evidence for the *narrative* around it — the
  run transcript and the six-condition table — is not in the repository, because the SPA that
  produced it was gitignored.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_conversations.py -q -p no:randomly
git show --stat a541aff
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | A marker whose conversation is gone is reopened, keeping its id | `resume_or_open` `conversations.py:848`, the except branch `:884-894`, ending in `conv.open()` at `:894` | `_open_conversation` `cli.py:577` | `test_conversations.py::test_a_marker_whose_conversation_is_gone_is_reopened` | **Pass** — the test deletes every `*.jsonl` under the store, resumes, and asserts `write_errors == []`, turns `["opened","note"]`, and that the marker's id is kept. Cutting the `conv.open()` at `:894` fails it |

One row, because the change is one fix. The test is unusually good: it asserts the **identity** is
preserved, not merely that no exception was raised — which is the difference between recovering a
conversation and starting a new one that looks like it.

## What is not in the repository

The record's run transcript — 35 nodes, 26 asks, 56 turns — and its six-condition table cannot be
verified. `git show --stat a541aff` shows the commit touched only the fix, its test, the CHG
document, `.gitignore` and the README. **The SPA was gitignored**, so the run that found the defect
left nothing behind.

The six conditions became repo-testable only when CHG-20260823-32 turned them into
`examples/tide-spa` scenarios. That is the right disposition arriving one change late, and it is why
this ACC can confirm the fix completely and the story around it not at all.

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| The reopen path is wired through the live CLI | PASS, `UNCONFIRMED: none` | PASS | **Agreed** |
| The run transcript and six-condition table | not examined | **checked the commit** and found no artefact | **fable's finding stands** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

The historical transcript, which nothing can now reconstruct. Recorded rather than repeated as
though it were evidence.
