# ACC-20260823-45 — Acceptance: the importer, third time

- Target CHG: CHG-20260823-45 (per-conversation error isolation; an inventory that does not use
  glob; same-id-different-content refused; four more write shapes audited; the structural test
  hardened)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **medium** — the importer refuses more.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** This is the change that made CHG-42's and CHG-44's claims true, and every
  behaviour it introduced survives CHG-47's later restructuring of the same code.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_conversations_sqlite.py tests/test_paths.py tests/test_server.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | One bad conversation does not stop the others | per-conversation `except Exception` **inside** the loop, `conversations.py:690-691`, `:738-740` | `import_file_store`, from `cli.py:474` | `::test_one_bad_conversation_does_not_stop_the_others` — **6 parametrised cases**, each placing a good conversation after the bad one | **Pass** — this is CHG-42's claim, made true |
| 2 | A directory named `*.jsonl`, a `.part` file, a third level — each reported, not skipped | `_inventory` `conversations.py:585-604` | `import_file_store` `:663` | `::test_a_third_level_is_reported_rather_than_skipped_in_silence` | **Pass** |
| 3 | No glob anywhere | `paths.listdir` in `_inventory` `conversations.py:564-567` and `FileBackend.conversations` `:295` | — | `::test_the_store_is_never_listed_with_glob` — **syntax-tree** check | **Pass** — the mechanism that made a deep store report as empty is now structurally impossible |
| 4 | A store past MAX_PATH reports what it could not read | `_inventory` | — | `::test_a_store_past_max_path_still_reports_what_it_could_not_read` — builds a 300+-character root **live** | **Pass** — behavioural, on a real filesystem |
| 5 | Same id, different content is **refused**, not skipped; a genuine re-import is skipped | `conversations.py:698-710` via `_for_comparison` `:746-755` | `import_file_store` | `::test_the_same_id_holding_a_different_conversation_is_refused_not_skipped` | **Pass** — both directions, which is what stops the fix from refusing legitimate re-runs |
| 6 | Four more write shapes audited | `test_paths.py:268` (`os.open`, `os.fdopen`, `sqlite3.connect`), `:272` (`tempfile.`); `store.py`'s `sqlite3.connect` allowlisted **with a reason** `test_paths.py:244-247` | the audit | `::test_the_audit_actually_catches_each_shape` | **Pass** |
| 7 | The structural test hardened to name the lock and the branch | `test_server.py:869-885` | — | verified biting by the `fable` engine's sabotage | **Pass** — see ACC-20260823-44, where this test is the only one that catches the window |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Safe inventory, isolation, content comparison, residue reporting | PASS | PASS | **Agreed** |
| Tests executed | **could not rerun** — read-only sandbox had no writable temp dir | **ran them** | **fable carries the executed evidence** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

- The ~23ms-per-conversation O(n²) measurement is historical and was **superseded by
  CHG-20260823-46** anyway, which lists the target once.
- CHG-20260823-47 later restructured this code — `_already_there` no longer exists by that name —
  but every behaviour introduced here survives and its tests pass. Refactoring, not regression.
