# ACC-20260907-22 - a listen backlog of five, and a refutation that measured the wrong condition

- Project: ai-sdlc-runner
- Target CHG: CHG-20260907-22
- Date: 2026-09-07 (UTC+0)
- Reviewed by: **fable**, which found the mechanism while deliberating the backlog queue and
  overturned my own recorded refutation, and **codex**, which had ranked the unexplained failure
  first for a different reason. Re-measured here before anything was written.
- Conclusion: **Pass**

## Criteria

| # | criterion | evidence |
|---|---|---|
| 1 | The mechanism is measured, not inferred | 5 held, 15 refused at size 5 |
| 2 | The fix is measured on the shipped server | 20 held, 0 refused through `serve()` |
| 3 | The test drives the condition, not the load | acceptor never started |
| 4 | Both ways of losing the value are pinned | `--only backlog`: **2 of 2 CAUGHT** |
| 5 | Each mutation is caught by its own test | node ids, not the file |
| 6 | The false sentence is replaced, not deleted | ACC-20260907-20 |
| 7 | The README count follows the suite | `--collect-only` |
| 8 | The suite is green | see below |

## Evidence

### The refutation I recorded was of the wrong condition

```
acceptor draining (my probe in CHG-20260907-20):
    64 concurrent connections            0 refused      <- a queue being drained never fills

acceptor not draining (the sighting's condition):
    20 connections, request_queue_size=5   5 held, 15 refused, all WinError 10061
    20 connections, request_queue_size=128 20 held,  0 refused
```

`10061` is *connection refused*, which is also what a client gets from a port with nothing
listening. The server was listening.

### On the shipped path

```
server.serve(..., port=0).request_queue_size            128
20 connections, acceptor never started                  20 held, 0 refused
```

### The mutations

```
CAUGHT  the listen backlog may go back to five again
CAUGHT  the backlog may be inherited by accident again
```

The second exists because a value that reads as deliberate and a value inherited by accident are
the same defect, and only one of them looks like one. Both name
`tests/test_server.py::test_a_burst_of_connections_is_queued_rather_than_refused` rather than the
file: a bare edit to this class trips other tests first, and a `CAUGHT` earned by collateral says
nothing about the guarantee - a lesson from CHG-20260907-21, applied at the start here rather
than after review.

### The suite

```
--only backlog     2 of 2 CAUGHT
pytest -q          2329 passed, 4 skipped in 777s
ledger             passed
```

## What this does not do

It does not explain why that particular run and not the twenty before it. The mechanism is
deterministic; the trigger is inference from a machine under concurrent load. It does not touch
`tests/test_store.py` - the test was right and the server was not - and it does not raise the
backlog to the OS maximum, because 128 is what `http.server` itself uses and nothing measured
here needs more.

## Verdict

**Pass.** A server that answered its sixth caller by saying nothing was there now queues it, and
a merged record that reported a correct explanation as refuted says what the second measurement
found instead.
