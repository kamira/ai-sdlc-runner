# ACC-20260823-23 — Acceptance: managing models — the rules, not just the shape

- Target CHG: CHG-20260823-23 (`docs/MODELS.md`: the fourteen refusals, reach classification, pool
  reproducibility, and a model that registers but cannot be dispatched to)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** — no behaviour changes.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** Every refusal the page documents was **driven and fired**, and the page is
  held to the code in both directions: a refusal in the code with no entry on the page fails a test.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_models_schema.py tests/test_models.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | Fourteen refusals, each documented and each real | `models.validate` `models.py:142`; `_SECRET_KEYS` in query strings `:56`, `:74` | `Registry.add` `models.py:227`; `load` `:293`; POST `/models` in `server.do_POST` | `test_models_schema.py::test_every_refusal_the_page_documents_actually_fires` | **Pass** |
| 2 | …and the page documents a refusal for every `raise` | same | — | `::test_the_page_documents_a_refusal_for_every_raise_in_validate` | **Pass** — the other direction, which is what stops the page going stale as the code grows |
| 3 | Reach is classified, and unresolvable means external | `reach_of` `models.py:79`; `Model.reach` `:127` | the console's GET `/models` sends the **computed** reach (`test_api_schema.py:151`) | exercised across all three reaches | **Pass** — computed, never stored, so it cannot drift from the address |
| 4 | A pool choice is random **and** reproducible | `random.Random(f"{dispatch_seed}:{node.id}:{nth_ask}")` `engine.py:692` | POOL mode dispatch `engine.py:1503` | `::test_a_pool_dispatch_is_reproducible_and_depends_on_all_three_seeds` | **Pass** — all three seed components tested, not just the fact of a seed |
| 5 | An `api` model registers and cannot be dispatched to | `session_factory` raises `cli.py:270-276` | both factory construction sites, `cli.py:699` and `:910` | `::test_an_api_model_registers_and_cannot_be_dispatched_to` | **Pass** |
| 6 | The page is held against `graph.MODES` | `graph.py:68` | — | `test_models_schema.py:160` | **Pass** |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Registry validation, reach, seat routing, panel/pool dispatch, reproducible selection | PASS | PASS, 55 tests green | **Agreed** |
| Tests executed | **could not run** — read-only sandbox had no writable temp dir | **ran them** | **fable carries the executed evidence** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

- The page's own admission that **the assignment side is not closed** still holds, and is still
  tested (`::test_the_page_admits_the_assignment_side_is_not_closed`). A document that tests its own
  disclaimer is an unusual and good idea.
- The plan-file openness it names remains an open finding from CHG-20260823-21, as the CHG states.
- Drift: 55 tests collected where the record said 52.
