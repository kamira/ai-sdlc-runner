# ACC-20260617-04 — Skill update detection

- Project: ai-sdlc-runner
- Date: 2026-06-18
- Target: CHG-20260617-04 (detect updates at the local skill location)
- Verifier: V1 (independent acceptance; ≠ implementer I1)
- Risk: low (inherited from CHG-20260617-04)
- Baseline source: CHG-20260617-04 modification guide; build-guide §5 version semantics; live ai-skills (tags ai-sdlc-v1.0.0 / ai-sdlc-v1.1.0)
- Conclusion: **Pass**

## Check details
| # | Acceptance criterion | Source | Result | Evidence |
|---|----------------------|--------|--------|----------|
| 1 | Detect local skill newer than project lock; classify minor → migrate | CHG-04; §5 | Pass | `check <proj>` (lock 1.0.0, local 1.1.0) → `minor — MINOR update available (1.0.0 → 1.1.0); run migrate`, exit 20; `test_detect_update_uses_project_lock` |
| 2 | Patch passes freely (no migrate) | §5 | Pass | `detect_update(local 1.0.7, base 1.0.0)` → `patch`, `needs_migrate False`; `test_detect_update_kinds[patch]` |
| 3 | Major → migrate; up_to_date; older handled | §5 | Pass | parametrized `test_detect_update_kinds` covers major/up_to_date/older |
| 4 | Baseline precedence: project lock over expected | CHG-04 | Pass | `test_detect_update_uses_project_lock` (lock 1.0.0 beats expected 9.9.9) |
| 5 | No baseline → unknown (no crash) | CHG-04 | Pass | `test_detect_update_unknown_without_baseline` |
| 6 | Newer git version tag detected (ai-sdlc-vX.Y.Z) | CHG-04 | Pass | `available_version_tags` returns `['1.1.0','1.0.0']`; `check` prints `newest tag: v1.1.0`; `test_available_version_tags_parses_skill_scoped` |
| 7 | Surfaced in CLI / status / dashboard / menu | CHG-04 | Pass | `runner check`; `status` shows `skill: local 1.1.0 vs baseline 1.0.0 → minor (run migrate)`; dashboard Status panel `skill: local 1.1.0 vs 1.0.0 → minor ⚠ migrate`; menu "Check skill updates" |
| 8 | Read-only — never auto-migrates | CHG-04 risk; §5 | Pass | `check` leaves `.sdlc-lock.json` byte-identical (verified); migrate remains explicit |
| 9 | v1.1.0 backward compatible with the runner | regression | Pass | `parse_role_table` on v1.1.0 → `[A1,I1,I1.x,V1]`; V1 excludes Agent; targets the "Role startup spec" header, not the new role-catalog table |
| 10 | Zero new dependencies | NFR | Pass | `contract.py` external imports = NONE (stdlib only; added `subprocess`) |
| 11 | Tests cover new logic; full suite green | §6 | Pass | `tests/test_contract.py` (+detect_update kinds, lock precedence, unknown, tag parse); full suite **50 passed** |
| 12 | Structure docs synced + .gitmodules tag name | §1.2 | Pass | logical.md/design.md document `detect_update`/`available_version_tags`; `.gitmodules` updated to `ai-sdlc-v1.0.0` (notes v1.1.0) |

## §7 prohibition re-check (unchanged)
- No skill markdown copied; halt matrix/role table still read from the skill (detection only reads versions/tags). Pass.
- V1 tool lockdown, red-line halts: untouched. Pass.

## Unmet items & handling
None.

## Structure consistency check
- [x] Implementation matches updated structure docs (`contract.detect_update`/`available_version_tags`; `cli check`; dashboard skill line).

## Summary
Deliverable. `runner check` (and the status/dashboard lines) detect when the local skill location is
newer than a project's lock/expected, classify patch (auto) vs minor/major (→ migrate), and report
newer version tags — all read-only, never auto-migrating. The runner verified backward-compatible with
the provided v1.1.0 skill (role-responsibility catalog added without breaking the role-table parse).
CHG-20260617-04 set to **Accepted**.
