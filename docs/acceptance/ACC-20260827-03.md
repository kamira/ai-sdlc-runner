# ACC-20260827-03 - Acceptance: a test shadowed by a later definition of the same name

- Target CHG: CHG-20260827-03
- Verifier: **self** (the session that built it). Permitted at this risk grade, and disclosed rather
  than dressed up: every change in this batch is low, and none touches the red lines, the gate
  matrix or the flow. The independent work that produced these changes was the acceptance round of
  2026-08-27 - two engines that had built none of the code they read. What follows is the
  implementer checking their own fix, which is worth exactly what it is worth.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** - no source change
- Conclusion: **Pass. The lost case runs again, and the recurrence has a guard with its own proof of failure.**

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_conversations_sqlite.py -q -p no:randomly \
    --collect-only -k "one_bad_conversation or dirty_legacy_store"
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_nothing_is_unwired.py -q -p no:randomly
```

Batch-wide: `pytest tests/ -q -rs` -> **1347 passed, 3 skipped**; `tools/mutation_check.py` -> **15 of 15 caught**;
`tools/ledger_check.py --repo .` -> passed, 86 changes.

## Check-details

| # | Check | Method | Result |
|---|---|---|---|
| 1 | The shadowed test is collected again | `--collect-only` on the two names | **Pass** - **7** items where 6 ran before |
| 2 | It is renamed, not deleted | read it | **Pass** - it drives `_dirty_legacy`, which is not one of the `DIRT` shapes, so the case is coverage the parametrised test does not have |
| 3 | Both tests pass | ran them | **Pass** - 7 passed |
| 4 | No module defines a test name twice | `test_no_test_module_defines_the_same_test_name_twice` over every `test_*.py` | **Pass** |
| 5 | The guard can fail | `test_the_duplicate_name_check_reads_definitions_rather_than_text` | **Pass** - a planted redefinition is seen |
| 6 | The guard does not cry wolf | same test | **Pass** - a name in a comment or a string is not a definition, which matters because this file's own docstrings quote the shadowed name |

## Not closed by this record

Nothing. Worth noting what made this survive: the replacement was **stronger**, so the suite stayed
green and the count stayed plausible. A shadowed test hides behind a better one.
