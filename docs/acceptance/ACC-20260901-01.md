# ACC-20260901-01 — Acceptance: absence is cannot-tell

- Target CHG: CHG-20260901-01
- Verifier: self (main agent = implementer)
- Reviewed by: a four-seat panel over `934cef1..ade3fe3` — `conformance`, `defect`, `risk`, `idiom`
  — each in its own git worktree, none seeing another's answer. The panel is the **input** to this
  change, not its verification; see reservation 1.
- Date: 2026-09-01 (UTC+0)
- Risk: medium — the harness's recovery, the ledger's excusal rule, the mutation harness
- Conclusion: **Pass on the corrections.** The panel's verdict was **fail** from all four seats,
  with seven vetoes; this change is what those verdicts asked for.

## Check-details

| # | Task | Result |
|---|------|--------|
| 1 | A record with no `owner` key is refused | **Pass** — it was overwritten and unlinked; reverting is `1 failed` |
| 2 | The gate reads the four fields it uses | **Pass** — a record with two spare keys recovers |
| 3 | The refusal names what is actually wrong | **Pass** |
| 4 | A path that is gone gets its own refusal | **Pass** — reverting is `1 failed` |
| 5 | `BROKE` asks the module, not the summary | **Pass** — reverting is `1 failed` |
| 6 | It imports by package path | **Pass** — `--only reach` is 12/12; it was 4 BROKE |
| 7 | A fixture that raises is still a catch | **Pass** — the summary guess is gone |
| 8 | The summary names five outcomes | **Pass** |
| 9 | A sentence end is not only a capital | **Pass** — 6 new rows; reverting the rule is red |
| 10 | The rename phrase survives a wrap | **Pass** |
| 11 | The case flag is scoped to the verbs | **Pass** — reverting is `1 failed` |
| 12 | The UTF-8 refusal is pinned | **Pass** — reverting is `1 failed`; it was `54 passed` |
| 13 | The five recovery guards are pinned | **Pass** — `1`, `1`, `1`, `1`, `5 failed` |
| 14 | The constants block describes the code | **Pass** |
| 15 | The literal tab is gone, again | **Pass** — 0 lines with a tab in either file |
| 16 | `found_start` is not a name for an offset | **Pass** |
| 17 | The seven vetoed figures are corrected | **Pass** |
| 18 | The three unstruck figures are struck | **Pass** |
| 19 | Case-insensitivity is not counted as a bound | **Pass** |
| 20 | The comment counts the places that exist | **Pass** — five, named |
| 21 | The ASCII dash is an em dash | **Pass** — 0 spaced hyphens in comments |
| — | Mutations | **Pass** — `--only stranded` **12/12**, `--only closure` **9/9**, `--only reach` **12/12**, `--only console` **8/8**; record cleared, tree clean |
| — | Full suite | **Pass** — 1898 passed, 4 skipped (1902 collected), pytest exit 0 |
| — | Ledger lint | **Pass** — 134 changes |

## The measurement that decides the headline

`recover()` on a record with the `owner` line deleted, over a mutated victim:

| | before | after |
|---|---|---|
| what it said | `restored target.py, which a killed run left mutated` | `REFUSING to act on …` |
| the file | overwritten | left as it was |
| the record | **deleted** | kept |

The gate that was supposed to stop this iterated `record.items()`. A dictionary cannot report a key
that is not there, so the one repair the refusal invites — delete the line rather than mistype it —
was the one that walked past it.

## Five bounds and one widening

| removed | rows red |
|---|---|
| `_PARAGRAPH_BREAK` | 6 |
| the sentence rule | 7 |
| `EXCUSE_WINDOW` | 1 |
| the change id must exist | 1 |
| the `_SPAN` backtick bound | 1 |
| *(case-insensitivity — a widening; its rows assert acceptance)* | 2 |

The previous acceptance called this six bounds and gave `8` for the id. Both are corrected; the id
bound is one row, and case-insensitivity is not a bound at all.

## §7 prohibition re-check

- No skill logic re-implemented; nothing under `skills/` exists here.
- `src/` changes: `cli.py` only, one comment corrected. **No behaviour change and no new
  control-flow path.** `models.py`, `server.py`, `policy.py`, `engine.py`, `store.py` untouched.
  `GATES`, `PERMANENT_HALT_KINDS`, `CLASSES` and the graph untouched.
- Non-`src/` hunks, enumerated because the last round shipped four that no task row covered:
  `README.md`'s test count (task —), `tools/ledger_check.py`, `tools/mutation_check.py`,
  `tools/mutation_recovery.py`, three test files, and the seven vetoed records.

## Reservations (disclosed, not waived)

1. **Written by the implementer.** Thirteen rounds, every verdict `fail`. Twelve of the thirteen had
   their largest finding inside the previous round's fix. This round's headline was a hole in the
   gate written last round to close the previous headline.

2. **The bystander case in `_excused` is open.** A live test named inside a run of names ending in a
   removal note is excused with them. Bounded by five things, all pinned.

3. **The sentence rule is punctuation plus a not-a-dash, not a parser.** A bystander clause joined
   by a semicolon, a comma, a colon, an em dash or a markdown bullet is still inside the window —
   measured by three seats across two rounds. This is the fourth formulation.

4. **`build_factory` still has no handler**, sixth round running.

5. **Nothing checks that a mutation's `says` describes what its replacement does.** The `stranded`
   re-anchor this round's predecessor shipped undisclosed walked through exactly that gap.

6. **The command scan reads line starts and backticks only.** Two seats enumerated the gap
   independently: ten `runner <word>` hits in non-docstring literals that are neither, all prose.

7. **`isn't done` still reads as finished**, and `_alive`'s coupling to `_owner` is still deferred,
   eight rounds running.

8. **The 3.9 floor is not exercised locally.** This change shipped a test whose `finally` used
   `Path.write_text(newline=…)`, which is 3.10+; it raised **inside the restore**, so the mutated
   file stayed on disk for the rest of that pytest process. Green here, red on both 3.9 CI jobs.
   Nothing in the local loop runs 3.9, and a restore that can itself fail is the shape this whole
   module exists to prevent — corrected before merge, and worth its own change.

9. **`test_concurrent_config_edits_and_model_adds_do_not_deadlock` is load-sensitive**, seven rounds
   now, with the defect seat's diagnosis on file: 16 jobs across 8 threads against a
   `ThreadingHTTPServer` whose `request_queue_size` is the stdlib default of 5.
