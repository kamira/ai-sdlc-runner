# ACC-20260823-10 — Acceptance: a name is evidence only if it constrains what happens

- Target CHG: CHG-20260823-10 (an executor can never be `ordinary` however it is vouched; the module
  form survives; a vouch covers the tool, not the command line)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions, no sight of each other's findings.
- Date: 2026-08-27 (UTC+0)
- Risk: **high** — `halt_independent`, the permanent halts.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** The rule is enforced at the recogniser, refused again at settings load, and
  both directions of the verb/flag fix are pinned. 205 tests across the files it rests on pass.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_targets.py tests/test_documented_numbers.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | An executor is never ordinary, however it is vouched | `EXECUTORS` `policy.py:295`; `recognise` `policy.py:400-404` returns `unrecognised` regardless of vouching | reached from the walk via `engine.py:962` / `:971`, then `policy.unverified` `policy.py:499` | `test_targets.py::test_an_executor_is_never_ordinary_however_it_is_vouched` — ten executor commands, each vouched by its own name | **Pass** |
| 2 | Vouching for an executor is refused at settings load, with the alternative named | `settings.load` `settings.py:168-178` raises `SettingsError` naming the `python -m <tool>` form | `cli.py:627` | `test_targets.py::test_settings_refuses_to_vouch_for_an_executor` | **Pass** — the refusal tells the operator what to do instead, which is why it is usable |
| 3 | The module form keeps the common case working | `_MODULE_FORM` `policy.py:310`; `recognise` `policy.py:393-397` — ordinary only if the module is vouched **and** is not itself an executor (`os`, `http` are in `EXECUTORS` at `policy.py:302-305`) | `recognise` | `test_targets.py::test_the_module_form_keeps_the_common_case_working` | **Pass** — `python -m pytest` still works; `python -m os` does not |
| 4 | A vouch covers the tool, not the command line | `_suspect` `policy.py:340` — `_DANGEROUS_FORMS` regexes plus bare words in positions 1-3 only, flags stripped | `recognise` `policy.py:396`, `:406` | `::test_vouching_for_a_command_does_not_vouch_for_its_arguments` | **Pass** |
| 5 | …and a benign flag is not a destructive verb | same positional guard | `recognise` | `::test_a_benign_flag_is_not_a_destructive_verb` | **Pass** — both directions of the fix are pinned, which is what stops it swinging back |
| 6 | Vouching a real toolchain does not cause false stops | the same recogniser | — | `::test_the_false_stop_rate_with_a_vouched_toolchain_is_zero` | **Pass** |
| 7 | The numbers check scans source and knowledge, per category | `test_documented_numbers.py:48-52` (knowledge.md, policy.py, graph.py, engine.py, settings.py) | the suite | `::test_each_category_is_still_stated_somewhere` `:204`, parametrised per named category; seat floor asserted numerically `:94` | **Pass** |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Executor ban wired into recognition | PASS | PASS | **Agreed** |
| Module form preserved | PASS | PASS, and named the executor-module exclusion | **Agreed** |
| Positional argument guard | PASS | PASS, both directions | **Agreed** |
| Overall | PASS — one reservation: the executor list is necessarily finite, which the CHG itself does not claim otherwise | PASS — one reservation: the "0 of 16 / 0 of 20" build-time tallies were not re-counted | **PASS** |

## Not closed by this record

- **The executor list is finite and always will be.** Both the CHG and the codex engine say so. What
  the design buys is that an unrecognised command is `unrecognised`, not `safe` — see KN-15.
- The CHG's Evidence section states **0 of 16 attack commands** and **0 of 20 bounded commands** as
  build-time measurements. The current corpora in `test_targets.py` cover the same commands and pass,
  but neither engine re-tallied exactly 16 and 20. Recorded as unconfirmed rather than repeated.
