# ACC-20260906-06 - the surface that was only ever a test fixture

- Project: ai-sdlc-runner
- Target CHG: CHG-20260906-06
- Date: 2026-09-06 (UTC+0)
- Reviewed by: **fable** (found it, correcting my brief) and **codex** (independent verification,
  asked to falsify it). Both engines agree. codex returned **GREEN** on five of the six sub-claims
  and **PARTIAL** on the sixth, and the partial is the useful one — see below.
- Conclusion: **Pass**

## Criteria

| # | criterion | evidence |
|---|---|---|
| 1 | The flag has never existed | `git log -S` over all branches: **0 commits** |
| 2 | It is declared nowhere today | `grep '"--json"' --include=*.py .`: **0 hits** |
| 3 | The false claims are corrected | 8 sites; only the note explaining the absence still names it |
| 4 | The corrected reason is checkable | asked of `cli.build_parser()`, never of the source text |
| 5 | The guard goes red when reverted | named the offender: `--json (the reason given for risk_settled)` |
| 6 | It is not hard-coded to one field | second mutation on a different entry: CAUGHT |
| 7 | The suite is green | `2234 passed, 4 skipped` in 438s |
| 8 | The README count follows the suite | `--collect-only` -> **2238**, README updated from 2237 |

## Evidence

### Where the two engines differed, and what that produced

`fable` reported the claim. `codex` was asked to falsify it and returned:

> the three comments mistake an unused `RunReport.as_dict()` representation for an output surface.
> That mistake understates the defect because the fields did not reach the HTTP snapshot or console.

That is the half I had not measured. My own finding stopped at *"the flag does not exist"*; codex
supplied *"and here is what people meant by it, and it has no caller"*:

```
report.as_dict() call sites in src/   ->   0
```

### The PARTIAL, which is the most useful line in the verification

codex refused to call `cli.py:1154` wholly false:

> "No terminal line": **historically true before CHG-20260903-48**.

The comment makes two claims. One is false and one is true, and the true one is what made the false
one survive review. That distinction is why the repair is a re-wording that *keeps* the true half,
rather than a deletion.

### One I withdrew

The same scan flagged `--store-remote`, cited in five source docstrings as the canonical example of
a runner-recorded relaxation. Re-measured before proposing:

```
git log -S'"--store-remote"' --all -- src/
  52017b4 refactor: remove the mongo and tinydb backends (CHG-20260823-35)
  2f7973b feat: every conversation stored by project, and exportable (CHG-20260823-17)
```

**It was real.** Added, then removed with the backend that needed it — and
`test_the_locality_flags_went_with_the_backend_that_needed_them` already pins its absence *from the
parser*, with a docstring recording that the first version of that test grepped the source and
failed on the comment explaining the removal. The repository had already handled this correctly.
The finding did not survive re-measurement and is withdrawn; what remains is that a dead example
was standing in for a live mechanism, so the entry now names `--undeclared allow`, measured:

```
undeclared=allow    halted_at=done            relaxations=13
undeclared=refuse   halted_at=intake_review   relaxations=0
```

### Why the guard is narrow

A repository-wide lint was written and thrown away before it reached the branch:

```
argparse declares                  35 long flags
named in prose but not declared   131
```

`--ink`, `--accent`, `--surface-2` are the console's own CSS custom properties; `--porcelain` and
`--grep` are git's; `--collect-only` is pytest's. **131 false positives to 1 true one.** That ratio
is the answer to "why does this class of defect keep recurring in this repository" — it is not
catchable by reading prose, so the check goes where the set is closed and the strings exist
precisely to be argued with.

## Verdict

**Pass.** A name that had never referred to anything was used as evidence in eight places, twice by
me today; each now says what is true, and the one inventory whose entries exist to be checked is
checked.
