# ACC-20260827-05 - Acceptance: millisecond timestamps are asserted by nothing

- Target CHG: CHG-20260827-05
- Verifier: **self** (the session that built it). Permitted at this risk grade, and disclosed rather
  than dressed up: every change in this batch is low, and none touches the red lines, the gate
  matrix or the flow. The independent work that produced these changes was the acceptance round of
  2026-08-27 - two engines that had built none of the code they read. What follows is the
  implementer checking their own fix, which is worth exactly what it is worth.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** - tests only
- Conclusion: **Pass. Both directions are now deliberate, and the forward one is pinned re-runnably.**

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_conversations.py -q -p no:randomly
PYTHONUTF8=1 PYTHONPATH=src python tools/mutation_check.py --only clock
```

Batch-wide: `pytest tests/ -q -rs` -> **1347 passed, 3 skipped**; `tools/mutation_check.py` -> **15 of 15 caught**;
`tools/ledger_check.py --repo .` -> passed, 86 changes.

## Check-details

| # | Check | Method | Result |
|---|---|---|---|
| 1 | A turn written now carries milliseconds | `test_a_turn_is_stamped_to_the_millisecond` - asserts a three-digit fraction on every turn | **Pass** |
| 2 | Reverting the clock is caught | mutation to `timespec="seconds"` | **Pass** - CAUGHT, 1 failed / 68 passed |
| 3 | A second-resolution stamp still loads | `test_a_turn_stored_at_second_resolution_still_loads` | **Pass** - parses to the moment it spells, and survives into the export |
| 4 | The backward direction is deliberate | read the test | **Pass** - it no longer depends on a test about duplicate `seq` happening to use an old stamp |

## Not closed by this record

Nothing. The record's claim that the resolution was "asserted both ways" is now true; it was not
when written, and `git show --stat 2661659` is the evidence - that commit touched only
`tests/test_paths.py` under `tests/`.
