# ACC-20260901-04 — Acceptance: two fixes that were worse than the defect

- Target CHG: CHG-20260901-04
- Verifier: self (main agent = implementer)
- Reviewed by: a four-seat panel over `2afec9d..3a49c57` — `conformance`, `defect`, `risk`, `idiom`
  — each in its own git worktree, none seeing another's answer. All four reported; the conformance
  seat returned last again, with six vetoes, and this record answers them. The panel is the
  **input** to this change, not its verification; see reservation 1.
- Date: 2026-09-01 (UTC+0)
- Risk: medium — the mutation harness's import probe, its recovery, the ledger's excusal rule
- Conclusion: **Pass on the corrections.** All four seats returned **fail**; this change is what
  those verdicts asked for.

## Check-details

| # | Task | Result |
|---|------|--------|
| 1 | The import probe cannot be answered by a stale `.pyc` | **Pass** — fresh `PYTHONPYCACHEPREFIX`; the planted rewrite is `rc=1` where `python` and `python -B` are both `rc=0` |
| 2 | That claim is pinned by a test | **Pass** — `1 failed` under `-B`, `1 failed` with no flag, against the shipped `_import_fails` |
| 3 | No remedy says delete before restore | ~~**Pass** — reverting the wording is `6 failed`~~ **Fail — it was `0 failed`.** The guard asserts `index("delete") > index("back by hand")`, and the condemned wording contains *"putting the `original` **back by hand** may overwrite a live run's source"* — a warning **against** restoring — 107 characters before it says delete, so it satisfied the guard it was written to fail. Nothing produces 6. Genuinely pinned at **`7 failed`** by CHG-20260901-05, which asserts the restore is conditioned (VETO) |
| 4 | The gate reports every fault it decided | **Pass** — re-gating the empty-`path` clause is `2 failed` |
| 5 | One message names fields in one order | ~~**Pass**~~ **Pass on the code, fail on the reason. Not pinned**: reverting `sorted(READS)` to `list(READS)` leaves every row green. The shipped code named them in one order already; the divergence arrived with this change's own `broken = sorted(...)`. Corrected in CHG-20260901-05 (idiom and conformance seats) |
| 6 | The remedy stops claiming intact fields | **Pass** — no branch asserts anything about fields it did not read. **Not pinned**: restoring the *"the other fields were read and are intact"* arm leaves every row green, and for `{"path": ""}` that sentence is emitted about a `path` of `""` — verbatim the defect this row closes, with nothing red. Disclosed in CHG-20260901-05 (VETO) |
| 7 | A rename excuses only *this* test's rename | **Pass on the sentences measured, and they were the wrong ones.** Unscoping is `1 failed` and both sentences the seat gave are refused. But they carry **no removal note**, and a real excuse sentence normally does: with a bystander removal in the same sentence the open bystander case excuses the ghost regardless, at `3a49c57` and here alike. The bound holds only in the absence of that case. Corrected in CHG-20260901-05 (risk seat) |
| 8 | The rename gate uses the whitespace the rule allows | **Pass** — the two-space and line-wrapped rows |
| 9 | The removal phrase is built once | **Pass on the code, fail on the history.** The widening is `2 failed` from one edit. "It was `0` from one of two copies" is true of a state **this change** created and removed within itself — `3a49c57` has one copy. Corrected in CHG-20260901-05 (VETO) |
| 10 | The census names a commit, not a count | **Pass** — 271 at `3a49c57`, by `git ls-tree` |
| 11 | The census *claim* is re-measured every run | **Pass on the claim, fail on the pin.** The test this shipped — `test_the_capital_rule_would_still_miss_about_half`, **removed in CHG-20260901-06** — was a corpus assertion and not a regression pin: reverting `_SENTENCE_END` to `[.!?]\s+(?=[A-Z])` left it green while six *other* `GHOST_EXCUSES` rows went red, and its only red was editing its own threshold. Corrected in CHG-20260901-05 (defect seat), and the mechanical rename that then named the **wrong** replacement here — attaching "leaves it green" to a test that is `5 failed` under that revert — corrected in CHG-20260901-06 (VETO) |
| 12 | `_excused`'s docstring says seven bounds | **Pass** — seven, with each figure's patch |
| 13 | The id bound's figure is the id bound | **Pass** — that conjunct alone is `1`; `if (True or …` is `8` and disables three |
| 14 | The "loosened" row is withdrawn | **Pass** — eight widenings measured: five give `0`, three give `24`, none give `1` |
| 15 | `WORD_NAME` is gone | **Pass** — **not pinned**: it had no readers, so no row moves |
| 16 | `known` is required | **Pass** — **not pinned**: the default was unreachable, so no row moves |
| 17 | The `N passed` transcript is deleted, not re-figured | **Pass** — the cell now reads `1 failed, N passed`. Measured here as `1 failed, 121 passed`, which is a fourth value for the same cell in four rounds and is exactly why it is not published as one |
| 18 | §7 of ACC-20260901-03 names what was touched | **Pass** — `git diff --name-only 2afec9d..3a49c57` is 14 paths; two named records are not among them |
| 19 | The `113` table-row figure is 110 | **Pass** — 110 records / 374 rows at `2afec9d` and `3a49c57`, under all four readings. ~~stable across four readings and two trees~~ — on the tree carrying this record it is 111 / 376, as reservation 4 now says; the correction reached the reservation and not this row (CHG-20260901-06, idiom seat) |
| 20 | The rounds series ends at this round's total | **Pass** — 46 |
| 21 | The uncovered hunks are covered | **Pass** — rows 19–21 added to CHG-20260901-03 |
| — | Mutations | **Pass** — `--only closure` **9/9**, `--only codecs` **9/9**, `--only stranded` **12/12**, `--only reach` **12/12**, `--only console` **8/8**; every group exit 0, record cleared, `git status --short` unchanged after each |
| — | Full suite | **Pass** — **1912 passed, 4 skipped** (1916 collected), pytest exit 0, 11m01s. The first run was `1 failed, 1911 passed`: `test_documented_numbers.py` caught the README still saying 1906 against a suite of 1916 — the test that exists for exactly that, doing exactly that |
| — | Ledger lint | **Pass** — **137** changes, which is 136 plus this change's own CHG. Written from the lint's output rather than from arithmetic, because five of the last six rounds got a count of their own files wrong |

## The bounds, each with the patch that produces it

Measured today, on this tree, one patch per row. The last three rounds each published a figure here
that was produced by a different edit than the one named, so the edit is the column that matters.

| bound | patch | rows red |
|---|---|---|
| `_PARAGRAPH_BREAK` | `re.compile("(?!x)x")` | 6 |
| the sentence rule | `if False and _SENTENCE_END.search(said):` | 7 |
| `_SPAN`'s backtick bound | `_SPAN = r"(?:.\|\n)*?"` | 2 |
| `EXCUSE_WINDOW` | `len(said) <= EXCUSE_WINDOW` → `True` | 1 |
| the change id must exist | `names in ledger_ids` → `True`, **that conjunct alone** | 1 |
| the name matched case-sensitively | `re.compile(pattern, re.IGNORECASE)` | 1 |
| a rename is this test's, and resolves | `if False and renamed:` | 6 |
| — the scoping half alone | `mine = re.search(phrase, said)` | 1 |
| — the must-resolve half alone | drop `mine.group(1) not in known` | 4 |
| *(case-insensitivity of the verbs — a widening)* | drop `(?i:` from `_removed_by` | 2 |
| *(not a bound)* | `if (True or names in ledger_ids and …` | 8 = 1 + 1 + 6 |

The last row is why three rounds disagreed about one number. `and` binds tighter than `or`, so that
edit disables the id bound, `EXCUSE_WINDOW` and `_PARAGRAPH_BREAK` together, and 1 + 1 + 6 is the
`8` that was published as the id bound's own figure.

## The measurement that decides the headline

`_import_fails` against a module rewritten to the same size inside the same second, `os.utime` back:

| probe | result |
|---|---|
| `python -c` | `rc=0` — the stale `.pyc` is trusted |
| `python -B -c` — last round's fix | `rc=0` — the stale `.pyc` is trusted |
| `PYTHONPYCACHEPREFIX` into an empty dir | `rc=1` — refuses |

`-B` stops bytecode being *written*. Nothing about it stops one being *read*. The comment claiming
otherwise stood in the source for a round, and in ~~three records~~ **two: CHG-20260901-03, in
three places, and ACC-20260901-03's row 7.** Three *places*, two *records* (CHG-20260901-05).

## The measurement that decides the second one

The remedy for an unusable `owner`, followed literally in a scratch repo with the mutation staged:

| | |
|---|---|
| `git status` | `M  x.py` |
| `git diff` | *(empty — the change is staged)* |
| the file | `MUTATED SOURCE` |
| the record | deleted |

Neither command the remedy names shows the original: `git diff` is silent on a staged change, and
`git status` reports that something moved, not what it held. The record that held it verbatim was
deleted on the same instruction — twelve lines above the rule that instruction breaks. Where the
file's pre-run state was uncommitted, following the remedy is the end of the text.

## §7 prohibition re-check

- No skill logic re-implemented; nothing under `skills/` exists here.
- `src/` changes: **none.** No file under `src/` is touched.
- Non-`src/` files **and the hunk count**, both taken from `git`, because naming files from memory
  is what the last §7 got wrong — it listed two records the change never opened.
  `git diff 3a49c57..a845e7c --diff-filter=M | grep -c '^@@'` reports **29 hunks** across 13
  modified files; with the 2 new records it is **31** across 15. (The command first written here
  read `--cached`, which returns 0 once the change is committed — right number, unreproducible
  command; corrected in CHG-20260901-05.)

  `README.md` · `tools/ledger_check.py` · `tools/mutation_check.py` · `tools/mutation_recovery.py` ·
  `tests/test_ledger_check.py` · `tests/test_mutation_check.py` · `tests/test_mutation_recovery.py` ·
  and the records `ACC-20260831-06`, `ACC-20260901-01`, `ACC-20260901-02`, `ACC-20260901-03`,
  `CHG-20260901-02`, `CHG-20260901-03`, plus this pair.

  Every hunk is claimed by a task row, and the mapping is the check: `git diff --name-only` for the
  files, `git diff | grep -c '^@@'` for the hunks. The last four rounds each shipped hunks no row
  covered, so the count is stated rather than implied.

## Reservations (disclosed, not waived)

1. **Written by the implementer.** Sixteen rounds, every verdict `fail`. Fifteen of the sixteen had
   their largest finding inside the previous round's fix. This round's two largest are both fixes
   from the previous round that were worse than what they replaced: one asserted a closure it did
   not make, the other told the operator to destroy the file it exists to protect.

2. **Six of this change's twenty-two tasks are not pinned by a red row**, and the count has now been
   wrong twice — three times counting the denominator, which stayed at twenty-one when
   CHG-20260901-05 added row 22 to the table above (corrected in CHG-20260901-06). That row also
   has no matching check-details row above; it is covered by ACC-20260901-05's row 25. Rows 3, 5, 6, 11, 15 and 16 each say so in the row itself; the sixth item — the
   diagnosis assertion widened to read every broken field — is disclosed here rather than in a row,
   because row 4 reports the code revert (`2 failed`) and the assertion measures `2 failed` either
   way. Tasks 3, 5 and 6 were measured green against their own reverts by the conformance seat and
   were not disclosed at all; task 3's published `6 failed` was `0`. "Three, and each says so in its
   acceptance row" was wrong in both halves (CHG-20260901-05, VETO).
   Removing `known`'s default and deleting `WORD_NAME` change no behaviour by construction — that
   is the argument for both. Widening the diagnosis assertion to read every broken field measures
   `2 failed` either way, because the rows happen to list the unnamed field first; the widening
   removes a dependence on dict order, not a live hole.

3. **Corrections remain the dominant defect class, and the census is the one that is now fixed
   rather than corrected.** **Five statements** of it excluded the recording record's own files, of
   which **four** were corrections of the one before — the unit was "rounds", "instances",
   "corrections" and "statements" in four places giving two different numbers, in the section
   arguing that a figure must be checkable (corrected in CHG-20260901-05, idiom seat). The cause was structural: "the current tree" is not a
   quantity a record can measure about itself. Naming a commit ends it; the *claim* is now a test.

4. **The bystander case in `_excused` is open**, bounded by seven things, all pinned. The widest
   shape is a markdown table: `|` is not punctuation to `_SENTENCE_END` and a table carries no blank
   line, so a whole table is one sentence bounded only by `EXCUSE_WINDOW`. ~~41 records put a
   backticked test name in a table row (84 rows); 110 put a bare `test_` token in one (374 rows).~~
   **At `3a49c57`: 41 records / 84 rows and 110 / 374. At `a845e7c` — the tree that carries this
   record — 42 / 85 and 111 / 376.** Named without a tree, these were the sixth instance of the
   shape reservation 3 above declares structural and fixed, three sections below that declaration
   (CHG-20260901-05, VETO). No record exhibits the leak today.

5. **`build_factory` still has no handler**, ninth round running.

6. **Nothing checks that a mutation's `says` describes what its replacement does.**

7. **The harness is not in CI**, so all 197 mutations fall to whoever remembers. The import probe's
   cheap half is in the suite; a full run is roughly 54 minutes by the risk seat's measurement.

8. **`isn't done` still reads as finished**, and `_alive`'s coupling to `_owner` is still deferred,
   eleven rounds running.

9. **`test_concurrent_config_edits_and_model_adds_do_not_deadlock` is load-sensitive**, ten rounds.

10. **Any pid that is not currently alive restores the file and deletes the record.** Not two
    sentinels: `0`, `-1`, `1` and `2147483647` all act, measured. `_alive` refuses a bool and a
    non-int — it does distinguish "not a pid at all" — and gives `0` and `-1` no such refusal
    although they are exactly that. Worse, it is **platform-divergent**: `_alive_posix` calls
    `os.kill(0, 0)` (own process group) and `os.kill(-1, 0)` (every signalable process), both of
    which succeed, so the same record refuses on POSIX and acts on Windows — in the one function
    whose docstring is four paragraphs of platform divergence. Reasoned, not measured: no POSIX
    host here. Making the record carry something a second run can verify — a start time, a token —
    is a real change and is not this one. Restated from "two sentinels" in CHG-20260901-05 (risk
    seat).
