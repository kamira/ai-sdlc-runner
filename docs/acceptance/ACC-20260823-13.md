# ACC-20260823-13 — Acceptance: the seats read the requirement before anybody builds against it

- Target CHG: CHG-20260823-13 (an `intake` stage: seats survey the requirement across six aspects,
  reach no verdict, and stop before planning when something is missing)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **medium** — it adds a node to the flow.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** Both engines independently confirmed the property that makes this stage
  different from every other panel: **the survey adjudicates nothing.** `policy.adjudicate` is not
  called anywhere in the SURVEY branch.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_intake.py tests/test_server.py -q -p no:randomly
PYTHONUTF8=1 PYTHONPATH=src python -m ai_sdlc_runner.cli flow      # 24 nodes — this change added the 24th
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | `intake` → `intake_review` → `pm_plan`, with no gate | `graph.py:130-133` (nodes), `SURVEY` `:66-70`, invariant `:284-287` | walked from `engine.py:1235`, start node `intake` | `test_intake.py` walk-level tests | **Pass** |
| 2 | A survey collects every seat and **reaches no verdict** | `engine.py:1415-1478` — the SURVEY branch contains **no `policy.adjudicate` call** | `intake.collect` `engine.py:1436` | `test_intake.py::test_a_survey_reaches_no_verdict` | **Pass** — the distinguishing property, confirmed by both engines reading the branch |
| 3 | Six closed aspects; an unknown aspect is an error | `ASPECTS` `intake.py:45-55`; `collect` `:116+` | `engine.py:1436` | `test_intake.py` | **Pass** |
| 4 | The run stops before planning when something is missing | `engine.py:1460-1478` — `suspended` dict plus `halt_reason` via `intake.stop_reason` | the walk | `::test_an_incomplete_requirement_stops_before_anything_is_planned` | **Pass** |
| 5 | After a third miss the stop carries options, model-authored, at least three | `MIN_OPTIONS` `intake.py:62`; `needs_options` `:153`; `option_request` `:161`; `read_options` refuses fewer than three `:180-194` | `engine.py:1444-1458` | `::test_after_three_asks_the_stop_carries_options` | **Pass** |
| 6 | `intake_history` survives across walks on the server | `server.py:179`, `:489`, `:502` | passed into `RunConfig` `engine.py:446` | `test_server.py::test_adding_to_the_brief_re_walks_the_run` | **Pass** — this test also covers the `/run/instruct` re-walk defect this change found |
| 7 | The console shows missing aspects and options | `index.html:440-470` | — | **none** | **Wired, untested — like all console rendering; see ACC-20260823-12** |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Survey is the first graph stage and adjudicates nothing | PASS | PASS, having read the whole branch for an `adjudicate` call | **Agreed** |
| Suspension before planning, escalation to options | PASS | PASS | **Agreed** |
| Overall | PASS, `UNCONFIRMED: none` | PASS | **PASS** |

## Not closed by this record

Task 8's console block is present in the markup and untested, in common with every other piece of
console rendering. That gap belongs to CHG-20260823-12's record, where it is stated in full, and is
not double-counted as a defect of this change. "847 passed" is a snapshot count, since drifted.
