# ACC-20260823-32 — Acceptance: long paths, an HTML export, and the example that found the next defect

- Target CHG: CHG-20260823-32 (a `paths` module every writer goes through; an HTML waterfall export;
  the `tide-spa` example)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** — a path module every writer needs.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass with reservations.** All three pieces exist, are wired and are bitten today.
  **Two of its claims were false when written**, and CHG-20260823-38 — reviewed by both seats as `not
  sound` — is what made them true.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_paths.py tests/test_example_tide_spa.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | A path module every writer goes through | `paths.py::check`, `paths.py::real` | **all eight** writing modules today — e.g. `store.py::connect`, `attachments.py::Store.add` | `test_paths.py::test_a_direct_write_is_either_routed_through_this_module_or_written_down` — an **AST audit** over every module | **Pass today — 3 of 8 when claimed, see below** |
| 2 | The check agrees with the filesystem | `paths.check` | — | `::test_the_check_agrees_with_what_the_filesystem_actually_does` | **Pass** |
| 3 | An HTML waterfall export | `_html` `conversations.py:1408`; `FORMATS` `:1070` | `export_conversation` `cli.py:518`; choices `cli.py:1049` | covered in `test_conversations.py` | **Pass** |
| 4 | The `tide-spa` example, driving the real CLI | `examples/tide-spa/{plan.json,scenarios.py,agent.py,README.md}` | — | `test_example_tide_spa.py` — 15 passed; `::test_the_readme_table_matches_the_scenarios_the_driver_defines` | **Pass** — a README table checked against the code that implements it |
| 5 | A misspelled key is refused before a single ask | plan validation | the example driver | `::test_a_misspelled_key_is_refused_before_a_single_ask` | **Pass** |

## The two claims that were false when written

Both established by CHG-20260823-38, which the seats reviewed and called `not sound` — so this is the
ledger catching itself, twice over.

1. **"A path module every writer now goes through."** It covered **3 of 8** modules. Full coverage is
   CHG-38's work, not this change's.
2. **"Genuinely cross-platform" per-component refusal.** It counted **bytes everywhere**, which
   refuses a legal 100-character CJK name on Windows. The unit-aware `measure()` at `paths.py:121`
   is CHG-38's.

Both are true today. Neither was true when the record said so.

## Superseded, not failed

Scenario C's asserted broken behaviour was **deliberately inverted** by CHG-20260823-34, which
defined blank content once and refused it at both boundaries. Documented supersession.

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Paths, HTML export and the example wired today | PASS | PASS, 15 example tests green | **Agreed** |
| The every-writer claim | **flagged** — "the original every-writer claim was false and only became true through CHG-38" | **same finding**, with the 3-of-8 count | **Agreed independently by both engines** |
| The cross-platform claim | not raised | **found it** — bytes everywhere, CJK on Windows | **fable's finding stands** |
| Overall | PASS_WITH_RESERVATIONS | PASS_WITH_RESERVATIONS | **PASS_WITH_RESERVATIONS** |

## Not closed by this record

Nothing outstanding — CHG-20260823-38 closed both, and is accepted separately with its own
reservation.

> **Citations re-anchored (CHG-20260905-05).** The line numbers this row carried were measured dead: three pointed at blank lines and the rest at unrelated code, having drifted by up to 615 lines — and two of them moved *again* inside the round that repaired them. The verdicts are unchanged; only the pointers are, from line numbers to symbol names, which move with the code.
