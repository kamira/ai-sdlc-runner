# ACC-20260823-46 — Acceptance: the target is listed once

- Target CHG: CHG-20260823-46 (build the target's index once before the import loop, keep it current
  during the loop, and read a target document only on an id collision)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** — same decisions, same refusals, fewer reads.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass.** A performance change whose tests count **calls** rather than measure seconds
  — which is the disposition the CHG argues for, and it is the right one: the old per-conversation
  listing could not pass these tests on any machine.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_conversations_sqlite.py -q -p no:randomly
git show f58a807:src/ai_sdlc_runner/conversations.py     # the per-conversation listing it replaced
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | The target is listed **once**, before the loop | `in_target` built from a single `into.conversations()` call, `conversations.py:668-673` | the import loop `:675-743` | `::test_the_target_is_listed_once_however_many_conversations_are_imported` — **behavioural**, counting calls through a `_Counting` wrapper | **Pass** |
| 2 | The index is kept current during the import | `in_target[cid] = pid` after each import, `conversations.py:720` | the loop | same | **Pass** — without this, listing once would be listing a stale index, and the refusals would change |
| 3 | A target document is read **only** on an id collision | `into.read` only when `cid in in_target`, `conversations.py:698-699` | the loop | `::test_the_target_is_read_only_where_an_id_actually_collides` — asserts **zero** reads on a clean import, **exactly once** on a full collision, and that the listing is still one | **Pass** |

The `fable` engine checked the counterfactual directly: the per-conversation listing at `f58a807`
could not pass either test. That is what makes these tests a pin on the behaviour rather than a
record of a timing run.

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Listed once, index current, read on collision only | PASS | PASS | **Agreed** |
| The timing table (3.86s to 0.93s) | **not re-measured**; called structurally confirmed | **not re-measured**; noted the counting tests stand in for it, which is the CHG's own stated position | **Agreed — and the CHG chose the better evidence** |
| Overall | PASS | PASS | **PASS** |

## Not closed by this record

- The before-and-after timing table was not reproduced. Neither engine treats this as a gap, because
  the change deliberately pinned **call counts** instead of durations — a timing assertion would be
  the flakier test and the weaker claim.
- **CHG-20260823-47 later corrected a defect introduced around here**: a full disk was filed as
  per-conversation damage. It is now `TargetError` (`conversations.py:534-542`), pinned by
  `::test_a_broken_target_is_tried_once_and_named_as_the_target`. It does not touch this change's own
  claims.
