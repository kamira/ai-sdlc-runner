# ACC-20260823-21 — Acceptance: the schema design went to the seats

- Target CHG: CHG-20260823-21 (a turn's body cannot rewrite its envelope; the model registry's
  top-level envelope is closed; the schema catalogue is pinned by exercising, not by labels)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **medium** — two real changes to shapes that persist.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass with a reservation.** Both fixes are present and **both fire when driven** — the
  `fable` engine drove each by hand. Neither has an automated test that bites, so cutting either
  check would leave the suite green.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_schemas.py tests/test_conversations_sqlite.py -q -p no:randomly
# driven by hand by the fable engine:
#   conversation.turn(NOTE, seq=999, ...)   -> ConversationError, as designed
#   a registry file with a `modelz` key     -> refused, as designed
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | A turn's body cannot rewrite its envelope | `Conversation.turn` `conversations.py:951-955`, checked **ahead of** `_guarded`; `Turn.as_dict` `:161-166` spreads the body first | every engine turn — `engine.py:590`, `:631`, `:1277` | **none** — driven by hand instead | **Fires, untested** |
| 2 | The registry envelope is closed | `models.load` refuses unknown top-level keys `models.py:276-286` | the registry import path `cli.py:870` | **none** — driven by hand instead | **Fires, untested** |
| 3 | The catalogue is pinned by **exercising** each schema, not by reading a label | `test_schemas.py:63` — the closed count is counted by driving each schema with an unknown field | the suite | `::test_the_closed_count_is_the_number_of_schemas_that_are_actually_closed` | **Pass** — and this is the change's best idea: a count derived by trying, not by trusting a docstring |
| 4 | The attachment id is not the stored filename | — | — | `::test_the_attachment_id_is_not_the_stored_filename` | **Pass** |
| 5 | Body and envelope stay separated in storage | — | — | `test_conversations_sqlite.py::test_the_body_is_stored_without_the_envelope` | **Partial** — pins the storage separation, not the refusal |

## The reservation

The two refusals this change exists for are the two things nothing tests. The `fable` engine
confirmed both by constructing the offending input and watching the error — which is real evidence
that the mechanism works today, and no evidence at all that it will still be called tomorrow.

Both engines note this independently. `codex` reached the same place from the opposite direction: it
could not run the tests at all in its sandbox and so recorded its findings as read-only.

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Both fixes on production paths | PASS | PASS, and drove both refusals by hand | **Agreed** |
| A test bites | **could not execute tests** — read-only sandbox had no writable temp dir | **searched and found none** | **fable's finding stands** |
| Overall | PASS | PASS_WITH_RESERVATIONS | **PASS_WITH_RESERVATIONS** |

## Not closed by this record

- **No automated test drives either refusal.** Needs its own CHG.
- Drift, not failure: `test_schemas.py` now holds 10 functions where the CHG claimed 17 collected —
  parametrisation and forty-plus later changes account for it — and
  `test_the_catalogue_has_all_fourteen_entries` was replaced by an index-driven test.
- The plan-file openness this change names remains an open finding, as the CHG itself states.
