# ACC-20260827-08 - Acceptance: the README diagram's completeness is unchecked

- Target CHG: CHG-20260827-08
- Verifier: **self** (the session that built it). Permitted at this risk grade, and disclosed rather
  than dressed up: every change in this batch is low, and none touches the red lines, the gate
  matrix or the flow. The independent work that produced these changes was the acceptance round of
  2026-08-27 - two engines that had built none of the code they read. What follows is the
  implementer checking their own fix, which is worth exactly what it is worth.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** - tests only
- Conclusion: **Pass. Arithmetic and completeness are now separate checks, because they fail separately.**

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_documented_numbers.py -q -p no:randomly
```

Batch-wide: `pytest tests/ -q -rs` -> **1347 passed, 3 skipped**; `tools/mutation_check.py` -> **15 of 15 caught**;
`tools/ledger_check.py --repo .` -> passed, 86 changes.

## Check-details

| # | Check | Method | Result |
|---|---|---|---|
| 1 | Every graph node appears in the drawing | `test_every_node_in_the_graph_is_drawn_in_the_readme_diagram` | **Pass** - all 24 |
| 2 | The check can fail | `test_the_completeness_check_would_notice_a_node_left_out` - runs the same comparison against a diagram with one node removed | **Pass** |
| 3 | The section is located structurally | read `_readme_flow_diagram` | **Pass** - by heading and fence, not by line number, so moving the section does not break it |
| 4 | The count check is untouched | ran the file | **Pass** - the arithmetic half still compares the sentence against the graph |

## Not closed by this record

Nothing. The gap was narrow and specific - a node dropped from the picture while the count sentence
stayed correct - and it is now the case that either failure is caught by a different test.
