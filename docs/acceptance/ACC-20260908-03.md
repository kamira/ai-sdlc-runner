# ACC-20260908-03 — the routability rule that covers one of the two panel modes

- Project: ai-sdlc-runner
- Target CHG: CHG-20260908-03
- Date: 2026-09-09 (UTC+0)
- **Measured against `origin/main` at `d9e750e`**, the tree this branch is cut from.
- Conclusion: **Pass** — on the measurements below. This line does not claim a seat has read the
  revision it sits in; a conclusion is written before its own review.

## The defect, made to happen before it was fixed

The rule CHG-20260901-11 added is guarded on one of the two panel modes. Measured by renaming the
one seat-panel node's branches and validating the result:

```
lead_review with branches approve/reject, panel_branches {}   validate PASSES
```

It would then reach `engine`'s branch lookup, `node {id!r} has no branch {choice!r}` — the same
line, the same death, that CHG-20260901-11 was opened for and closed on the model-panel side only.

With the rule in place, both shapes are refused:

```
renamed branches          "node 'lead_review' is routed by the review seats, whose 'pass' names
                           no branch of its ['approve', 'reject']"
declared panel_branches   "node 'lead_review' declares `panel_branches` and is routed by the
                           only a model panel routes through"
```

## Why the seat rule is stricter, and why that is not an inconsistency

| | takes its branch by | `panel_branches` |
|---|---|---|
| `MODEL_PANEL` | `node.panel_branches.get(panel_outcome, panel_outcome)` | consulted — declaring it is a real escape |
| `SEAT_PANEL` | `_adjudicate(...)` returns the outcome as the branch name | **never routed through** — but read once more at `engine.py:3074`, after the branch, to name the word meaning ratified |

So the model-panel message can offer *"Declare `panel_branches` on it, or name its branches in the
panel's own words"* and the seat message cannot offer the first half. Two rules rather than one
widened guard, because the escapes differ.

This is also what makes the queue item's *"`lead_review` qualifies identically"* wrong. It names
`pass`/`fail`, so it is one of the four nodes that do — but it is not in the population those two
comments count, because they are about a table its path does not route through.

## The evidence

| # | Criterion | How | Verdict |
|---|---|---|---|
| 1 | The gap was real, not inferred | constructed and run: `validate` accepted a seat panel branching `approve`/`reject` | **Pass** |
| 2 | It was reachable in principle and not in fact | one `SEAT_PANEL` node ships and it names `pass`/`fail`, so this is latent | **Pass** |
| 3 | The repair refuses both shapes | both messages above, run | **Pass** |
| 4 | It refuses nothing the shipped graph does | `test_graph_validation` + `test_execution_mode` + `test_flow` + `test_panel_and_pool`: 203 passed before the tests were added | **Pass** |
| 5 | The message does not offer the escape it does not have | it names the mapping only to say that only a model panel routes through it. An earlier version said the path *never reads* it, which `engine.py:3074` refutes — a seat measured that, and the true reading makes the refusal sharper: a declared-but-unrouted mapping gives a `ratified` the node's own answer can never equal | **Pass, after a refusal** |
| 6 | Both new rules are pinned when removed | `--only panel-routability`, **3 of 3 CAUGHT** | **Pass** |
| 7 | And so is the rule that never had a reverse test | the third of those three is CHG-20260901-11's own rule; this file's docstring lists it among nineteen with none | **Pass** |
| 8 | The two comments say which population they count | both rewritten; four figures between them | **Pass** |
| 9 | And those figures are held | `test_the_populations_the_panel_comments_count_are_the_ones_they_say` asserts all four — written in the same round that wrote them, not after a seat asked | **Pass** |
| 10 | `_mutate` is one function | `mutated` in `_graph_swap`, imported by both modules; the now-unused `dataclasses` import in one of them removed | **Pass** |
| 11 | The two swaps that hold assertions open share one implementation | `swapped_graph`; a sweep found the second site, which the item did not name | **Pass** |
| 12 | The new shared `finally` is guarded | `test_the_context_manager_puts_both_views_back_when_the_body_raises`, asserted by identity, and registered: `--only graph-swap` **3 of 3 CAUGHT** | **Pass, after the guard caught it** |
| 13 | Every mutation still names one occurrence | `test_every_shipped_mutation_has_a_unique_anchor` refused the old anchor once this module held two identical restores; re-anchored on the line above | **Pass, after a refusal** |
| 14 | Documented numbers move with the tree | five new tests made `README.md`'s count stale in the same run that added them — 2354 to **2359**, caught by `test_the_test_count_the_readme_states_is_the_real_one` | **Pass** |
| 15 | The suite is green | `2355 passed, 4 skipped in 533s`, taken on the tree that ships. Two earlier greens are not evidence about it: `2354 passed` was measured with `README.md` stale, and `644s` before the corrections a seat required | **Pass** |

## What the repository's own guards caught, in this record

Three, and each is the kind of thing that would otherwise have shipped:

1. **`test_every_shipped_mutation_has_a_unique_anchor`** — adding a second `try`/`finally` to
   `_graph_swap.py` gave an existing mutation two occurrences of its anchor, so it would have
   reverted whichever came first rather than the one it names. Re-anchored.
2. **`test_every_change_id_the_source_cites_has_a_record`** — `graph.py` and `engine.py` cited
   `CHG-20260908-03` while the record was still in a scratch directory.
3. And the gap the first of those exposed: a **new shared `finally` that nothing guarded**. The
   module's own docstring says what that costs — *"39 test functions share this one `finally`, and
   until that test was written nothing named could notice it going"* — and the fix added a second
   one. It now has the twin test and its own mutation.

## What this does not do

- No node routes differently. The shipped graph passes both new rules unchanged.
- `validate_with` is written in terms of `swapped_graph`. The first version of this record kept
  them apart and said 39 test functions rest on the swap closing before they see anything — true,
  and not a reason, because a `with` closes before its function returns. A seat measured the fold
  and so did this record: 68 passed in the three modules that use it, and one restore is left in
  the module whose first line says "once".
- The deliberate half-swap in `test_graph_validation` is left alone, for the reason its own
  docstring gives — it exists to be wrong so the two-view rule has something to refuse.
- Nothing is done about the two function-local `import dataclasses` statements this record read
  past. They are unrelated, and a record that tidies whatever it passes is one nobody can review.

## The run

```
--only panel-routability   3 of 3 CAUGHT
--only graph-swap          2 of 2 CAUGHT   (one restore in the module, so one entry; the
                           second went with the fold, and two named tests reach the one left)
tools/ledger_check.py      passed (242 changes)
doc guards                 217 passed
pytest -q                  2355 passed, 4 skipped in 533s
README.md                  2354 -> 2359
```

The `2354 passed` run is not in that block: it was taken with `README.md` stale, so it is not
evidence about this tree. The figure above is the run taken after the count moved.
