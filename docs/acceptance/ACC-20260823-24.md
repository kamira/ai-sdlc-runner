# ACC-20260823-24 — Acceptance: where model configuration is actually persisted

- Target CHG: CHG-20260823-24 (the assignment half of model configuration is persisted by nobody,
  exposed read-only, and pinned by a test that fails if a writer appears)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** — no behaviour changes.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Superseded, and superseded exactly the way it was designed to be.** Its central claim
  was replaced by CHG-20260823-25 after the user ruled that 「模型配置」 covers both halves. **Nothing
  this change claimed was ever false.**

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_models_schema.py -q -p no:randomly
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## The tripwire worked, and that is the finding

This change wrote a test whose job was to **fail when somebody built the thing it said was not
built**. Somebody did, one change later.

`tests/test_models_schema.py:228` — `test_both_halves_of_model_configuration_persist` — carries the
inversion in its own docstring: *"A writer appeared… So this now pins the other direction."*

That is the mechanism doing precisely what it was for. A test that pins an absence and is then
inverted by the change that fills it is a better record than a paragraph saying "not built yet",
because the paragraph would have gone stale in silence.

## Check-details

| # | Claim | Where it stands now | Test | Result |
|---|---|---|---|---|
| 1 | The assignment is persisted by nobody | **replaced** — `set_node_models` `store.py:400`, `set_seat_model` `store.py:421`; POST `/config/nodes` and `/config/seats` `server.py:810-812` | `::test_both_halves_of_model_configuration_persist`, inverted | **Superseded by CHG-25** |
| 2 | The registry half **is** persisted | still true — POST `/models` persists via `models.save` | asserted in the same test | **Pass** |
| 3 | The assignment is read-only over HTTP | still true of GET; mutation lives only in POST | `::test_the_assignment_is_read_only_over_http` | **Pass** |
| 4 | The §0.2 ambiguity is named and answered | `docs/DATABASE.md:46-58` — "Answered: both halves", naming CHG-25 | `::test_both_pages_say_where_each_half_lives` | **Pass** — the answer is on the page and the page is tested |
| 5 | Assignments are read from the plan at startup | **moved** — assignments now resolve plan-over-store with a `source` map, per CHG-25 | — | **Superseded by CHG-25** |

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| The read-only claim | **FAIL** — "current code persists them through two HTTP mutation routes and the original test has been inverted" | **SUPERSEDED** — same facts, named CHG-25 and the user ruling behind it | **fable's reading is correct** |
| Overall | FAIL | SUPERSEDED, `UNCONFIRMED: none` | **SUPERSEDED** |

The two engines saw the same code and split only on what a verdict word should mean. The codex
engine's brief did not distinguish supersession from failure; that gap is recorded in the round
record as a defect of the brief, not of this change.

## Not closed by this record

Nothing. This is the cleanest supersession in the ledger: a claim made carefully, pinned by a test
designed to break when it stopped being true, and broken on purpose one change later by a user
decision.
