# ACC-20260617-08 — Multi-project workspace, structure analysis, cross-repo run

- Project: ai-sdlc-runner
- Date: 2026-06-18
- Target: CHG-20260617-08
- Verifier: V1 (independent; ≠ implementer I1)
- Risk: medium (inherited)
- Baseline source: CHG-20260617-08 modification guide; user directive (register projects → set main/entry → structure analysis → then AI-SDLC); skill `cross-repo.md` + `cross_repo_check.py`
- Conclusion: **Pass**

## Check details
| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | Register one or more project locations | Pass | `runner workspace --authority A --repo B --repo C` saves `.sdlc-workspace.json` at the authority; `workspace.build/save/load` round-trips; `test_workspace_build_and_save_load` |
| 2 | Designate the main project as the entry (authority) | Pass | `Workspace.authority` = main; consumers listed; `set_authority` swaps; matches the skill's authority model |
| 3 | Structure analysis across projects (full four structures) | Pass | `runner analyze A` scanned 3 repos and scaffolded 12 `docs/structure/*` docs (directory from real tree; logical/design/data seeded for A1); `test_write_structure_baseline_creates_four_docs` |
| 4 | Cross-repo wiring (authority contract + consumer pointers) | Pass | authority `docs/contracts/VERSION = v1.0.0`; each consumer `docs/authority.md` with `Pinned version: v1.0.0` (exact format `cross_repo_check.py` parses); `test_analyze_workspace_sets_authority_and_pointers` |
| 5 | Then run AI-SDLC, cross-repo consistency gated | Pass | `runner run A`: consistent → proceeds → four-stage loop → red-line halt; bump authority to v2 → **drift HALT** (exit 10); `test_cross_repo_gate_consistent_then_drift` |
| 6 | Agents run inside their target repo | Pass | `AgentSpec.workdir` carried + named in prompt; `CommandExecutor` runs with `cwd=workdir` (a `pwd` agent prints the project dir); `test_command_executor_runs_in_workdir` |
| 7 | Persisted + reusable | Pass | manifest saved at the authority; `workspace.load` reused by `run`/`analyze` |
| 8 | Single-project behavior unchanged | Pass | `run` without a workspace shows no `workspace:`/cross-repo lines; single-project `analyze` writes no `docs/contracts`; `test_analyze_single_project_no_contracts` |
| 9 | Interactive registration available | Pass | `runner workspace` (no flags) prompts for the main + consumer paths; menu actions "Set up workspace" / "Analyze structure" |
| 10 | Gates/red-lines unchanged; no duplicated logic | Pass | cross-repo decision comes from the skill's `cross_repo_check.py` (via `gates`); halt gates unchanged; V1 still excludes Agent |
| 11 | Zero new dependencies | Pass | `workspace.py` / `structure_scan.py` external imports = NONE |
| 12 | Tests + docs | Pass | `tests/test_workspace.py`; full suite **83 passed, 3 skipped**; directory/logical/design + ARCHITECTURE + README (EN + 繁中) synced |

## §7 prohibition re-check
- No skill logic duplicated (cross-repo decision from the skill's script); V1 tool lockdown, red-line halts intact; skill markdown not copied into runner code (the store is the separately-governed CHG-05 override). Pass.

## Unmet items & handling
None. (The four-structure *analytical content* — logical/design/data — is seeded from the scan and
completed by the A1 stage when a real backend runs; the `directory` doc and all cross-repo wiring are
produced deterministically now. Integration acceptance across repos is the closing V1 step per the
skill's cross-repo model.)

## Structure consistency check
- [x] Implementation matches updated structure docs (`workspace`, `structure_scan`, executor `workdir`, cross-repo gate).

## Summary
Deliverable. The runner now supports the requested flow: register one or more projects, designate the
main (authority) project, run a structure analysis across them (four-structure scaffold + authority
contract + consumer pointers), and then run the AI-SDLC loop gated on cross-repo consistency, with each
agent executing inside its target repo. Aligned to the skill's cross-repo model; single-project use is
unchanged. CHG-20260617-08 set to **Accepted**.
