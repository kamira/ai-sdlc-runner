# ACC-20260902-20 — Acceptance: a flag that counted where it said it would refuse

- Target CHG: CHG-20260902-20
- Verifier: self (main agent = implementer)
- Found by: the **defect seat** of the CHG-20260901-15 third-draft review (D-4, D-5) and the
  **conformance seat** (three false statements, turned up by its sweep for texts the design would
  falsify — these were already false).
- Date: 2026-09-02 (UTC+0)
- Risk: medium — whether `--worktree` keeps its promise, and what three documents say that is not so
- Conclusion: **Pass.** Defect nineteen, one latent guard, and three false statements.

## Test-the-test

| | revert | result |
|---|---|---|
| ① the `--worktree` refusal | drop the `trees.required` branch | **1 failed** — `test_worktree_refuses_a_module_ask_it_cannot_isolate` |
| ① the unflagged path | — | **still green** — `test_without_the_flag_the_same_ask_is_recorded_rather_than_refused`, which is the point: the refusal must not become the default |
| ② `plan.FIELDS` | — | latent; both directions clean today, and the guard can now fail |
| ③ three statements | — | no behaviour; each is pinned by being true |

## Check-details

| # | Task | Result |
|---|------|--------|
| 1 | `--worktree` refuses an ask it cannot isolate | **Pass** — measured in a real git repo: `agent_command` node ISOLATED, registry-model node REFUSED |
| 2 | Without the flag, nothing changes | **Pass** — isolated / shared-dir, and the shared one is counted |
| 3 | The refusal names why and what to do | **Pass** — names the registry, `agent_command`, and the flag |
| 4 | `plan.FIELDS` drifts in neither direction | **Pass** |
| 5 | `SHIP_FIELDS` too | **Pass** |
| 6 | `engine.py` says where `validate` runs | **Pass** — `graph.validate()` at the top of `walk` |
| 7 | `logical.md` matches `design.md` and the code | **Pass** |
| 8 | `data.md` states three fields and the right subject | **Pass** |
| 9 | Each fix fails without it, where it can | **Pass** — see above |
| 10 | README follows the suite | **Pass** — 1954 → **1957** |
| — | Full suite | **Pass** — **1953 passed, 4 skipped** (1957 collected), pytest exit 0 |
| — | Ledger lint | **Pass** |

## Reservations (disclosed, not waived)

1. **Written by the implementer**, on defects somebody else measured.

2. **I wrote the README count wrong first.** I said 1958 from arithmetic — four tests added — and
   the collector says 1957. Two of the four moved between files while I was fixing a helper-name
   collision, and I counted the move as an addition. `test_documented_numbers` caught it, which is
   what it is for; the number in this record is the collector's, not mine.

3. **I created a test file that already existed under another name.** `tests/test_worktree.py`,
   beside the shipped `tests/test_worktree_isolation.py`, with no imports and a `_repo` helper that
   shadowed one four hundred lines above it — four existing tests went red. Merged and renamed. The
   suite caught it immediately; a reviewer would not have, and a second file for one subject is the
   kind of drift this repository's guards are otherwise full of.

4. **① does not fix the isolation, only the silence.** A configured model still runs in the
   operator's shell directory when the flag is off, exactly as `_cwd_for` documents. Whether that
   should change is a different change and this deliberately is not it — but a reader of the
   refusal may expect the flag to *make* it work rather than to stop.

5. **② is latent and I fixed it anyway.** Nothing fails today. It is here because it is
   CHG-20260901-17 M-3's shape in a second file, two changes later, and a guard that has been
   one-way since it was written will not become two-way by being noticed twice.

6. **③ has no test.** Three sentences and a table row are pinned by being true, not by a check.
   `test_documented_numbers` covers numbers in the README and not these files, so the next drift in
   `logical.md` or `data.md` will be found the same way this one was — by somebody reading.
