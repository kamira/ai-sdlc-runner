# ACC-20260827-14 - Acceptance: a design section that describes more than the repository contains

- Target CHG: CHG-20260827-14
- Verifier: **self** (the session that built it). Permitted at this risk grade, and disclosed rather
  than dressed up: every change in this batch is low, and none touches the red lines, the gate
  matrix or the flow. The independent work that produced these changes was the acceptance round of
  2026-08-27 - two engines that had built none of the code they read. What follows is the
  implementer checking their own fix, which is worth exactly what it is worth.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** - documentation
- Conclusion: **Pass. Designed-and-not-built is now visible where the design is read.**

## Evidence commands

```
grep -n "not built" docs/changes/CHG-20260823-11.md
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_documented_numbers.py -q -p no:randomly
```

Batch-wide: `pytest tests/ -q -rs` -> **1347 passed, 3 skipped**; `tools/mutation_check.py` -> **15 of 15 caught**;
`tools/ledger_check.py --repo .` -> passed, 86 changes.

## Check-details

| # | Check | Method | Result |
|---|---|---|---|
| 1 | The six unbuilt endpoints are marked | read the Endpoints section | **Pass** - each carries **[not built]**, with a note above naming the round |
| 2 | The phase strip is marked | read "The screen" | **Pass** - marked in place, under the item it qualifies |
| 3 | Nothing is deleted | `git diff` | **Pass** - what was proposed stays; only its status is added |
| 4 | The task-count test still holds | ran it | **Pass** - the annotations do not add or remove a `**[x]**` |
| 5 | What replaced `/run/resume` is said | read the note | **Pass** - the decision routes each re-walk, so there was nothing for a resume verb to do |

## Not closed by this record

Nothing about this record. The general question it raises is untouched: **nothing checks that a
design section's endpoint list matches the server's routes.** `docs/API.md` is pinned that way
(CHG-20260823-22) and a change record is not, deliberately - a record states what was true when it
was written. Marking divergence in place is the convention; catching it automatically is not.
