# ACC-20260823-28 — Acceptance: the plan file, closed

- Target CHG: CHG-20260823-28 (an unknown top-level key is refused; the `ship` block is closed with
  four required keys; wrong shapes are refused at the door)
- Verifier: **two independent engines, neither of them the implementer** — `codex-cli 0.147.0` and a
  Claude `fable` subagent, same brief, separate sessions.
- Date: 2026-08-27 (UTC+0)
- Risk: **medium** — a behaviour change; a plan that used to run now does not.
- Round: retrospective — see [the round record](../worklog/acceptance-round-20260827.md).
- Conclusion: **Pass with a reservation about its framing.** Everything it built is real and wired.
  Its claim to have **closed the silent dry run** was not true when written — CHG-20260823-30 proved
  the dry run still producible with keys this change accepted.

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_plan.py tests/test_schemas.py -q -p no:randomly
git show eeeaa90:src/ai_sdlc_runner/plan.py      # this change's own commit
```

Round-wide machine evidence: **1316 passed, 3 skipped**; mutation check **12 of 12 caught**;
`ledger_check` passed, 71 changes.

## Check-details

| # | Claim | Mechanism | Caller | Test that bites | Result |
|---|---|---|---|---|---|
| 1 | An unknown top-level key is refused | `check` `plan.py:99-105` | `plan.load` `:224-233`, then `cmd_run` `cli.py:595` and `cmd_serve` `cli.py:800` | `test_plan.py::test_an_unknown_top_level_key_is_refused` | **Pass** |
| 2 | The `ship` block is closed, with its required keys | `_check_ship` `plan.py:188-199` — unknown keys and `SHIP_REQUIRED` | `check` `plan.py:184-186` | `::test_a_misspelt_ship_key_is_refused_rather_than_skipped`; `::test_a_ship_block_missing_a_key_it_indexes_is_refused_at_the_door` | **Pass** |
| 3 | Wrong shapes are refused | `_MAPPINGS` as objects; `node_models` must be a list — `plan.py:107-121` | `check` | covered in `test_plan.py` | **Pass** |
| 4 | The refusal happens at the door the operator actually walks through | — | the CLI | `::test_the_cli_refuses_a_typo_instead_of_running` — a **subprocess**, not a function call | **Pass** — this is the difference between a validator and a gate |
| 5 | No drift between what the code reads and what the plan may carry | two derived lists | — | `::test_every_key_the_code_reads_is_one_the_plan_may_carry`; `::test_every_ship_key_the_code_reads_is_one_the_block_may_carry` | **Pass** |
| 6 | The closed count is the number actually closed | — | — | `test_schemas.py::test_the_closed_count_is_the_number_of_schemas_that_are_actually_closed` | **Pass** |

## The reservation: what was still open at commit `eeeaa90`

The refusals below were **absent** at this change's own commit and arrived only with
CHG-20260823-30:

- `acc_id` / `acc_body` present without `task` — **and** `task` present without `acc_id`
- `risk` restricted to the defined grades
- `autonomy` must be a string
- every `ship` value must be a string
- `operations` as a list of objects
- the three decision forms

The consequence at `eeeaa90` is concrete: a plan carrying `acc_id` and `acc_body` with no `task`
passed every check and **ran as a silent dry run, reporting `finished`**. That is the exact case
this change's record says decided the change — which makes the framing, not the work, the thing that
was wrong.

Recorded as a reservation and not a failure because every task in the table was genuinely done. What
overreached was the sentence around them.

## Cross-consensus

| Claim | `codex-cli` | `fable` | Resolved |
|---|---|---|---|
| Plan validation closed at the claimed levels, invoked by both commands | PASS, `UNCONFIRMED: none` | PASS | **Agreed** |
| "The silent dry run is closed" | not examined | **found it false at `eeeaa90`**, listed the six refusals that arrived with CHG-30 | **fable's finding stands** |
| Overall | PASS | PASS_WITH_RESERVATIONS | **PASS_WITH_RESERVATIONS** |

## Not closed by this record

Nothing outstanding against this change — CHG-20260823-30 closed what this one left open, and is
accepted separately.
