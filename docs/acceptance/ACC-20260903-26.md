# ACC-20260903-26 — the withdrawal reaches the machine, and the edge is walked in both directions

- Project: ai-sdlc-runner
- Target CHG: CHG-20260903-26
- Date: 2026-09-03 (UTC+0)
- Reviewed by: the operator's standing instruction, on the **idiom seat's** finding 4
- Conclusion: **Pass**, with two reservations recorded below

## What was actually broken, restated so a later reader need not reconstruct it

```
docs/changes/CHG-20260901-15.md  line  16   ## Status: **withdrawn**, and re-issued as two records
                                 line 352   ## Status  ->  **Under review, fifth draft**

tools/ledger_check.py            regex      ^##\s+Status\s*$
                                 line 16    NO match  - the trailing prose defeats the anchor
                                 line 352   match

what the lint reported           'under review'  ->  IN_PROGRESS
`python tools/ledger_check.py`   ledger check passed (154 changes)
```

A record this session withdrew, that two successors claim to supersede, and that eight defect
records point back to, was — to the only machine in this repo that reads a status — a fifth draft
still under review. The lint was green **because it never looked**.

## Criteria

| # | criterion | evidence |
|---|---|---|
| 1 | `_status_line` returns the withdrawn line | `'**Withdrawn** — superseded by CHG-20260903-24 and CHG-20260903-25…'` |
| 2 | The record carries exactly one `## Status` heading | counted: 1 |
| 3 | The prose heading keeps its words and loses the field's name | `## Withdrawn, and re-issued as two records` |
| 4 | The back-pointer field exists and parses | `_superseded_by` → `['CHG-20260903-24', 'CHG-20260903-25']` |
| 5 | The forward edges parse | `_supersedes` → `['CHG-20260901-15']` on both successors |
| 6 | A `Supersedes:` naming a nonexistent CHG is reported | `test_a_supersedes_naming_a_change_that_does_not_exist_is_reported` |
| 7 | A missing back-pointer is reported | `test_a_superseded_record_with_no_back_pointer_is_reported` |
| 8 | A superseded record still under review is reported | `test_a_superseded_record_still_under_review_is_reported` |
| 9 | Prose shaped like the field does not clear it | `test_saying_it_in_prose_above_the_status_section_does_not_clear_it` |
| 10 | A correctly withdrawn pair passes | `test_a_properly_withdrawn_pair_passes` — so the checks are pinned to the defect, not the field |
| 11 | The real record is asserted, not only fixtures | `test_this_repos_own_withdrawn_design_record_says_so_where_the_lint_reads` |
| 12 | `_status_line`'s regex is unchanged | `git diff` shows no change to it |
| 13 | The `IN_PROGRESS` vocabulary is unchanged | `withdrawn`/`superseded`/`abandoned` were already in it |
| 14 | **Each fix has a test that fails without it** | two reverts, below |
| 15 | The README's test count follows the suite | `--collect-only`, not arithmetic |

## Criterion 14 — the reverts, run rather than asserted

**Revert A — the record restored, the lint code kept.** The lint went red on the real ledger with
four messages naming the right records:

```
CHG-20260903-24: says it supersedes CHG-20260901-15, and CHG-20260901-15 does not say so back.
CHG-20260901-15: CHG-20260903-24 supersedes it, but its Status still says 'under review'.
CHG-20260903-25: says it supersedes CHG-20260901-15, and CHG-20260901-15 does not say so back.
CHG-20260901-15: CHG-20260903-25 supersedes it, but its Status still says 'under review'.
```
and `test_this_repos_own_withdrawn_design_record_says_so_where_the_lint_reads` **failed**.

**Revert B — the record fixed, the lint code removed.** Five of the six new tests **failed**:

```
FAILED test_a_supersedes_naming_a_change_that_does_not_exist_is_reported
FAILED test_a_superseded_record_with_no_back_pointer_is_reported
FAILED test_a_superseded_record_still_under_review_is_reported
FAILED test_saying_it_in_prose_above_the_status_section_does_not_clear_it
FAILED test_this_repos_own_withdrawn_design_record_says_so_where_the_lint_reads
```

The sixth, `test_a_properly_withdrawn_pair_passes`, **passed under revert B** — correctly: it is the
negative case, and a check that is always red proves nothing. Both halves of the fix are pinned,
each by the revert that removes it.

## Reservations

1. **This is the twenty-eighth defect of the CHG-20260901-15 review, and I shipped it.** The other
   twenty-seven were found by seats reviewing a design; this one was found by a seat reviewing my
   *withdrawal* of that design, and it has the identical shape — a declaration that does not reach
   the thing it declares about, written in the file that breaks it. The suite found none of the
   twenty-eight.
2. **`ACC-20260903-23` still defers L-27 into a record that is now withdrawn.** An accepted
   reservation points at CHG-20260901-15 task 24, which neither successor carries. This change makes
   the withdrawal visible; it does not give L-27 an owner. Named here rather than folded in — it is
   a live console defect (six snapshot keys reach no front end; `RunState.error` is never cleared, so
   a finished run shows the previous walk's error box), and it needs its own record.

## The suite

```
baseline on origin/main   1967 passed, 4 skipped   (1971 collected)  README: "# 1971 tests"
after this change         1972 passed, 4 skipped   (1977 collected)  README: "# 1977 tests"
                          +6, which is the six tests this record adds
measured                  pytest --collect-only -q  ->  1977 tests collected
```

`test_the_test_count_the_readme_states_is_the_real_one` was the only failure of the full run and it
was this: the count is stated, not derived, and the README was one change behind. Counted with
`--collect-only`, not with arithmetic — the arithmetic has been wrong four times this session.

Re-run after the README edit, with the package on the path: **179 passed**
(`tests/test_documented_numbers.py` + `tests/test_ledger_check.py`).

Noted in passing, not fixed here: `tests/test_documented_numbers.py` run **alone** fails 15 tests
with `ModuleNotFoundError: No module named 'ai_sdlc_runner'` — it depends on another module having
put `src/` on the path first. Green in the full suite, red on its own. Not this change's, and not
folded in; recorded so the next reader does not mistake it for one.

## Status

**Accepted**
