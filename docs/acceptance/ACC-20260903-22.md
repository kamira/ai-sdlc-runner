# ACC-20260903-22 — Acceptance: the console can declare a class

- Target CHG: CHG-20260903-22
- Verifier: self (main agent = implementer)
- Found by: the **idiom seat** of the CHG-20260901-15 fourth-draft review, which told this review to
  carry it out rather than leave it as the design's open question 5.
- Date: 2026-09-03 (UTC+0)
- Risk: low
- Conclusion: **Pass.**

## Check-details

| # | Task | Result |
|---|------|--------|
| 1 | `serve` accepts `--change-class` | **Pass** |
| 2 | Same parse, same shape | **Pass** — both commands through `_change_classes`, asserted against each other |
| 3 | The same operator turn | **Pass** — `by=authorised_by`, so the export says `operator` |
| 4 | A malformed one refuses at startup | **Pass** — `CliError` caught, printed, exit 2 |
| 5 | The drift guard's exemption set is empty | **Pass** |
| 6 | The mutation anchor is unique again | **Pass** — and `mutation_check --only voice` still reports **CAUGHT** |
| 7 | README follows the suite | **Pass** — 1964 → 1966 |
| — | Full suite | **Pass** — 1959 passed, 4 skipped, and the three reds resolved: the README count (fixed), the mutation anchor (re-anchored), and `test_concurrent_config_edits_and_model_adds_do_not_deadlock`, which passes in isolation |
| — | Ledger lint | **Pass** |

## Reservations (disclosed, not waived)

1. **Written by the implementer.**

2. **The drift guard caught my own change, twice.** Adding the flag turned
   `test_both_ways_to_start_a_run_take_the_same_governance` red — correctly, because its exemption
   list said *"no `--change-class` flag on the serve subparser"* and that had stopped being true.
   And copying `cmd_run`'s relaxation block into `cmd_serve` made a shipped mutation's anchor match
   twice, which `test_every_shipped_mutation_has_a_unique_anchor` refused. Both guards did what they
   were written for, on the same commit.

3. **A `standard` class on a console governs every walk it runs.** `run` declares one per
   invocation; `serve` declares one at startup and it stands for the session. That is the same scope
   `--risk` and `--undeclared` already have there, and it is a longer reach than the command line's
   — an operator who starts a console with a class and walks away has left it in force.

4. **`test_concurrent_config_edits_and_model_adds_do_not_deadlock` failed in the full run and passes
   alone.** It has done this before in this session, under load. Recorded rather than dismissed: it
   is a concurrency test and a flake in one is a fact about the test, not proof the code is fine.

5. **This closes CHG-20260901-15's open question 5 and nothing else about that design.** The design
   is still failed by four seats and vetoed.
