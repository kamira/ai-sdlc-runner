# ACC-20260827-07 - Acceptance: two refusals that fire, and no test that says so

- Target CHG: CHG-20260827-07
- Verifier: **self** (the session that built it). Permitted at this risk grade, and disclosed rather
  than dressed up: every change in this batch is low, and none touches the red lines, the gate
  matrix or the flow. The independent work that produced these changes was the acceptance round of
  2026-08-27 - two engines that had built none of the code they read. What follows is the
  implementer checking their own fix, which is worth exactly what it is worth.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** - tests only
- Conclusion: **Pass. Both refusals are now driven through their public entry points, in both directions.**

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_conversations.py tests/test_models.py -q -p no:randomly
```

Batch-wide: `pytest tests/ -q -rs` -> **1347 passed, 3 skipped**; `tools/mutation_check.py` -> **15 of 15 caught**;
`tools/ledger_check.py --repo .` -> passed, 86 changes.

## Check-details

| # | Check | Method | Result |
|---|---|---|---|
| 1 | A turn body carrying `seq` or `at` is refused | `test_a_turn_body_cannot_rewrite_its_own_envelope` | **Pass** - and the refusal **names** the field |
| 2 | `kind` is a different case, and it is asserted | same test | **Pass** - `turn(kind=...)` is a `TypeError` from Python itself; the body can never carry it through this API. Asserted rather than assumed, because it is why the loop covers two of three envelope names |
| 3 | A refused turn does not poison the conversation | write a good turn afterwards | **Pass** - `["opened", "note"]` |
| 4 | An unknown registry key is refused | `test_a_registry_with_an_unknown_top_level_key_is_refused` | **Pass** - the message names `modelz` **and** `models` |
| 5 | A correct envelope still loads | `test_a_registry_whose_envelope_is_correct_still_loads` | **Pass** - the over-refusal direction |

## Not closed by this record

Nothing. Both refusals were confirmed to fire by hand during the acceptance round; the difference
this makes is that they will be confirmed again on every run, by something that is not a person
remembering to check.
