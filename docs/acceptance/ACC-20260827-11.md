# ACC-20260827-11 - Acceptance: the defect log's own counts are the only figures nothing checks

- Target CHG: CHG-20260827-11
- Verifier: **self** (the session that built it). Permitted at this risk grade, and disclosed rather
  than dressed up: every change in this batch is low, and none touches the red lines, the gate
  matrix or the flow. The independent work that produced these changes was the acceptance round of
  2026-08-27 - two engines that had built none of the code they read. What follows is the
  implementer checking their own fix, which is worth exactly what it is worth.
- Date: 2026-08-27 (UTC+0)
- Risk: **low** - tests only
- Conclusion: **Pass, and it found a live miscount on its first run - the third this file has had.**

## Evidence commands

```
PYTHONUTF8=1 PYTHONPATH=src python -m pytest tests/test_documented_numbers.py -q -p no:randomly -k defect_log
```

Batch-wide: `pytest tests/ -q -rs` -> **1347 passed, 3 skipped**; `tools/mutation_check.py` -> **15 of 15 caught**;
`tools/ledger_check.py --repo .` -> passed, 86 changes.

## Check-details

| # | Check | Method | Result |
|---|---|---|---|
| 1 | The table sums to the total the file states | `test_the_defect_logs_table_sums_to_the_total_it_states` | **Pass** - 13+9+4+10+5+14+11+13 = **79**, and the file says 79 |
| 2 | Each group heading agrees with its table row | `test_every_defect_log_group_heading_agrees_with_its_row_in_the_table` | **Pass, after a correction** - see below |
| 3 | Quoted historical figures do not trip it | read the test | **Pass** - it reads the table and the headings, never the prose, so `"two of thirty-two"` is untouched |
| 4 | Counting `###` subsections was rejected | measured it | **Pass** - three groups carry more findings than subsections (14 vs 6, 11 vs 5, 13 vs 6), so the heading was never a subsection count |

## The miscount it found

`## Found in the fix for the last review - 12` sat above a table row reading **13**, with the
document's stated total (79) agreeing with the row. Two of three numbers agreed, so the heading was
the stale one; it now reads 13.

That is the **third** time this file has miscounted itself, and it quotes the previous two in its
own opening. The difference is that this one was found by a check rather than by a reader.

## The ambiguity it did not resolve, deliberately

One row disagrees in a way that cannot be settled from the document:

| | says |
|---|---|
| the table | `**4** (+1 intended collision, below)` |
| the heading | `3, plus one intended collision` |

Four-plus-one against three-plus-one. The closing summary says "the four suite-caught defects",
which points at 4; the sum-to-79 depends on 4; the section has 4 `###` subsections, which points at
3-plus-the-collision.

**I did not pick one.** Putting a guessed number into the file about wrong numbers is the failure
this whole change is about. The check asserts the qualifier survives in both places and skips the
equality, the test says why in its own comment, and the question is yours to settle.

## Not closed by this record

**Which of 3 and 4 is the count of defects the test suite found unprompted.** Named above; needs a
person who knows what the fifth item was.
