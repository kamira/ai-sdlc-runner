# CHG-20260827-20 — a change type assessed once should not be assessed every time

- Project: ai-sdlc-runner
- Branch: claude/chg-20260827-20-change-classes
- Date: 2026-08-27 (UTC+0)
- Type: one missing column in the gate table
- Proposed by: the decision to target a mid-to-large organisation shape, where `merge = confirm` on
  every change makes throughput a function of one person's availability
- Risk: **high** — this is the only change here that can make a gate stop less often. Everything
  about it has to be built so that relaxing is a recorded human act, never a default.
- Autonomy: high
- Related: CHG-20260823-01 (GATES)

## The gap

`merge` is `confirm` even at `low`. That is correct reasoning — merging is a one-way door — and it
means **every change needs a person**. At the scale now being targeted that is not a strict policy,
it is a policy that gets bypassed, which is the failure this repository already documented once
about the red-line word list.

## The shape borrowed from somewhere it already works

ITIL splits changes three ways, and the split is exactly what is missing here:

| class | what it means |
|---|---|
| **standard** | the *type* was assessed once by a person and pre-authorised; individual instances do not stop |
| **normal** | full approval, which is what every change is today |
| **emergency** | proceeds, and is reviewed after |

This does not weaken the axiom that governance is not a model's judgement. What a standard class
records is **a person's** judgement about a type, made once, written down, and revocable.

## The guards this needs to be safe

1. A standard class is declared by a person, never inferred, and never by a model.
2. The six permanent halts are **never** available to a standard class, at any grade.
3. Every standard-change run records which class let it through, and who authorised that class.
4. A class carries a review date. An unreviewed class expires back to `normal` rather than
   persisting on nobody's authority.
5. Emergency runs are marked and queued for review; nothing marks itself emergency.

## Tasks

| # | Task | Done-when | State |
|---|------|-----------|-------|
| 1 | Change class is data beside `GATES` | `policy.CLASSES`, three of them, asserted field by field | **[x]** |
| 2 | Declaring a standard class is an operator act | `--change-class CLASS:WHO:REVIEW_BY`, recorded as a `RELAXATION` turn; `plan.check` refuses the key so no model can reach it | **[x]** |
| 3 | Permanent halts ignore class entirely | tested for **all six** kinds × both relaxing classes, and end to end | **[x]** |
| 4 | Each run records the class it used | `change class:` on every run, and each relaxed gate named with what it would have been | **[x]** — report and turn; see the reservation about `export` |
| 5 | Classes expire | past the review date the run stops and says which date it passed | **[x]** |
| 6 | Emergency is marked and queued, never self-declared | reachable only from the command line; the plan key is refused | **[~]** — *marked*, not *queued*: see below |
| 7 | Every fix mutated | `tools/mutation_check.py --only classes` | **[x]** — 10/10 |

## The rule the record left open, and how it was decided

The proposal says a standard class means *"individual instances do not stop"*. Taken literally that
would dissolve a `halt` as readily as a `confirm`, and it should not.

**Decided: `confirm` → `auto`, and nothing else.** `confirm` means *a person is asked and may say
yes*, and pre-authorising a type is exactly saying yes in advance about every instance of it. `halt`
and `halt_independent` say something stronger — stop, and put this in front of somebody. A
pre-authorisation is a person's **approval**; it is not a person's **attention**, and dissolving a
halt would trade the second for the first with nobody noticing.

So `merge` at `low` relaxes, and `merge` at `high` still halts. The class changes who has to be
asked; it never changes who has to look.

## Task 6 is partly done, and the half that is missing is named

*"Emergency is marked and queued for review."* **Marked** is done: `emergency` is reachable only
from the command line, `plan.check` refuses the key outright, and the run records it. **Queued** is
not: there is no review queue in this runner, and inventing one under this record would be a second
feature wearing the first one's number. An emergency run is therefore recorded and visible, and
nothing chases it. Stated rather than counted as done.

## A defect this change made and the closed schema caught

`policy.verdict` carries a comment on its `autonomy` branch: *"Reported through `source` rather than
as an extra key: the verdict's shape is fixed so a work order's closed schema can carry it."* The
first draft of this change added `relaxed` and `change_class` keys to that same dict, in that same
function. `workorder.render` refused them.

The fix is better than the thing it replaced: the engine now asks what the verdict **would have
been without the class** and compares. "It would have been `confirm`" is the sentence an audit note
actually needs, and it is what the report prints.

## The permanent-halt message enumerated what cannot relax it, and the list went stale

*"No risk grade, confirmation or mode relaxes this"* — a change class is now a member of that family,
so the sentence had to grow. There were **two** copies and the second is split across source lines,
so a single-line search found only the first. Caught by a test asserting the message, then swept for
by rejoining continuations before searching again.

## Risk & rollback

- This is the one change that can let something through that used to stop. Tasks 2–5 exist to make
  that a decision somebody made and signed, with an expiry.
- With no classes declared, every change is `normal` — today's behaviour exactly.
- Rollback: revert, or delete the classes.

## Status

**Accepted** — see [ACC-20260827-20](../acceptance/ACC-20260827-20.md).

### Not claimed

**That a type stays safe because it was safe.** Task 5 is an admission that it does not. The expiry
is a floor under the drift, not a fix for it.

**That anyone will review classes on time.** An expiry that fires and is renewed without thought is
a rubber stamp with a calendar. This change makes the renewal visible; it cannot make it considered.
