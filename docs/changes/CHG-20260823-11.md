# CHG-20260823-11 — An operator console: front end, back end, and the three things the engine must learn

- Project: ai-sdlc-runner
- Branch: claude/chg-20260823-11
- Date: 2026-08-23 (UTC+0)
- Type: **design only** — a specification and a working mock-up. How much is built is the task
  table's State column, which is the single place this record says so.
- Proposed by: the user, across one working session, correcting the design four times as it went
- Risk: **high** — the specification changes how a gate stops (the governance core), and adds a
  second meaning to multi-model configuration
- Autonomy: high → **implementation is a separate confirm gate.** Design settled is not build
  authorised, and this record does not claim otherwise.
- Recurrence check: the *shape* of this record is a recurrence risk, not the content. CHG-20260822-04
  was design-only, said so, and then was never swept when the tasks landed — so for the length of
  the build it claimed both "all nine tasks built" and "nothing in `src/` has been written". Two
  verifiers found it. **The Status section below is the thing to keep honest.**
- Related: CHG-20260823-01 (the flow), -07 (resume), -09 (single-model disclosure), KN-11, KN-16

## Why there is a console at all

The runner works and cannot be operated. Running it today means writing a JSON plan by hand,
watching stdout, and answering a gate by re-running the whole command with `--confirm`. Three things
the requirement asks for have no surface at all:

| The requirement | Today |
|---|---|
| 使用者輸入指令 | there is no input — you write `plan.json` |
| PM 確認方案 | answered by the dispatched agent, never by a person |
| 在 GUI 上設定 | `runner settings` is a curses menu for two fields |

An end-to-end run was done by hand to establish that the flow itself is sound before designing a
surface for it: 19 nodes, 14 asks, stopped at `merge`, resumed after a killed session re-asking only
the pending question, and refused a `git push origin +main:main` declared `ordinary`. The engine is
not what is missing.

**The mock-up:** `docs/design/console-mockup.html` (also at
https://claude.ai/code/artifact/2e387693-6720-4d06-a803-62cfc8e8d0a7) — interactive, on the real 10
gates, which are value-for-value identical to `policy.GATES`, and the 4 seats, of which the **names
and veto flags** match the code and the **questions are paraphrases**.

> **Corrected again in review round 2.** The round-1 correction said the gates and the seats were
> *both* "value-for-value identical". The gates are; the seats' questions are not. The sentence
> written to fix an overclaim about fidelity to the code contained a smaller one of the same
> species. Recording that here rather than quietly fixing it, because the pattern is the finding.

> **Corrected in review round 1.** This said "built on the real 23 nodes". It is **18 of the 23**,
> plus one node (`next_module2`) that does not exist in `graph.py`. The five it omits are
> `fix_pass`, `re_review`, `halt_second_fail`, `review_failed` and `acceptance_failed` — every one
> of them a failure path. So the mock-up is the happy path, and the paths it leaves out are exactly
> the ones an operator most needs a console for. Found by fable-seat; confirmed by comparing the
> mock-up's node ids against `graph.py` directly.

## The screen

Four things, and deliberately nothing else on the main screen:

1. **An instruction box.** Free text, plus **attachments** — a spec, a flow mock-up, a screenshot.
   Attachments become `input_artifacts` on **every** node's work order, so the reviewer reads the
   same spec the engineer built from.
2. **Where the run is.** Six phases (Plan → Assess → Build → Review → Verify → Ship), the current
   one lit, the exact node id beside it, one word of state: running / waiting / stopped / done.
3. **The whole flow**, every node, each showing its role and which model will answer it.
4. **The agent log** — the main content. Every ask: who, which model, via CLI or API, how long, what
   it was asked, what it answered. **Agents, the sub-agents they dispatch, and every model voice are
   visually distinct**, because "an agent said so" and "the engineer the lead dispatched said so" are
   different claims.

Configuration lives on separate tabs: Project, Models, Node → models, Review panel.

## The four design decisions, and what each was corrected from

### 1 · Multi-model means two different things, and the node decides which

Initially every multi-model node was a panel. The user corrected it: a **coding** node has a
main → sub structure, and several models on the sub are **the pool the main may dispatch to** — one
of them does the work. Calling that a vote would have been wrong.

| Node kind | N models means | At run time |
|---|---|---|
| verdict (`lead_task_review`, `qa_accept`, `pm_confirm`…) | **N voices, adjudicated** | N sessions, blind to each other; veto → majority → tie |
| dispatch (`engineer_build`, sub of `lead`) | **a pool** | the main picks one **at random**; one does the work |
| follows (`engineer_selfverify`) | not selectable | whichever model built it |

**Random dispatch is deliberate.** Any cleverer rule is the runner deciding which model is better at
a task it has not seen, and it would quietly become a ranking nobody wrote down.

### 2 · A tie is not a failure — it is nobody deciding

Currently `policy.adjudicate` treats a tie as fail. The user's decision: a tie is **undecided**, and
undecided is a person's job. The console stops and asks, in short plain sentences:

> 2 said yes, 2 said no. Nobody has the casting vote, so the runner will not pick one. This is
> yours to call.

Both sides are shown with the reason each gave and which model gave it. Whichever the person picks
is recorded as their decision.

### 3 · An undecided panel may be re-run, and the re-run carries both sides

Configurable: on/off, and a limit. Each round is fresh sessions — but from round 2 every voice is
told **what every reviewer said last round, for and against, and which model said it.**

That was corrected twice. First it was a blind re-roll, which is close to useless: nothing new enters
the room. Then it carried only the objections — which the user identified as a thumb on the scale, a
reviewer shown three reasons to fail and none to pass is not being asked to weigh anything.

**The cost is anchoring** and it is accepted knowingly: a voice told codex objected may defer to
codex. The argument for accepting it is that a panel which ties twice is short of *information*, not
of independence — the two sides never saw each other's reasoning.

> **Qualified in review round 1.** That last sentence is an argument, not a finding, and codex-seat
> is right that it is unsubstantiated: a repeated tie can equally be a genuine value disagreement, a
> correlated blind spot, or a stable split. It is why the author accepted the cost — it is not
> evidence that the cost is worth paying.
>
> **And this section does not apply to the review seats.** The re-run switch was placed on
> `/config/panel`, which is the seats' tab, and nothing here excluded them. Both seats objected. Each
> seat answers a *different* question, so carrying every seat's reasons to every seat is
> cross-question contamination, not a second opinion; and seat independence is a stated invariant of
> the engine rather than a setting (`engine.py:16`). Round 2 carry is for a multi-model panel on one
> question. The seats re-run blind or go to a person.

The limit still ends at a person, now for a sharper reason: if they have read each other and still
cannot agree, another round will not help.

### 4 · A send-back leads with a summary and appends the originals

The lead writes it — it is the one dispatching the rework, so it is a real ask on the lead's model,
logged like any other. Three objections become three short items.

**The originals are appended to the same work order, below the brief, marked reference rather than
instruction.** Not behind an API call: an appendix nobody has to fetch is one they will not read at
the moment the summary looks wrong. Summarising is where an objection gets softened, and this
repository's entire recorded history is disagreement being flattened.

## The split

**Front end — any language.** Draws the flow, the position, the log. Collects the instruction, the
configuration and the gate answers. Holds **no** governance: never decides a verdict, never
adjudicates, never skips a gate. Closing the tab does not stop a run.

**Back end — the existing runner.** Owns every decision, halt, adjudication, retry and record. Bound
to **one project**. Holds the API keys — the registry stores an environment variable *name*, never a
secret, so a key cannot reach a screenshot or a bug report.

> **Corrected in review round 1.** This claimed `policy.py` and `graph.py` were **unchanged**. It was
> false eleven lines above this record's own "Plus one policy change: `adjudicate` gains an
> `undecided` outcome", and false again for `graph.py`, which is the only honest home for the
> pool/panel distinction (task 12). Both seats found the contradiction independently. A reader
> reconciling this record against itself failed before reaching the repository.

### The three things the engine must learn

| # | Change | Where | Why it is not cosmetic |
|---|---|---|---|
| 1 | **A gate suspends and the caller resumes.** `walk` returns a report that says *suspended* — distinguishable from finished — and a resume re-enters carrying exactly one recorded decision. | `engine.walk` | This is the governance core. It is the one change that could break the halt guarantee, and it is why this record is high risk. |
| 2 | **Retries live in the dispatcher.** An attempt that never answers is a lost session — already handled. Retrying is the backend's job, and it is logged, so a run that took four tries does not read like one that took one. | `cli._Process` / the server's dispatcher | Not the engine's business; putting it there would make "one ask, one session" untrue. |
| 3 | **Any node can be a panel** — but **not** by the same call. `policy.adjudicate` *refuses* a set of voices that are not seats: `raise PolicyError(f"unknown seat(s): …")` for anything outside `BY_SEAT` (`policy.py:649-651`), and veto is a per-seat property (`policy.py:655`). Model voices have neither. | `engine.walk`, `_adjudicate`, `policy.adjudicate` | **Corrected in review round 2.** This said "the same call with a different set of voices", and that the change was "a generalisation of code that exists, not new governance — which is the only reason it is acceptable at all." Today that call is a `PolicyError`. So this is **not** a generalisation of existing code, and the stated reason it was acceptable does not hold. Somebody must decide whether a model voice can veto. → task 20. |

Plus one policy change: **`adjudicate` gains an `undecided` outcome** where it currently returns
fail on a tie.

### Endpoints

`GET/PUT /project` · `GET/POST /models` · `DELETE /models/:id` (refused while a node selects it) ·
`POST /models/:id/test` · `GET/PUT /config/nodes` · `GET/PUT /config/panel` · `GET /flow` ·
`POST /attachments` · `POST /run` · `GET /run/events` (SSE) · `POST /run/gate` ·
`POST /run/decide` (break a tie) · `POST /run/resume` · `GET /run/objections/:node`

## Tasks

**How many are done is the State column below, and nowhere else.** This record originally asserted
"nothing is written" in four separate places — the Type field, this header, Rollback, and Status.
That is four things to sweep and four chances to leave one stale, in a record whose whole purpose was
to not repeat CHG-20260822-04's stale claim. fable-seat named the multiplication; the other three
copies now defer to this column.

| # | Task | Done-when | State |
|---|------|-----------|-------|
| 1 | A gate suspends; the caller resumes | `walk` returns a report that is **distinguishable from finished** — which today it is not, since a TERMINAL node sets `halted_at` too (`engine.py:719-721`) — carrying run id, node, gate, phase and verdict; a resume consumes exactly one recorded decision; duplicate, stale, wrong-run and wrong-gate answers are refused. **Two things must be written down before this task starts** (below the table) | **[ ]** |
| 2 | `adjudicate` gains `undecided` | a tie returns undecided, not fail — **and `engine._adjudicate` stops collapsing every non-pass to `"fail"` (`engine.py:572`) in the same task**; an unknown outcome raises rather than falling through | **[ ]** |
| 3 | Re-run an undecided panel | configurable count; round 2+ carries both sides attributed; the limit ends at a person — **and the carry never reaches a panel whose voices are review seats: a seat panel re-runs blind or blocks for a person, and the setting moves off `/config/panel`** | **[ ]** |
| 4 | Multi-model on any verdict node | N voices, N sessions, adjudicated by **the generalised rule task 20 defines** — not "the existing rule", which raises `PolicyError` on any voice that is not a seat — and a node whose configured model count does not match the sessions actually opened is **refused**, not quietly asked once | **[ ]** |
| 5 | Dispatch pool on `engineer_build` | main picks at random; `engineer_selfverify` follows the same model | **[ ]** |
| 6 | Send-back brief + verbatim appendix | the work order leads with the summary and carries the originals below it | **[ ]** |
| 7 | Retries in the dispatcher, logged | exhausted retries leave the question pending, as today | **[ ]** |
| 8 | The model registry | vendor, id, CLI command or API endpoint + key env var; a key never leaves the server | **[ ]** |
| 9 | Attachments | stored in the project, added to `input_artifacts` on every work order | **[ ]** |
| 10 | The HTTP layer and the SSE stream | the endpoints above; a reload mid-run rebuilds the view | **[ ]** |
| 11 | The front end | the four-part screen and the four configuration tabs — **and it renders and can operate the five failure nodes the mock-up omits** (`fix_pass`, `re_review`, `halt_second_fail`, `review_failed`, `acceptance_failed`) — the screen requirement above already says "the whole flow, every node", so there is no opt-out clause here | **[ ]** |

### Added by review rounds 1 and 2

Tasks 1–11 described behaviour without adding the data or the routes that decide it. Without tasks 12
and 13 in particular, tasks 4 and 5 can only be built on name-inference — and this repository merged
**"a name is evidence only if it constrains what happens"** (CHG-20260823-10) three commits before
this record was written.

> **Corrected in review round 2.** This paragraph said "**both** seats independently found" and then
> attributed tasks 16–19 to one seat each, four lines below. Two parts of the same record
> contradicting each other about who found what — in the section added to fix a contradiction. Which
> seat found which is now only in the attribution list, and the seats' full verdicts are committed at
> `docs/design/reviews/` so the attribution can be checked against the source rather than trusted.

The per-task attribution is in **Review rounds** below.

| # | Task | Done-when | State |
|---|------|-----------|-------|
| 12 | **The execution mode is node data** | `graph.Node` carries `mode` from the closed set `runner` / `single` / `seat_panel` / `model_panel` / `pool` / `follows`; **all 23 nodes are assigned** (9 runner, 5 single, 1 seat_panel, 5 model_panel, 1 pool, 2 follows); `main` is a **role**, `follows` is a **node id**, each with its reason written next to it; `graph.validate()` enforces eight named cross-rules; the walk decides a panel from `mode`, not `role`. 20 tests. | **[x]** |
**checks** that role and mode agree, which is the opposite operation and is why `role` belongs in
both sentences | **[ ]** |
| 13 | **`undecided` has a route through the walk** | `lead_review` branches on `pass`/`fail` only (`graph.py:114-118`); a tie is **not a gate** and has no gate name for `confirmed` to spend, so neither task 1 nor task 2 wires it. This task owns: block, record the person's decision, take the branch they chose | **[ ]** |
| 14 | **A run-state snapshot** | task 10's "a reload mid-run rebuilds the view" has no endpoint behind it — the list carries only the event stream. A reconnecting browser that missed the blocked event can tell waiting from running | **[ ]** |
| 15 | **Identity behind a decision** | the server issues an operator credential and **derives** the identity from it; a name in a request body is refused. `halt_independent` compares that derived identity against the recorded builder and refuses a match. **A client-supplied name does not satisfy this task** — that is the button caption moved one layer down and called enforcement | **[ ]** |
| 16 | **Idempotency on every decision endpoint** | duplicate clicks, stale tabs, wrong run/node/gate and two browsers deciding at once cannot advance the run twice | **[ ]** |
| 17 | **Attachment paths are kept out of the target scanner** | task 9 puts attachments on **every** node's `input_artifacts`, which is a `_TARGET_FIELDS` member (`engine.py:385`) read by `_spoken_halt` through `policy.derive`. `policy.py:219` matches a bare `prod` or `production` **path segment**, so an attachment stored at `…/production/spec.pdf` raises a **permanent halt on every node of every run** — unrelaxable by design. **Both directions must survive**: an ordinary attachment stored at `…/production/spec.pdf` must not halt, *and* a brief that genuinely names a production target must still halt. A fix that exempts `input_artifacts` from the scan satisfies the first and breaks the second | **[ ]** |
| 18 | **Rejection routing is graph governance** | the mock-up's gate offers "Reject — send back" and sends it to `pm_plan`. `graph.py` has no such edge and not every gate has that branch. Either the edge exists in the graph or the button does not exist | **[ ]** |
| 19 | **Attachment validation** | project-contained storage, filename and path handling, byte limit, allowed types, **content-addressed immutable identity**, and defined behaviour when an attachment changes or disappears mid-run | **[ ]** |
| 20 | **Adjudication for voices that are not seats** | `policy.adjudicate` raises on any voice outside `BY_SEAT` (`policy.py:649-651`) and reads veto off a seat property (`policy.py:655`). This task decides and implements: **can a model voice veto, or is a model panel majority-only?** Until it lands, task 4's "adjudicated" names nothing that exists | **[ ]** |

**Two questions task 1 must answer in writing before it starts** — both raised by fable-seat in round
2, neither invented by this record:

1. **Is a resumed gate decision a spend of the existing `confirmations` mechanism, or a second
   mechanism beside it?** The engine already counts approvals per gate, decrements them at a stop,
   and reports the unspent ones (`engine.py:631-644`, `670-673`, `575-591`). Task 1 introduces a
   second approval path. Two overlapping approval mechanisms with slightly different audit semantics
   is the quiet-governance-bug shape this repository keeps documenting itself shipping.
2. **Who mints the run id?** "Wrong-run answers are refused" presumes one, and no run-id concept
   exists in `RunReport` or `RunConfig`. The journal directory is currently the only durable run
   identity. If the record does not decide this, the builder decides governance.

## Risk & rollback

- **Task 1 is the dangerous one.** Every halt in this system currently works by **returning**: `_gate`
  sets `halted_at` and returns the report through `_finish` (`engine.py:674-678`), `walk` returns it,
  and the CLI exits. Nothing after the halt can execute. Making it *wait* introduces a state where the
  run is alive and stopped — and "alive and stopped" is where a bug can look like "alive and
  continuing".

> **Corrected in review round 1.** This said halts work "by ending the process". They do not — the
> engine returns a halted `RunReport`; it is the **CLI** that exits. Both seats corrected it, and the
> correction cuts against the design: because a stop is already a *return*, the safer shape — a
> suspended report the caller resumes — is a **smaller** change than this record assumed. The record
> graded itself high risk partly on a property the engine does not have.
>
> **Narrowed in review round 2**, twice. First, "its machinery already exists and is tested" was
> overstated: `RunConfig.resume` and `AskJournal` resume **asks**. Nothing records a gate *decision*,
> refuses a duplicate or stale one, or gives a run an identity — `RunReport` and `RunConfig` have no
> run-id concept at all. Second, and worse for this design: **a finished run sets `halted_at` too**
> (`engine.py:719-721`, at every TERMINAL node including `done`), so *halted and finished are
> currently the same shape*. Task 1's "distinguishable from finished" is therefore not a nicety — it
> is the property that does not exist yet, and the one everything else rests on.
>
> **And the convergence was primed.** The record reported "both seats named the same safer
> alternative" as independent agreement. The round-1 brief handed both of them that alternative as
> its worked example — `docs/design/review-round-1-brief.md`, Q2, verbatim: *"e.g. the walk
> returning a suspended state the caller resumes"*. Two primed reviewers agreeing with the primer is evidence about
> the primer. The shape still looks right on the engine evidence — but it is my proposal that two
> seats did not reject, which is a weaker claim than the one I made.
- **Task 2 changes an existing rule.** `policy.adjudicate` returns fail on a tie today and there are
  tests asserting it. They are not wrong; the rule is being changed deliberately, and the tests must
  change with a reason written next to them, not because they turned red.
- **Task 4 is where a design error would be quiet.** A verdict node silently asking one model when
  the operator configured three is the exact class of failure this repo has shipped four times.
- Rollback: whatever the task table's State column says is unbuilt needs none.

## Status

**草稿 / draft — reviewed three times, did not pass any of them. 1 of 20 tasks built.** How much is
built is the task table's State column. All three rounds split, and a tie does not pass. The seats'
own verdicts are at `docs/design/reviews/`, so the summaries below can be checked against the source
rather than trusted.

Round 3 is where the reviewing stopped: **both seats independently answered `NEXT: build task 12`**,
the first thing they have agreed on. Task 12 is built.

The mock-up is a front end with no back end: it simulates the flow in the browser so the interaction
could be judged before anything was built. It is not connected to the runner and does not pretend to
be — and per the correction above, it simulates 18 of the 23 nodes, all of them on the happy path.

### Review round 1 — the design does not pass

Two seats, same brief, neither seeing the other, against a frozen tree at `73f4032`.

| Seat | Verdict | Required before task 1 |
|---|---|---|
| codex-seat | **DESIGN: not sound** | 7 changes |
| fable-seat | **DESIGN: sound with changes** | 9 changes |

**Outcome: does not pass.** Two seats, split, no casting vote — the repository's own rule is that a
tie does not pass, and it applies to its own design record or it is not a rule. Neither verdict is
averaged into the other and neither is picked as the winner. What follows separates what both found
from what one found, because those are different strengths of claim.

### Found by both, independently

1. **`engine.py:572` collapses everything.** `return "pass" if outcome["outcome"] == "pass" else
   "fail"`. Adding `undecided` to `policy.adjudicate` and stopping there leaves the engine silently
   flattening it to `fail` and routing the work back down the fail branch — an automated decision,
   where the entire point of `undecided` is that **nobody decided**. Task 2 as originally written
   would have gone green, changed no behaviour, and looked done. That is this repository's signature
   defect — a coarse check answering for something it never examined — arriving *before* the code was
   written this time, which is the first time that has happened here.
2. **The pool/panel distinction is not decidable from the node data.** `graph.Node` carries no
   execution multiplicity. `engineer_build` (pool) and `engineer_selfverify` (follows) are both
   `role="engineer"`, kind `STEP`, no branches. The mock-up had to **invent** `kind`, `main` and
   `follows` fields, and every behavioural branch in it keys off the invented field. Task 5 as
   written ("Dispatch pool on `engineer_build`") keys behaviour off a node's **name** — the exact
   thing CHG-20260823-10 was merged to stop. → task 12.
3. **A node configured with three models can silently be asked once.** The engine has no concept of a
   node's model count; the non-seat path opens exactly one session per node, and the only diversity
   disclosure runs solely on the `seat` path. Task 4's done-when described the happy path and left
   the failure mode unpinned — in the one place it would have bound. → tightened.
4. **Identity is missing, so `halt_independent` is unenforceable.** The design promises decisions
   "recorded with who decided", but no endpoint establishes who anyone is. The verifier-independence
   rule is enforced by a button caption. → task 15.
5. **The `Approver` shape is the wrong primary state.** Both seats named the same safer alternative:
   the walk returns a suspended report the caller resumes. See the corrected risk note above — the
   argument for `Approver` rested on a property the engine does not have.
6. **The seat panel is conflated with an ordinary panel.** The re-run carry is configured on
   `/config/panel` — the seats' tab — and §3 never excludes them. Two facts make that wrong: each
   seat answers a **different question**, so carrying every seat's reasons to every seat is
   cross-question contamination rather than a second opinion; and seat independence is a stated
   invariant, not a tunable (`engine.py:16`: "The count is the user's to set; the independence is
   not"). §3 quietly makes it configurable from round 2.

### Found by one seat

- **fable-seat — an attachment filename can permanently halt every run.** Task 9 puts attachments on
  every node's `input_artifacts`, which is scanned for red lines; `policy.py:219` matches a bare
  `prod`/`production` path segment. A spec stored at `…/production/spec.pdf` raises an unrelaxable
  permanent halt on every node of every run. Verified. Neither I nor the mock-up had considered that
  the attachment feature feeds the safety scanner. → task 17.
- **fable-seat — the record contradicted itself**, claimed the mock-up was "the real 23 nodes" when it
  is 18 plus one invented, and asserted design-only in four places. All three corrected above.
- **codex-seat — no idempotency or race handling** on the decision endpoints; two browsers or one
  double-click can advance a run twice. → task 16.
- **codex-seat — rejection routing is not graph governance.** The mock-up's "send back" goes to
  `pm_plan`; `graph.py` has no such edge. → task 18.
- **codex-seat — the record understates what it added**: commit `19e34ea` also added
  `docs/design/README.md`. True.
- **codex-seat — "a tie means the panel is short of information" is not substantiated.** A repeated
  tie can equally be a genuine value disagreement or a correlated blind spot. The claim stays in §3
  as the *reason the author accepted the anchoring cost*, but it is an argument, not a finding, and
  it is now labelled as one.

### Review round 2 — the corrections do not pass either

Same two seats, same freeze rule, against `5aed9b0`. They were told the record now contains its own
round-1 review, that this anchors them, and to judge the corrections rather than defer to them.

| Seat | Verdict | Required before task 1 |
|---|---|---|
| codex-seat | **ROUND 2: fail** | 6 changes |
| fable-seat | **ROUND 2: pass with changes** | 7 changes |

**Outcome: does not pass.** Split again. Both seats' full verdicts for both rounds are committed at
`docs/design/reviews/`, which is itself one of round 2's required changes — see below.

**Three of the nine round-1 corrections were defective, and two of those are the same disease the
correction was treating:**

1. **I fixed two of three places.** The corrected risk note and task 1 both said *suspend and resume*
   while the engine-changes table still said *a new `Approver` … the server one that waits*. Two
   incompatible control-flow models in one document — the exact defect round 1 caught, reintroduced
   by the fix for it. codex-seat.
2. **I overstated the attribution.** "**Both** seats independently found…" introduced tasks 12–19,
   and four lines later tasks 16–19 were attributed to one seat each. codex-seat.
3. **The correction to an overclaim contained a smaller overclaim.** "Value-for-value identical to
   `policy.GATES` and the seat definitions" — the gates are; the seats' *questions* are paraphrases.
   fable-seat.

**Two corrections were honest and changed nothing about what the design does** — fable-seat's
sharpest structural point. The mock-up's missing failure paths were named as "exactly the ones an
operator most needs a console for" and then no task required covering them; the seat exclusion was
written into prose that no test will ever read while task 3's done-when stayed satisfiable by an
implementation that carries to the seats. *A correction that names a consequence and produces no task
has answered the misstatement, not the finding.* Both now have done-whens.

**The finding that undercuts the design's stated justification** (fable-seat, missed by both seats in
round 1 and by me): `policy.adjudicate` **raises `PolicyError`** on any voice outside `BY_SEAT`
(`policy.py:649-651`), and veto is a per-seat property (`policy.py:655`). The record's reason that
"any node can be a panel" was acceptable at all was that it is *"a generalisation of code that
exists, not new governance"*. It is not a generalisation — it is a crash — and generalising it means
deciding whether a model voice can veto, which nobody had decided. → task 20.

**And the convergence I reported was primed.** The round-1 brief handed both seats the suspend/resume
alternative as its worked example, and I then reported their agreement as independent convergence.
fable-seat checked the brief. That is a methodological error in how I ran the panel, not a factual
one in the record, and it is the one I would not have caught myself — which is the argument for the
panel.

**What round 2 did not overturn:** every line-level code claim in the round-1 corrections was checked
and held. Both seats agreed the tie rule was applied correctly to the record's own review.
**fable-seat** called the "Not claimed" section honest; codex-seat did not address it — its
"internally honest" covers the split-verdict handling only
(`docs/design/reviews/round-2-codex-seat.md:82`).

### Review round 3 — fail, and both seats said stop reviewing

| Seat | Verdict | Next |
|---|---|---|
| codex-seat | **ROUND 3: fail** | **build task 12** |
| fable-seat | **ROUND 3: fail** | **build task 12** |

**The first agreement in three rounds, and it is agreement to stop.** Round 3 was the test this
record wrote for itself: *is prose review still producing new information, or has it started
producing new defects at roughly the rate it removes them?*

**Round 3 found four more defects, all introduced by round 2's corrections, and zero new design
findings.** Every one is mine:

1. **A false citation inside the paragraph about evidence-chain integrity.** It said
   `review-round-2-brief.md` quotes the primed alternative. It does not — the only source is
   `review-round-1-brief.md`, Q2. fable-seat read both briefs. Now quoted directly instead of
   pointed at.
2. **An attribution overclaim inside the round that corrected an attribution overclaim.** "Both
   agreed the 'Not claimed' section is honest" — only fable-seat said that; codex-seat never
   addressed the section. Its committed verdict is the proof, which is what committing them was for.
3. **I silently dropped `role` from task 12's inference-refusal clause.** Round 1 wrote "id, **role**
   or branch shape"; round 2 wrote "id or branch shape", with no reason anywhere, **in a commit whose
   message said "tightened"**. The engine keys the whole seat path off `role`, so under the narrowed
   clause a decorative `mode` field with role-keyed dispatch would have violated nothing.
4. **Task 11 got an escape hatch from the fix for escape hatches** — "…or this record says in writing
   why the console ships happy-path-only". A paragraph satisfied it. Added while fixing "a correction
   that names a consequence and produces no task". Struck; the screen requirement already says "the
   whole flow, every node", so there was never anything to opt out of.

All four are corrected above. **The pattern is the finding**: round 1's defects were about the
design, round 2's about the corrections, round 3's about the corrections to the corrections. The
record had gone two meta-layers deep and each layer was minting its own errors, while everything
still genuinely open was a **decision** rather than a discovery — and a fourth prose round cannot
validate a decision, only re-describe the absence of one.

**fable-seat also checked the brief for steering, unprompted**, and found it: the round-3 brief led
with the record's own escape clause, framed the round as "that test", and set a trigger condition
observable by no seat — only by me aggregating both — which put the declaration of the outcome back
in the corrected author's hands, and "build" is the outcome that ends review pressure on my record.
It said so plainly and then answered on the defect trajectory it had verified itself. That is the
second time a seat has caught me steering a panel, and neither time would I have caught it.

**Where they disagreed:** codex named the highest-cost error as a wrong execution-mode contract
(it propagates into graph, engine, policy, API, config and front end). fable named task 1's
`confirmations`-vs-second-ledger question, on the ground that every other open item **fails loud** —
an incompatible graph fails `validate()`, a decorative mode field is visible in a diff — while two
overlapping approval ledgers **fail silent**, and silent is where the halt guarantee degrades without
anyone seeing it. Both are recorded; neither is averaged. fable's argument is the stronger one and
the record now treats it as the thing to decide before task 1.

### Task 12, built — every deferred decision made

Round 2 tightened task 12 by demanding decisions; round 3 found it had **converted missing decisions
into demands that someone else decide them** — "referent type is stated" while stating none, "every
node is assigned a mode" while assigning none. Building it meant answering, not restating:

| Question | Answer | Why |
|---|---|---|
| Referent of `main` | a **role** | no node represents "the lead handing out work"; the dispatching is part of the build node |
| Referent of `follows` | a **node id** | roles are not unique — three nodes are `role="lead"` — so "verify what *this* build produced" cannot be said with a role |
| Nodes that ask nobody | a sixth mode, `runner` | "nobody is asked" and "one model is asked" are different facts. Folding them into `single` would make `single` mean *unclassified* — and `single` is the dataclass default, so every unconsidered node would land there silently |
| Seat panel vs model panel | separate modes | the seats' independence is not configurable; a model panel's is |
| Does mode claim how many models are configured? | **no** — it says how to *read* the list | a `model_panel` with one model is one voice; a `single` with three is a configuration error, not a panel |
| May a work-producing node be a model panel? | **refused by `validate()`** | several candidate outputs and no rule for choosing. Still unsettled, so the validator refuses rather than the record inventing |

**The distinction that let `role` go back into the refusal clause:** `validate()` *checks* that role
and mode agree (`role is None` ⟺ `mode == runner`; `role == "seat"` ⟺ `mode == seat_panel`).
Checking is the opposite of inferring — inference lets one fact silently do two jobs, checking makes
a disagreement a build error. So the clause refuses inference from role **and** the validator reads
role, without contradiction.

**And the mode is not decoration.** `engine.walk` now decides a panel from `graph.SEAT_PANEL`, not
`node.role == "seat"`. The two agree on every node today — which is exactly why the switch had to
happen now, and why one of the 20 tests reads the walk's source and fails if anyone puts the role
back. A field the engine never consults is the decorative data an independent seat predicted.

### What is settled

Decisions 1, 2 and 4 stand — no seat in either round proposed replacing them. Decision 3 (the re-run
carry) stands **only for a multi-model panel answering one question**; its application to the review
seats is struck, and task 3's done-when now says so rather than the prose alone.

### What is not settled

- **The pool's retry behaviour.** If a dispatched model fails its retries, does the main pick a
  different one from the pool, or does the question stay pending on the same one? Not decided.
- **A work-producing node with several models and no main/sub structure** (`pm_plan`, `qa_verify`,
  `pr`) would mean several candidate outputs and no rule for choosing. The mock-up flags it rather
  than inventing a rule.
- **Whether the console should show a run it did not start.** One project, one runner — but two
  browsers on the same runner is not addressed.

Two of the three are now narrower rather than open: **the pool's retry behaviour** and **a
work-producing node with several models** both become decidable once task 12 puts the execution mode
in the node data, because there is then a place for the answer to live. **Two browsers on one runner**
stopped being a display question and became task 16 — codex-seat's point is that concurrency has to
be handled before the display policy is settled, not after.

### Not claimed

**Acceptance.** Nothing to accept: no code. When implementation begins it needs its own confirm gate
under the `Autonomy` field above, and a task is unticked until its done-when is met and
**demonstrated** — not when it is written.

**That round 2's corrections are sound.** Round 1 said the design does not pass; round 2 said the
corrections did not pass either, and found that three of nine were defective — two of them
reproducing the disease they were treating. The same sentence therefore applies again and it is not a
formality: these corrections have not been reviewed. The verdict on record is **does not pass**.

Whether this converges is an open question, not an assumption. Two rounds have both split, and round
2's failures were *caused by* round 1's fixes. If round 3 splits on defects introduced by round 2, the
honest reading is that the record is being polished rather than fixed, and the next move is to build
task 12 — the smallest load-bearing piece — and let running code settle what prose cannot.

**The hand-run.** "19 nodes, 14 asks, stopped at `merge`, resumed after a killed session, refused a
`git push origin +main:main`" is uncorroborated — no journal, transcript or report of it is
committed. Both seats flagged it. It stays in the record because it is true and it is why this design
exists, but as evidence for "the engine is not what is missing" it rests on the author's word, and
that is now stated rather than implied.
