# CHG-20260903-24 — a gate arrives pre-answered

- Project: ai-sdlc-runner
- Branch: claude/chg-24-withdrawn
- Risk: **none, now** — nothing is built. It was medium while it was a proposal.
- Date: 2026-09-04 (UTC+0) · **seventh draft, and the last** — withdrawn on the converging
  decision of four seats over six rounds
- **Measured against `origin/main` at `cb7d41f`**, pinned by sha and by string
- Type: new capability — **withdrawn**
- Proposed by: the operator — 「改成預先答好、人按一下」 — and split out of CHG-20260901-15 on the
  independent recommendation of two seats
- Outcome: **not built.** What it found is kept; what it proposed to build already exists.

## Why it is withdrawn

The risk seat said it plainly in the sixth round and the other three measured the same facts from
their own angles. Three of them are decisive and none is about the design being *wrong*:

### The render already exists, on both surfaces, node-attributed

```
cli.py     print(f"panel:         {decision['node_id']} → {said} ({seat_line})")
console    block("adjudicated", ...) → node_id + " -> " + said + " (" + reason + ") [" + voices + "]"
```

Both loop every adjudication, both print `grade || outcome`, both name the voices, and **both carry
the node id** — the one property `report.adjudications[-1]` discards. What this record would add is
**adjacency**: moving the relevant line into the *"Waiting for you"* box.

### The bound admits the two cells the record spent two sections protecting

Task 9 was *"shown only where the verdict is `confirm`"* — the risk seat's own correction, adopted.
Measured:

```
cells the bound admits   5   plan_confirmed@medium · feasibility_confirmed@medium
                             before_dispatch@medium · pr@high · merge@low
cells the record states  3
unstated and admitted    2   pr@high · merge@low
```

Task 10 was withdrawn so that no model speaks at `merge` before the person does. **Task 10 removed
the dispatch; the render was never bounded to the node.** At `merge`, `adjudications[-1]` is
`qa_accept`'s panel — a different node's judgement about acceptance, displayed as the one-way
door's pre-answer.

### On a default install it delivers nothing

```
pre-answers shown at the three stated cells
  node_models = {}         0    single-ask path, no adjudications
  one model per panel      0    same path
  >= 2 models per panel    3
```

Which is verbatim the defect this record's own first section convicts its **first** draft of:
*"Zero of eight stops carried a pre-answer. The deliverable was a sentence and an exit code."*

## What is kept, and where it goes

- **「可以依節點 by pass」 is owned by neither record, on either branch.** `-25` task 2 forbids
  naming nodes, so its tasks 12 and 13 cannot deliver it whether `-25` is accepted or refused. That
  is the operator's own requirement and it now has no owner — stated here so the next round starts
  from it rather than rediscovering it.
- **The sweep's *made-incomplete* list** — `ARCHITECTURE.md`'s legend and `README.md`'s *"not from a
  model's judgement"* — carries over to `-25`, which already keeps that distinction.
- **The adjacency** is filed as an ordinary console/terminal defect: the *"Waiting for you"* box
  renders `reason` and a button and no adjudication, while `drawDecisions` two blocks below renders
  every one of them. That is the shape `CHG-20260903-48` and `-49` already used for this surface,
  twice, in the last two days.

## What this record cost, and what it bought

Six rounds, four seats, seven drafts. It built nothing. It produced:

```
changes shipped while this record was under review     30   CHG-20260903-26 .. CHG-20260904-06
                                                            counted by commit subject, not by
                                                            mentions — a grep for the id finds 35,
                                                            five of which are cross-references
its own central artefact, withdrawn                      1
```

Almost every one of those came from a seat reading this design record against the program and
finding something else wrong, and **none** was found by the suite, which stands at 2113 tests. The record's value was as an instrument, not as a proposal — which
is worth writing down, because a record that closes without building is otherwise read as waste.

## The failure that recurred in this record's own drafting, six times

| draft | the claim | what was measured |
|---|---|---|
| 1 | falsifies zero governing texts | untested; three texts named, none checked |
| 2 | same three texts, opposite signs | forty lines apart |
| 3 | every address re-resolved | four of eight stale by 88–106 lines |
| 4 | every address re-resolved | 2 of 6 landed |
| 5 | task 18 bounds the feature | it refused all ten gates |
| 6 | not by address | seven addresses, 2 of 6 landing |

The sixth is the one worth keeping: **the claim was strengthened and nothing it described was
changed.** A record cannot check itself, and this one had four readers who could.

## Status

**Withdrawn** — nothing was built, so nothing is owed an acceptance. `withdrawn` rather than
`closed` deliberately: the ledger counts `closed` as finished work and would require an ACC for a
record that produced none. The adjacency is filed separately and `CHG-20260903-25` continues on its
own conditions.
