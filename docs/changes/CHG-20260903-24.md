# CHG-20260903-24 — a gate arrives pre-answered

- Project: ai-sdlc-runner
- Branch: claude/chg-20260903-24-split-3
- Date: 2026-09-03 (UTC+0) · **fifth draft — four seats failed the fourth, all four maintaining the veto**
- **Measured against `origin/main` at `5f6b8b3`**, pinned by sha, by string and by address.
  The third draft pinned `22cba85`; **nine changes landed before a fourth round convened** and
  three of its six addresses moved (`engine.py` 2248→2297 and 2579→2628, `graph.py` 307→313).
  They were **correct when written** — this is rot, not error, and it is the second consecutive
  draft to rot the same way. The string pin survived both times; only the line numbers did not.
  The second draft kept *"pinned by string as well as address"* and dropped the sha; four of
  its eight addresses had gone stale by 88–106 lines. Every address below is re-resolved.
- Type: new capability
- Proposed by: the operator — 「改成預先答好、人按一下」 — and **split out of CHG-20260901-15 on the
  independent recommendation of two seats**, which reached it separately in the same round.
- Risk: **medium** — up from *low*. See the first section: the first draft's claim to falsify no
  governing text was untested, and its headline chart was wrong.
- Autonomy: high
- Supersedes: the first half of CHG-20260901-15, which is withdrawn and re-issued as this record and
  [CHG-20260903-25](CHG-20260903-25.md)

## Read this first: as first drafted, this shipped nothing

Four seats reviewed the first draft. All four failed it and the conformance seat vetoed it. Two
things it claimed are false, and both are measured below rather than argued:

```
gates that hold a pre-answer at a stop, as first drafted, on a default install

  claimed  [======]  6
  measured [      ]  0
```

`ANSWERED_FIRST` shipped as `{}`, so `answering()` returned `("operator", "default")` for all ten
gates; and with `node_models = {}` the five model panels take the single-ask path and produce no
`outcome`, `reason` or `verdicts` at all. **Zero of eight stops carried a pre-answer.** The
deliverable was a sentence and an exit code.

`-25` states its own zero in its first section and this record did not. That discipline is the thing
the split was justified by, so it is here now.

### And "falsifies zero governing texts" was untested

The first draft justified `ANSWERED_FIRST`'s shape three times by `routed_to` / `HALT_ROUTING` /
`DEFAULT_RECIPIENT` — which is exactly *a project-configured store table read into a policy
function*. The conformance seat named three texts that would fall under that reading. **Re-measured, none of
them falls, and the third is already false for an unrelated reason:**

- `settings.py` is **scoped** — *"Settings cannot touch a gate verdict, a permanent halt, or the
  adjudication rule"* — and a per-gate answerer is none of those. It also would not arrive through
  `settings.FIELDS`.
- `settings.py`'s operator-facing refusal was rewritten by CHG-20260903-28 to be **derived from
  `FIELDS`**, so it names whatever the runner actually reads.
- `docs/structure/data.md` — *"everything about the governance is in `policy.py`"* — **is already
  false on `main`**, and neither split record made it so. `halt_routing` is a `CREATE TABLE` in
  `store.py`, written there, read by `store.halt_routing(db)`, carried on `RunConfig` and passed
  into `policy.routed_to(kind, routing)`. Who a halt is routed to is governance, it lives in a
  project store table, and `policy.py` reads it as an argument. Filed as **CHG-20260903-33**.

So the sweep's conclusion changes: a project override would falsify **no** text that is true today.
What it would do is make this record depend on -25's store table, reader, CLI surface and door
validation — which is the defect seat's finding and the reason the ruling below still stands.

**The ruling this draft takes**, and it settles both problems at once: `ANSWERED_FIRST` ships
**populated in `policy.py`**, the way `HALT_ROUTING` ships six rows, and there is **no `table=`
parameter and no project override.** Then:

- zero texts fall, because nothing configures it;
- the feature is not inert, because the table names real gates;
- and the record stops leaking into `-25`, which is the defect seat's finding below.

**What that costs, stated plainly: 「可以依節點 by pass」 is not delivered by this record.** A
per-node project setting needs a store table, a reader, a CLI surface and a door validation — which
are `-25`'s tasks 12 and 13. The first draft's `table=` parameter was a socket wired to a record
this one claims to be independent of.

## The chart, recounted

The first draft's chart summed to **11 over 10 gates** — `lead_task_review` was counted twice, once
as an `after` gate and once as one that never stops. Recounted against `graph.NODES` × `policy.GATES`,
as a partition:

```
never stops at any grade                        [==]    2   engineer_selfverify, lead_task_review
after, model panel — needs >= 2 models          [====]  4   lead_assess, pm_confirm, pm_signoff,
                                                            qa_accept
after, seat panel, adjudicates AFTER its gate    [=]    1   lead_review
after, single — never adjudicates                [=]    1   qa_verify
before, and it can stop                         [==]    2   pr, merge
                                                ──
                                                        10   = 10 gated nodes = 10 gates
```

Four, not six — and **four only when a project has configured two or more models on that node**.
`qa_verify` was in the first draft's six and is `mode=SINGLE`, which the same draft said never
adjudicates, sixty lines below. That is the objection this record raises against its own draft
three, reproduced in the chart that makes it.

## The design

### `ANSWERED_FIRST` — one bit, in `policy`, shipped populated

```python
#: Whether a stop is answered first by a person or by the node's own voices. Data, beside `GATES`.
#: Populated, the way `HALT_ROUTING` is — an empty policy table whose only consumer is a default is
#: the shape CHG-20260901-16 D4 was filed for. There is no project override and no `table=`: this
#: record has no configuration surface, and that is what makes it independent of CHG-20260903-25.
ANSWERED_FIRST: Dict[str, str] = {...}       # gate -> "node"
DEFAULT_ANSWERER = "operator"

def answering(gate: str) -> Tuple[str, str]:   # (who, source); str in, str out
```

`str` in, `str` out, keyed by the same thing its argument is — `routed_to`'s shape, minus the
override. `source` therefore has **two** values, not three, and the docstring says so rather than
copying a third that cannot occur.

Three earlier drafts got the key wrong in three ways, each killed by a seat: draft two wrote
`models`, a **value** of `adjudicate`'s `voices=` parameter — which at `lead_review` would have
silently stripped `conformance`'s veto, because that branch is documented *"No voice vetoes."*
Draft three wrote `voices`, the parameter's own name. Draft four returned four answerers from a
resolver in `policy.py` taking a `graph.Node`, reversing the only dependency edge this codebase has.

### Which arrangement answers is read in `engine`, from three inputs

The first draft's task said `(mode, grades_risk)`. Measured, `walk` selects on
`if node.mode == MODEL_PANEL and len(configured) > 1:` (`engine.py:2297`) with `if node.grades_risk:`
**nested inside it**. The selector is `(mode, len(configured), grades_risk)` — and the third input is
exactly what the chart's "needs >= 2 models" column is about. The first draft's own §"On a default
install" proves the pair is insufficient, two sections from the task asserting it.

### The pre-answer is the run's own adjudication record — and which entry

`walk` writes `report.adjudications.append({"node_id": node.id, **outcome, "verdicts": …})` at four
sites. **The idiom is to spread the adjudicator's own output**, not to enumerate a shared subset:
draft four enumerated five keys, carried `vetoed` (which `adjudicate_grade` never returns) and
dropped `grade` and `proposed` (which `adjudicate` never returns). `CHG-20260901-18` is titled *"the
grade a panel decides reached nothing that names it"*.

**Which entry, when a node has several.** The re-run loop appends one per round, so `lead_review`
failing once then passing leaves two entries and no key names the lap. The pre-answer is the **last
entry for that node id** — and `engine.py:2628` already does `report.adjudications[-1]` with **no
`node_id` filter at all**, which is a defect this record must not copy.

## The one open question, and its real cost

`lead_review` is the only `after` gate whose pre-answer is not free: `_adjudicate` runs *after*
`_gate("after")` and calls `_send_back`, which mutates the report.

The first draft named the report mutation. It did not name the routing, which is the sharper half:
`_adjudicate` is called **43 lines after** `_gate("after")` and after the `strictest` re-resolution,
so moving it ahead of the gate means that at `lead_review` @ high — where `GATES` says `HALT` — a
failing panel would route to `review_failed` through `_send_back` **before the human halt fires**.

This stays an open question and is **not** carried as a task with a done-when, which is what the
first draft did with it.

## The two blockers, measured before they were accepted

Both came from the risk seat, and both were re-measured here rather than taken on report.

### R1 — the pre-answer reaches the one `halt_independent` cell, and this record never said the word

```
halt_independent cells in policy.GATES     [('acceptance', 'high')]     exactly one
qa_accept                                  gate='acceptance'  gate_when='after'
                                           mode='model_panel'  answer_decides=True

the words in this record, before this draft
  acceptance         0
  halt_independent   0
  qa_accept          1     (a chart cell)
```

`qa_accept` is one of the four gates this record pre-answers. `GATES["acceptance"]["high"]` is the
**only** `halt_independent` cell in the table, and `engine._gate` was rewritten by CHG-20260903-27
and -34 specifically to refuse a pre-authorisation there — *"A person answering the stop they were
shown is what `halt_independent` means … What is refused is authorising it in advance."*

A pre-answer is not an approval and the rung check does not fire on it. That is exactly why it has
to be bounded here: `relax` carries the bound in as many words (*"a class pre-authorises, and a
halt is not a question"*), `_gate` carries it, and `ANSWERED_FIRST` carried nothing.

**Task 18 (new): `ANSWERED_FIRST` is refused for any gate whose cell at any grade is not
`confirm`.** Stated as the rung rather than as a list of gate names, so a table change cannot
widen it silently. `acceptance@high` is named in the refusal's message as the reason the bound
exists.

### R2 — task 10 dispatches a lead-capability order in front of the one-way door

```
merge              role='lead'  gate_when='before'  mode='single'
capabilities(lead) can_spawn=True  can_write=True  can_execute=True
sandbox_for(low, can_write=True)   {'write': 'workspace', 'network': True}

WORK_ORDER_FIELDS  17, and none of them can say "recommend" rather than "do"
```

This record's mitigation was *"it produces no effects and the node's real ask stays where it is"*.
Measured, that is a property of `walk`'s control flow and **not of the order**: the order is
dispatched with the lead role's full capabilities and, at low risk, a writable workspace and the
network. `workorder.render` checks the field tuple is exactly `WORK_ORDER_FIELDS`, so there is no
field to carry the distinction and no way to add one inside this record's scope.

**Task 10 is withdrawn to an open question.** It is not deleted: the question — *can a model
usefully speak before a one-way door, and what would carry "advisory" in a closed schema?* — is
worth asking, and asking it at `merge` without a channel is not the way. Restated below with the
seat's measurement attached, so the next round argues about the mechanism rather than rediscovering
the sandbox.

## The chart's last row was a label error, not a count error

Both the defect and idiom seats flagged `before — the gate fires before the node runs [==] 2`
against a measured three (`engineer_selfverify`, `pr`, `merge`). Re-measured, and **the fix is the
label**:

```
never stops at any grade   2   engineer_selfverify, lead_task_review
...
before, and it can stop    2   pr, merge
                          ──
                          10
```

`engineer_selfverify` is `gate_when="before"` **and** `auto` at all three grades, so it is claimed
by the first row. The partition is sound; the axis switched from *when the gate fires* to *whether
it stops* halfway down, and only the last row's caption said otherwise. Changing the 2 to a 3 would
have broken the sum — the third time in this round that acting on a finding's face value would have
produced a wrong fix.

## Tasks


| # | Task | Done-when | State |
|---|------|-----------|-------|
| 1 | This record is reviewed by the seats | four seats, four worktrees — the second draft, after four failures | **[ ]** |
| 2 | `ANSWERED_FIRST` is gate-keyed, one bit, `str` in `str` out | `routed_to`'s shape | **[ ]** |
| 3 | It ships **populated**, with no `table=` and no project override | an empty policy table read only by a default is CHG-20260901-16 D4 | **[ ]** |
| 4 | The record says 「可以依節點 by pass」 is **not** delivered here | that needs -25's tasks 12 and 13 | **[ ]** |
| 5 | The arrangement is read in `engine`, from `(mode, len(configured), grades_risk)` | three inputs, measured at `engine.py:2297` | **[ ]** |
| 6 | The pre-answer is the **last** `adjudications` entry for that node, spread | a re-run appends one per round and no key names the lap | **[ ]** |
| 7 | A graded panel's pre-answer carries `grade` and `proposed` | dropping them is CHG-20260901-18 in the record that cites it | **[ ]** |
| 8 | The render is per-answerer | `.voices` is binary; a grade is one of three | **[ ]** |
| 9 | Where no adjudication exists, the gate says so | four gates hold one, and only with two models configured | **[ ]** |
| ~~10~~ | **Withdrawn to an open question** (risk seat R2) | the order would carry `capabilities('lead')` and, at low risk, a writable workspace and the network, into the one gate that stops at every grade — and `WORK_ORDER_FIELDS` has no field that can say *advisory* | **[—]** |
| 11 | A one-voice pre-answer says it is one voice | the only one-voice case is a **seat** panel below the floor via `high_risk_mode`, not a model panel | **[ ]** |
| 12 | The exit code distinguishes finished / suspended / stopped | all three return `0`; a named map with a refusal | **[ ]** |
| 13 | Divergence and latency are recorded | *"followed without being read"* is measurable from data already written | **[ ]** |
| 14 | An approval is bound to **the stop**, then to the recommendation | `run_id` was the wrong invariant: CHG-20260903-34 found the approval was not bound to the stop at all | **[ ]** |
| 15 | Each fix has a test that fails without it | **revert the fix, watch the test go red** — the standing task both split records dropped | **[ ]** |
| 16 | The README's test count follows the suite | `--collect-only`, not arithmetic | **[ ]** |
| 17 | An invalid `ANSWERED_FIRST` row is refused where the table is defined | a **test** over the literal, as `HALT_ROUTING` is watched — not `check_routing`, whose one caller is the project **write** path (`store.py`), and task 3 removes every configuration surface from this record, so nothing arrives at a door (idiom seat) | **[ ]** |
| 18 | **`ANSWERED_FIRST` is refused for a gate that is not `confirm` at every grade** | stated as the rung, not a list of names, so a `GATES` change cannot widen it silently; `acceptance@high` — the only `halt_independent` cell — named in the refusal | **[ ]** |

## Task 10, restated, because the first draft laundered a risk

The first draft said `pr` and `merge` *"need a new ask"*. Measured, both are `gate_when="before"`,
`mode="single"`, `role="lead"`, `can_execute=True`:

```
today    _gate("before") -> ask -> effects        the stop is first
drafted  ask (to produce the pre-answer) -> _gate  a model speaks first
```

Two texts say the stop comes first — `graph.py:313` (*"a one-way door — its gate is consulted
BEFORE, because the stop has to come before the door swings"*) and `docs/structure/logical.md:58`.
**Between the two split records nobody owned "a model now speaks at merge before the person does."**
The risk seat's words, and it is the finding that most damages the split's accounting.

So: a **recommendation order** with `can_execute=False`, and the node's real ask stays where it is,
after the gate. The stop keeps its place.

## Risk & rollback

- **Two gates gain a dispatch**, `pr` and `merge`, and it is a recommendation rather than the node's
  work. The other `after` gates gain none — which is now consistent with the chart above, where the
  first draft's Risk section contradicted its own chart sixty lines up.
- **A pre-answer is a recommendation a person may follow without reading.** The stop survives; the
  quality of attention behind it may not. Task 13 makes the *absence* of reading visible. Nothing
  makes the presence provable.
- **`lead_review` is the sharp one** and it is the open question, not a task.
- **This falsifies no governing text**, and the second draft then contradicted itself about
  why. Its §"falsifies zero" measured that **none** of the three named texts falls under a
  project override; its Risk section said **all three** fall. Same three texts, opposite
  signs, forty lines apart — the exact defect this record's own first draft was failed for,
  and the one the idiom seat failed `-25`'s first draft for (idiom seat).

  **The measurement stands and the Risk sentence is withdrawn.** `settings.py` is scoped to
  gate verdicts, permanent halts and the adjudication rule; its operator-facing refusal
  derives from `FIELDS` (`settings.py:167`); and `docs/structure/data.md` was rewritten by
  `CHG-20260903-33` because `halt_routing` had already made it false. A project override
  would falsify no text that is true today. **The reason this record has none is the defect
  seat's, not the sweep's:** a `table=` with no writer is a socket wired to `-25`.
- Rollback: revert. Every gate answers cold.

## What the four seats found, and where each went

| finding | seat | disposition |
|---|---|---|
| the chart sums to 11 over 10 gates | conformance · defect · risk | fixed; recounted as a partition |
| six is really four, and zero by default | conformance · defect · idiom | fixed; stated in the first section |
| "falsifies zero" is untested | conformance | fixed by task 3's ruling |
| `table=` is a socket wired to -25 | defect | fixed; the parameter is gone |
| the arrangement has three inputs, not two | conformance | fixed as task 5 |
| which `adjudications` entry, when there are several | conformance · defect | fixed as task 6 |
| task 10 laundered "a model speaks first at merge" | risk | fixed as task 10 |
| the approval is not bound to the recommendation | risk | added as task 14 |
| task 11 cites the diversity detector, not the panel-size one | defect | fixed as task 11 |
| the two standing tasks were dropped | idiom | added as tasks 15 and 16 |
| `README.md:27` — *"not from a model's judgement"* — is a vocabulary collision | conformance | **open**: named here, not disposed of |
| `gate_when`'s docstring is false at `lead_review` today | conformance | **open**: a live text-vs-code defect, not this record's |

## Task 10's mitigation was vacuous, and the record that found it is already merged

The second draft answered the risk seat's *"a model now speaks at merge before the person does"*
with `capabilities` carrying `can_execute=False`. **Measured, that flag bounds nothing.**
`policy.sandbox_for(risk, can_write)` does not take it, and `planner` (`can_execute=False`) and
`engineer` (`can_execute=True`) receive a byte-identical bound at the same grade —
`CHG-20260903-35` recorded that in `policy.py` and added the rule that catches the next such
flag. Worse, where the flag *does* bite it goes the wrong way: `_does_work` returns
`role.can_write or role.can_execute`, so a role with both `False` is **exempt** from owing an
operations declaration, which **loosens** `_permanent_halt`.

And there is no channel: `workorder.render` computes `"capabilities": policy.capabilities(
role.name)` from `node.role` alone, and `WORK_ORDER_FIELDS` is a closed schema checked at render
time. A per-order flag needs a new `render` parameter and a schema change, neither of which the
second draft named (risk seat).

**So task 10 is re-stated as a property of the order's kind, not of a capability flag**: the
dispatch at `pr` and `merge` is a *recommendation*, it produces no effects, and the node's real
ask stays where it is — after the gate. How that kind is carried is open, and this record says
so rather than naming a field that cannot hold it.

## The off switch this record's own ruling deleted

Empty plus a default meant the feature was inert everywhere. **Populated with no `table=` and no
project override means it is live everywhere, on every install, with no opt-in and no opt-out**,
and the only rollback is reverting the runner. The second draft's *"Rollback: revert. Every gate
answers cold"* did not say that its own task-3 ruling is what removed the per-project way back
(risk seat).

The `HALT_ROUTING` analogy breaks in three places, and they are the places that matter:

| | `HALT_ROUTING` | `ANSWERED_FIRST` as ruled |
|---|---|---|
| project override | **yes** | none |
| a bad row's blast radius | bounded — *"Routing narrows who is told FIRST. It never narrows who is told"*; `recipients` appends the operator unconditionally | a row **is** the whole decision at that gate; no backstop stated |
| a bad key refused | `check_routing` refuses an unknown kind at configuration time | **no analogue** |
| watched by tests | every kind asserted to have an owner, pinned by value | tasks 2 and 3 pin the *shape*; **no task pins the rows** |

**And the missing validator is tasked in the other record.** `-25` task 13 reads *"`policy.
check_routing` is the precedent, and CHG-20260903-24 has no analogue"* — a finding about this
record, done-when'd against `-25`'s configuration surface. That is the split laundering a second
finding, the same shape as the merge one. **It is carried here as task 17.**

## CHG-20260901-15 task 24, disposed of

Task 24 was *"Permissions, exercises and measurements reach three surfaces | sixteen report
fields reach no console"*. It appeared in neither successor, and `ACC-20260903-23` — an
**accepted** record — deferred a live defect to it. The conformance seat raised the orphan in two
consecutive rounds.

**Disposition: the defect is discharged and the task is not adopted.** `CHG-20260903-29` shipped
the six unreached snapshot keys with a rule requiring every future one to reach the console;
`CHG-20260903-34` fixed the stale `RunState.error` that `-29` had wrongly reported as not
reproducing. Both halves of L-27 now have an owner. The wider *"sixteen report fields, three
surfaces"* scope is **not** carried by this record and is not owed by it — the rule `-29` shipped
means the next field to go unrendered fails the suite rather than needing a task.

## The question task 10 becomes

*Can a model usefully speak before a one-way door, and what would carry “advisory” in a closed
schema?* Worth asking. Asking it at `merge` without a channel is not the way, and the measurement
is attached so the next round argues about the mechanism rather than rediscovering the sandbox:

```
capabilities('lead')             can_spawn=True  can_write=True  can_execute=True
sandbox_for('low', can_write)    {'write': 'workspace', 'network': True}
WORK_ORDER_FIELDS                17, none of which can say advisory
workorder.render                 refuses any tuple that is not exactly WORK_ORDER_FIELDS
```

Two shapes exist and neither is free: a new work-order field (a schema change every renderer and
every test of the closed tuple must follow), or a capability the role does not have (which means
`policy.capabilities` stops being a function of the role alone — the thing CHG-20260903-24's own
draft four died for).

## What the fourth draft still leaves open

- **`can_execute` is unenforced.** `CHG-20260903-35` records it; nothing draws it. Task 10's
  mechanism has to come from somewhere else.
- **「可以依節點 by pass」 is owned by nothing if `-25` is refused.** Task 4 defers it to `-25`
  tasks 12 and 13, and `-25`'s own Risk section says a seat would refuse `-25` outright. On that
  branch the operator's second requirement has no record at all, and neither record said so
  (idiom seat).
- **`README.md:27`** — *"not from a model's judgement"* — and **`gate_when`'s docstring at
  `lead_review`** remain open, as they were in the second draft. `CHG-20260903-35`'s sibling
  finding that `graph.Node`'s class docstring is false for eight of ten gated nodes is the same
  sentence one layer out, and is also unowned.

## Status

**Under review, fifth draft** — design only. Nothing is built.

Three of the four seats named, independently, that the fourth draft's header said *fourth draft* while this line said *third*. That was mine and it is fixed here rather than explained: a record that cannot say which round it is in is one nobody can review twice.

The conformance seat vetoed this record at both drafts. What it asked for is here: the two
cancelling arguments are resolved, the addresses are re-resolved with a sha pin, and
`CHG-20260901-15` task 24 is disposed of below rather than left in neither successor.
