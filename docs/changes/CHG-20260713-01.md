# CHG-20260713-01 — Structure truthfulness: list docs/ARCHITECTURE.md and the real tests/ files in directory.md

- Project: ai-sdlc-runner
- Branch: claude/jovial-blackwell-703283
- Date: 2026-07-13 (UTC+0)
- Type: adjustment (governance docs only)
- Proposed by: user (explicit directive "補上缺少項目" following the PR #4 review, which surfaced two directory.md truthfulness gaps)
- Implemented by: main agent (session jovial-blackwell-703283)
- Risk: low (documentation-only; no code, structure-of-code, data, or interface change; whitelist category "pure documentation")
- Commit/PR: (backfilled after commit); folded onto PR #4 (https://github.com/kamira/ai-sdlc-runner/pull/4)
- Recurrence check: no recurrence — directly repairs the residual truthfulness gap in CHG-20260706-01's `directory.md` sync (that CHG added AGENTS.md + knowledge/ to the tree but left two pre-existing omissions untouched); not a repeat of any prior CHG motive
- Skill: ai-sdlc v1.17.0
- Related: CHG-20260706-01 / ACC-20260706-01 (same `directory.md` tree); ACC-20260713-01

## Motivation

The PR #4 review found `docs/structure/directory.md`'s tree is not fully truthful — two files
that exist on the branch are absent from it:

1. `docs/ARCHITECTURE.md` — the handover-oriented architecture/feature overview — is not listed
   under `docs/` at all.
2. `tests/` lists only `test_contract.py`, but the suite actually carries `conftest.py` plus
   `test_dashboard.py`, `test_executors.py`, `test_skillstore.py`, `test_tui.py`,
   `test_workspace.py` (the 151-test suite cited by prior ACCs).

Both are pre-existing omissions (they predate CHG-20260706-01) but that CHG edited this same tree
for truthfulness, so this CHG closes the remaining gap in the same PR.

## Decisions & trade-offs

| Decision | Options | Assumptions | Why this choice |
|----------|---------|-------------|-----------------|
| ARCHITECTURE.md placement | list under `docs/` tree vs leave to prose | it's a real tracked file agents will meet | List it first under `docs/` with a one-line role note; keeps the tree an accurate index. |
| tests/ listing detail | enumerate every file vs a `test_*.py` glob | 6 test files + conftest is small and stable | Enumerate — the glob would hide which concerns are covered; explicit names match the CHG tags they trace to. |

Confirm gate: the user's directive ("補上缺少項目") plus the review that enumerated exactly these
two gaps is treated as explicit approval for this specific, reversible docs change.

## Modification Guide

### Goal
`docs/structure/directory.md` lists every tracked file it purports to index — specifically
`docs/ARCHITECTURE.md` and the real `tests/` contents — so the structure doc is truthful.

### Impact scope
| Affected item | Type | Change | Knock-on |
|---------------|------|--------|----------|
| `docs/structure/directory.md` | directory doc | add `docs/ARCHITECTURE.md` line; expand `tests/` from 1 to its actual files | none (docs-only; no rule/scope change) |

### Modification steps (tick when done)
- [x] 1. Add `docs/ARCHITECTURE.md` to the tree under `docs/` with a one-line role note.
- [x] 2. Expand the `tests/` block to the actual files (conftest + the 6 `test_*.py`).
- [x] 3. Verify: `git status` shows only `directory.md` + this CHG/ACC touched; pytest regression green; produce ACC-20260713-01 same round.

### Structure document updates
- [x] docs/structure/directory.md synced (this CHG *is* that sync — no other structure doc affected).

### Risk & rollback
- Risk: low — pure documentation; no runner code, no governance logic, no data.
- Rollback: revert the `directory.md` edit (restores the two omissions).

## Structure change summary
`directory.md`'s tree now indexes `docs/ARCHITECTURE.md` and the real `tests/` files. No code touched.

## Status
Accepted (see docs/acceptance/ACC-20260713-01.md) — Pass (self-verified, low-risk docs-only).
