# CHG-20260827-06 - the replay's second script payload is escaped and unguarded

- Project: ai-sdlc-runner
- Branch: claude/chg-20260827-round-findings
- Date: 2026-08-27 (UTC+0)
- Type: test (coverage) - an unguarded emitter in the change about unguarded emitters
- Proposed by: the acceptance round of 2026-08-27, finding 8 - see
  `docs/worklog/acceptance-round-20260827.md`
- Risk: **low** - tests only; the escaping is already correct
- Autonomy: high
- Related: CHG-20260823-33, CHG-20260823-38, ACC-20260823-38

## The finding

`_script_json` is called for **both** playback payloads - `__TURNS__` (`conversations.py:1366`) and
`__RAIL__` (`:1367`) - but only `__TURNS__` has a test that notices. Removing the `__RAIL__` call
leaves all 81 conversation and recording tests passing.

Exposure is genuinely low: the rail carries node ids and clock strings, not model-authored text. It
is still an unguarded emitter site in the change whose entire subject is unguarded emitter sites.

## What changes

A test that drives a closing script tag through the **rail** specifically, and fails if that one call
stops escaping.

## Tasks

| # | Task | Done when | State |
|---|---|---|---|
| 1 | A test that content reaching the rail cannot end the script block | removing `_script_json` from the `__RAIL__` line fails it | **[x]** |
| 2 | The test names the rail, so a reader can tell it apart from the `__TURNS__` one | both payloads have a test naming which one it guards | **[x]** |

## Status


Accepted (see docs/acceptance/ACC-20260827-06.md) -- Pass. Proven by cutting the __RAIL__ escaper - the new test fails, the old one passes.
