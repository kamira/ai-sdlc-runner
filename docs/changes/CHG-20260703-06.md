# CHG-20260703-06 — Align runner baseline to updated ai-sdlc v1.16.0

- Project: ai-sdlc-runner
- Branch: claude/align-v1.16.0
- Date: 2026-07-03 (UTC+0)
- Type: adjustment
- Proposed by: user ("已更新skill 現在的內容開出PR並merge" — upstream ai-sdlc skill updated to v1.16.0)
- Implemented by: I1 (subagent)
- Risk: medium (new vendored store version + config default bump; existing per-project locks untouched; migrate path preserved)
- Commit/PR: <fill at close-out>
- Skill: ai-sdlc v1.16.0
- Related: CHG-20260703-01 (align to v1.12.1), CHG-20260617-05 (offline store); ACC-20260703-06

## Motivation
The upstream ai-sdlc skill (github.com/kamira/ai-skills) has advanced from v1.12.1 to **v1.16.0**
(v1.14.0 root entry-point/AGENTS.md, v1.15.0 panel quotas + cross-validation, v1.16.0 elicitation-by-
proxy). Vendor v1.16.0 into the offline store and make it the runner's current baseline so the store,
the expected contract version, and the docs agree with the current skill.

## Decisions & trade-offs
| Decision | Options | Assumptions | Why this choice |
|----------|---------|-------------|-----------------|
| Vendor source | git tag vs main HEAD archive | v1.16.0 has **no git tag** (version in SKILL.md frontmatter) | Offline `git archive` of the clone's main HEAD `b4d6ef3` (labelled "ai-sdlc v1.16.0") into `skills/v1.16.0/` — same offline method as CHG-01/CHG-05 override, no fork |
| Keep old store versions | replace vs keep | existing project locks may point at 1.0/1.1/1.12 | **Keep** v1.0.0 / v1.1.0 / v1.12.1 — backward compatible; store is multi-version |
| Config expected version | leave 1.12.1 vs bump | first-run lock uses config_expected | Bump `contract_version` → `1.16.0` so **new** projects lock at current; existing locks unaffected |
| Runner compatibility | assume vs verify | runner reads SKILL.md version + calls `halt_gate.py` + parses "Role startup spec" | **Verified before this CHG**: v1.16.0 has `scripts/halt_gate.py`, `scripts/cross_repo_check.py`, `references/agent-hierarchy.md` "Role startup spec" (L127), and `SKILL.md metadata.version: 1.16.0` — all runner integration points intact. V1 re-verifies with a live run. |

## Modification Guide
### Goal
Vendor v1.16.0 into the offline store and make it the current baseline (config + docs), without
breaking existing 1.0/1.1/1.12 locks.

### Impact scope
| Affected item | Type | Change | Knock-on |
|---------------|------|--------|----------|
| `skills/v1.16.0/` (NEW) | directory | offline `git archive` of the ai-sdlc skill @ v1.16.0 into the store | skillstore resolver now offers 1.16.0 |
| `config/runner.yaml` | design | `contract_version: "1.12.1"` → `"1.16.0"` | new-project first-run lock = 1.16.x |
| `docs/ARCHITECTURE.md` | docs | §5 store-versions line (now four: 1.0.0/1.1.0/1.12.1/1.16.0) + §10 change-history row | — |
| `README.md` (EN + 繁中) | docs | store now v1.0.0 / v1.1.0 / v1.12.1 / v1.16.0; current baseline 1.16.0 | — |
| `docs/structure/directory.md`, `docs/structure/design.md` | directory/design | note the new store version + config default | keep structure docs truthful |
| `docs/ai-guideline.md` | docs | note current baseline v1.16.0 (Constraints/Assumptions) | — |

### Modification steps (tick when done)
- [x] 1. Offline-extract v1.16.0: `git -C <clone> archive b4d6ef3 skills/ai-sdlc | tar -x --strip-components=2 -C skills/v1.16.0` → repo `skills/v1.16.0/` (skill root: SKILL.md at top).
- [x] 2. Verify: `skills/v1.16.0/SKILL.md` frontmatter shows `version: 1.16.0`; `scripts/halt_gate.py` + `scripts/cross_repo_check.py` + `references/agent-hierarchy.md` ("Role startup spec") present.
- [x] 3. `config/runner.yaml`: `contract_version` → `"1.16.0"`.
- [x] 4. Sync docs: ARCHITECTURE (§5 store versions — now FOUR + §10 change history), README (both languages), structure/{directory,design}, ai-guideline.
- [x] 5. Run tests green (`python -m pytest -q`); the store-resolution + update-detection tests must still pass with four versions present.

### Structure document updates
- [x] directory.md (+skills/v1.16.0), design.md (config default 1.16.0)

### Risk & rollback
- Risk: medium — adds a store version + bumps the first-run default; existing locks & migrate path unchanged. Rollback: remove `skills/v1.16.0/`, revert `contract_version` to `1.12.1`, revert docs.

## Structure change summary
Adds `skills/v1.16.0` to the offline multi-version store and makes 1.16.0 the config default; docs/
structure updated in the same change. No governance logic duplicated (still read from the vendored
skill's own scripts/refs); runner integration points verified present in v1.16.0.

## Status
Accepted (see docs/acceptance/ACC-20260703-06.md) — Pass (independent V1; live run locks 1.16.0 via vendored halt_gate.py, no runner code changed, 158 passed with skill store).
