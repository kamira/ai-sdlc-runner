# CHG-20260904-12 - CHG-20260903-25 is withdrawn, and two of its findings outlive it

- Project: ai-sdlc-runner
- Branch: claude/chg-20260904-12
- Date: 2026-09-04 (UTC+0)
- **Measured against `origin/main` at `09c5e35`**, pinned by sha and by string
- Type: governance - a withdrawal, and where what it found goes
- Proposed by: the **conformance seat**, holding the veto, on its own round-seven ruling; the
  **risk seat** concurring after twice declining to withdraw; the **idiom seat** withdrawing its
  own round-seven withdrawal vote and confirming the repair was real
- Risk: low - nothing is built or removed; a record ends and three facts get a home
- Autonomy: high
- Target ACC: ACC-20260904-12

## Read this first

`CHG-20260903-25`'s seventh draft landed the conformance seat's conditions 1 and 4 and did not land
condition 2. The seat's round-seven ruling was that this was the last draft the veto stood over:
land or withdraw. It has not landed.

**The residue is one table row**, and that is why it decides rather than being tidied. Task 9's
done-when survived byte-identical to the sixth draft, carrying the dead polarity in backticks
outside the table the record exempts, saying **new code** and *this is not a class* where the body
retracts both - inside a record that certifies *"The four rows above are the only place the dead
addresses and the dead quotation still appear."*

**And the self-check that certified it was mine.** It grepped `here != policy.DEFAULT_CLASS`; task
9's row spells it without the `policy.` prefix. A negative result is only as good as the pattern
that produced it, which is the record's own sentence applied to the person verifying it.

Two further statements the seventh draft added are false, and both are mine:

```
price item 2   "combined_class reports the registered class instead"
               driven: combined_class([name]) -> standard, and so does [name, "standard"],
               because the function ends `return "emergency" if "emergency" in seen else
               "standard"`. Task 21 in the same document says exactly this.

the headline   "class_by_workstream carries no review date"
               measured: policy.class_in_force refuses a class without one, per workstream.
               The true reason is the record's own, 285 lines below.
```

## Where the findings go

`README.md` § *Known gaps* - which exists for a requirement nothing enforces, is pinned as a
governance property by `ACC-20260823-16` criterion 5, and already carries one entry struck through
by the change that closed it.

- **A change class relaxes everywhere or nowhere.** `policy.relax(graded, klass)` takes no node and
  `ChangeClass` carries no node-aware field, so a class that relaxes lowers **every** `confirm` cell
  it reaches, `merge@low` included. This is the real discovery in `-25` and the fact that made it
  unbuildable: it proposed a class bounded to one cell, and that bound cannot be written where a
  class lives, because `logical.md`'s *"`policy` depends on nothing"* is what stops a class reading
  a node.
- **Two requirements have no owner.** 「可以依節點 by pass」 is named in three records and deliverable
  by none. And both halves of `CHG-20260901-15` are now recorded as re-issued into records that
  produced nothing.

## Tasks

| # | Task | Done-when | State |
|---|------|-----------|-------|
| 1 | `CHG-20260903-25` is withdrawn, not closed | no ACC is owed, and the ledger agrees | **[x]** |
| 2 | Its false statements are corrected, not carried | all three named and measured | **[x]** |
| 3 | `relax`'s node-blindness is in *Known gaps* | with what closing it needs | **[x]** |
| 4 | Both unowned requirements are in *Known gaps* | where a later record can strike them | **[x]** |
| 5 | `CHG-20260903-24` stops saying `-25` continues | and names `-11` as the adjacency's number | **[x]** |
| 6 | The count of changes shipped is measured | `git log`, not arithmetic | **[x]** |
| 7 | The README's test count follows the suite | `--collect-only` | **[x]** |

## Task 6

```
changes shipped while CHG-20260903-25 was under review   35   CHG-20260903-26 .. CHG-20260904-11
found by the suite                                        0
```

Counted by distinct id in commit subjects on `origin/main`, not by mentions.

## Risk & rollback

- No code changes. Two records reach their final state and `README.md` gains two entries.
- The `Supersedes:` edge from `-25` to `CHG-20260901-15` is kept: a withdrawn record can still be
  where a superseded one's content went, which is the reading `CHG-20260904-08`'s check enforces.
- Rollback: revert. `-25` goes back to a seventh draft under a veto, and three facts lose their
  home.

## Status

**Done** - see ACC-20260904-12
