# CHG-20260706-01 — Governance backfill: root AGENTS.md entry anchor + docs/knowledge bootstrap

- Project: ai-sdlc-runner
- Branch: claude/jovial-blackwell-703283
- Date: 2026-07-06 (UTC+0)
- Type: adjustment (governance docs only)
- Proposed by: user (explicit directive: close the two doc_integrity_check findings under ai-sdlc v1.17.0 — missing root AGENTS.md, missing docs/knowledge/)
- Implemented by: main agent (Claude Fable 5, session jovial-blackwell-703283)
- Risk: low (documentation-only; no code, structure-of-code, data, or interface change; whitelist category "pure documentation")
- Commit/PR: (PR pending — backfilled at close-out)
- Recurrence check: no recurrence — first governance-backfill CHG in this repo (prior CHG motives: build stages 20260617-01..08, offline store 20260703-01/02, dashboard/perf 20260703-03..05)
- Skill: ai-sdlc v1.17.0
- Related: ai-skills CHG-20260706-01 (origin of the v1.17.0 knowledge-bootstrap lint); ACC-20260706-01

## Motivation

`doc_integrity_check.py` (ai-sdlc v1.17.0) fails this repo with two findings:

1. Governed repo (`docs/changes/` exists) lacks the root entry point `AGENTS.md` — the entry
   anchor must live at repo root so any agent from any vendor finds the governance in seconds
   (SKILL.md "Entry anchor").
2. `docs/knowledge/` is missing — v1.16 "knowledge founded at start" + v1.17 legacy-backfill rule:
   the knowledge container must exist up front (an empty INDEX is a legitimate knowledge base);
   if the container doesn't exist, autonomous recording never happens.

The user's verification bar: `python3 …/ai-sdlc/scripts/doc_integrity_check.py --repo .` exits 0.

## Decisions & trade-offs

| Decision | Options | Assumptions | Why this choice |
|----------|---------|-------------|-----------------|
| Skill rule source | local ai-skills checkout (v1.16.0) vs origin/main (v1.17.0) | user explicitly named v1.17.0; local main was clean and 3 commits behind | Fast-forward pulled ai-skills main to v1.17.0 (0c32c31) — the v1.16 lint cannot even detect the knowledge finding the user cited. |
| AGENTS.md content | verbatim SKILL.md template vs expanded custom page | template is designed to be scannable in seconds; expansions drift | Verbatim SKILL.md entry-anchor template (handshake reading order, governance location, non-negotiables). No tool-specific stub files exist here, so none are needed. |
| knowledge mode & language | single-file `knowledge.md` vs entries/ split; English vs zh-TW | entries < 30 → single-file per spec; all repo docs are English | Single-file INDEX-first `knowledge.md` with zero entries + seed `vocabulary.json`; English to match repo docs. *(decided on user's behalf — low-risk, reversible)* |
| vocabulary seed tags | minimal `_doc` only vs seed domain tags | tags are the retrieval key; this repo's domains are stable | Seed 3 controlled tags (`governance`, `contract`, `dashboard`) with cross-language aliases. *(decided on user's behalf — low-risk, reversible)* |

Confirm gate: the user's directive already specifies exactly what to implement, from which
template, and the verification command — treated as explicit approval for this specific change.

## Modification Guide

### Goal
`doc_integrity_check.py --repo .` exits 0: root `AGENTS.md` present (SKILL.md template) and
`docs/knowledge/` founded (zero-entry INDEX `knowledge.md` + seed `vocabulary.json`), with
structure docs kept truthful.

### Impact scope
| Affected item | Type | Change | Knock-on |
|---------------|------|--------|----------|
| `AGENTS.md` (new, repo root) | directory | entry anchor per SKILL.md template | none (docs-only; no stub files exist to repoint) |
| `docs/knowledge/knowledge.md` (new) | directory | INDEX-first knowledge base, zero entries | future DIR/KN entries land here |
| `docs/knowledge/vocabulary.json` (new) | directory | tag registry + alias bridge (seed) | lint fail-loud parses it once entries/ mode is used |
| `docs/structure/directory.md` | directory doc | add AGENTS.md + docs/knowledge/ to tree & rules | keeps structure docs truthful |
| `docs/ai-guideline.md` §6 | requirement doc | baseline note: v1.17 entry-anchor + knowledge-bootstrap governance | none (no requirement/scope change) |

### Modification steps (tick when done)
- [x] 1. Create root `AGENTS.md` — verbatim SKILL.md entry-anchor template (handshake order → guideline → knowledge INDEX → open CHG/branch state; governance under docs/; non-negotiables).
- [x] 2. Create `docs/knowledge/knowledge.md` — single-file mode header + zero-entry INDEX table (per references/knowledge "Bootstrap" + "AI-friendly structure").
- [x] 3. Create `docs/knowledge/vocabulary.json` — `_doc` + seed tags with any-language aliases.
- [x] 4. Sync `docs/structure/directory.md` (tree + governance rule lines) and add the §6 baseline note in `docs/ai-guideline.md`.
- [x] 5. Verify: `doc_integrity_check.py --repo .` exit 0 + pytest regression green; produce ACC-20260706-01 in the same round.

### Structure document updates
- [x] docs/structure/directory.md synced: root AGENTS.md entry anchor + docs/knowledge/ added to tree and naming rules.

### Risk & rollback
- Risk: low — pure documentation; no runner code, no governance logic, no data. The only shared
  surface is the lint contract itself, which is exactly what this change satisfies.
- Rollback: delete `AGENTS.md` + `docs/knowledge/` and revert the two doc edits (restores the
  pre-change lint failures).

## Structure change summary
Repo root gains the vendor-neutral AI entry anchor `AGENTS.md`; `docs/` gains the pre-founded
knowledge base (`knowledge/knowledge.md` zero-entry INDEX + `knowledge/vocabulary.json` seed).
`docs/structure/directory.md` and the Guideline §6 baseline now record both. No code touched.

## Status
Accepted (see docs/acceptance/ACC-20260706-01.md) — Pass (self-verified, low-risk docs-only): doc_integrity_check exit 0, both findings closed; pytest 151 passed/7 skipped (158 with skill store).
