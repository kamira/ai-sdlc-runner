# CHG-20260827-10 - a retracted docstring survived one function along

- Project: ai-sdlc-runner
- Branch: claude/chg-20260827-round-findings
- Date: 2026-08-27 (UTC+0)
- Type: fix (docs in code)
- Proposed by: the acceptance round of 2026-08-27, finding 12 - see
  `docs/worklog/acceptance-round-20260827.md`
- Risk: **low** - a docstring
- Autonomy: high
- Related: CHG-20260823-30, ACC-20260823-30

## The finding

CHG-20260823-30 retracted the claim that `plan.check` refuses what it cannot honour, and pinned the
retraction with `test_the_module_no_longer_claims_to_refuse_what_it_cannot`.

The fix reached `check`'s summary line only. `plan.load`'s docstring (`plan.py:225`) still reads
*"refuse it if this runner would not fully honour it"* - the exact retracted phrasing, one function
along, where the existing test does not look.

## What changes

`plan.load`'s docstring says what `load` does. The existing retraction test widens to cover the
module's other public docstrings, so the next retraction cannot survive by moving one function
along.

## Tasks

| # | Task | Done when | State |
|---|---|---|---|
| 1 | `plan.load`'s docstring corrected | it no longer claims to refuse what the runner would not fully honour | [ ] |
| 2 | The retraction test covers every public docstring in the module | putting the retracted phrasing into any of them fails it | [ ] |

## Status


Accepted (see docs/acceptance/ACC-20260827-10.md) -- Pass. The retraction covers the module rather than the one docstring somebody tested.