# CHG-20260713-02 — Knowledge close-out: backfill INDEX with prior-CHG durable decisions + document the single-file→entries/ split threshold

- Project: ai-sdlc-runner
- Branch: claude/jovial-blackwell-703283
- Date: 2026-07-13 (UTC+0)
- Type: adjustment (governance docs only)
- Proposed by: user (directive "全部自動治理執行 ai-sdlc-autopilot" — run the remaining PR #4 review follow-ups #3/#4 under autopilot)
- Implemented by: main agent (session jovial-blackwell-703283) via ai-sdlc-autopilot
- Risk: low (documentation-only; no code, structure-of-code, data, or interface change; whitelist category "pure documentation")
- Autonomy: low → full-auto through PR; merge to `main` gated for one human confirm (outward, hard-to-reverse)
- Commit/PR: 03c4547; folded onto PR #4 (https://github.com/kamira/ai-sdlc-runner/pull/4)
- Recurrence check: no recurrence — first knowledge-content backfill in this repo; CHG-20260706-01 founded the empty base, this CHG populates it. Not a repeat of any prior CHG motive.
- Skill: ai-sdlc v1.17.0; autopilot: ai-sdlc-autopilot v1.0.0
- Related: CHG-20260706-01 (founded the empty knowledge base); CHG-20260713-01 (directory truthfulness); ACC-20260713-02

## Motivation

The PR #4 review left two knowledge follow-ups open:

3. `docs/knowledge/knowledge.md` INDEX is empty. Prior CHGs encode durable, recurring decisions an
   incoming agent must not re-derive: the offline skill-store **vendoring pattern**, the per-project
   **version-lock semantics**, and the **dashboard/curses conventions**. These are exactly the
   "observed pattern" (KN) entries the base exists to hold.
4. The single-file mode header states "entries < 30" but never states the *action* taken when the
   threshold is crossed (split into `entries/`), so the migration trigger is undocumented.

## Decisions & trade-offs

| Decision | Options | Assumptions | Why this choice |
|----------|---------|-------------|-----------------|
| Which entries to seed | one-per-CHG (13) vs a few consolidated patterns | the value is the *pattern*, not per-CHG minutiae | Three consolidated KN entries, each citing its source CHGs — high signal, stays well under the 30-entry single-file ceiling |
| Entry tier | flat vs tiered | ai-sdlc knowledge uses a tier column | Tier `pattern` (durable architecture decisions), matching the INDEX schema |
| Tags | reuse seeded tags vs add new | `contract`/`dashboard`/`governance` already seeded in vocabulary.json | Reuse the seeded tags; no new tag needed → no vocabulary.json change |
| Threshold doc placement | knowledge.md header note vs separate doc | the rule belongs where the mode is declared | One line in the existing single-file-mode header block |

Confirm gate: the user's autopilot directive plus the review that enumerated exactly #3/#4 is
explicit approval for this specific, reversible docs change (low risk → auto per the halt policy).

## Modification Guide

### Goal
`docs/knowledge/knowledge.md` carries three grounded KN entries (vendoring, version-lock, dashboard)
with matching INDEX rows, and the header documents the entries/ split action. Lint stays green.

### Global constraints (all tasks)
- Docs-only: touch **only** `docs/knowledge/knowledge.md`. No code, no vocabulary.json change (tags already seeded), no other structure doc.
- Every KN entry: one INDEX row (`id | tier | tags/scope | rule | status`) + one anchored body section, each citing source CHG ids that are real and Accepted.
- Stay in single-file mode (3 entries ≪ 30); do not create `entries/`.
- Verification bar: `doc_integrity_check.py --repo .` exit 0; pytest unchanged (no code touched).

### Tasks (TDD N/A — pure docs; "green" = lint pass + accurate citations)

| # | Task | Interface / output | Done-when |
|---|------|--------------------|-----------|
| 1 | Add KN-1 vendoring pattern | INDEX row + body: skills/ is PRIMARY offline store, vendored via `git archive` of published skill main HEAD, labelled by SKILL.md frontmatter; ai-skills/ submodule is optional fallback (CHG-20260703-01/CHG-20260703-06) | row + section present, CHG ids resolve |
| 2 | Add KN-2 version-lock semantics | INDEX row + body: per-project `.sdlc-lock.json` locks major.minor; `contract_version` default in runner.yaml is first-run only; bumps never touch existing locks; migrate is explicit/validating, never auto (patch=auto, minor/major=migrate-required) (CHG-20260617-01/CHG-20260703-01/06) | row + section present |
| 3 | Add KN-3 dashboard/curses conventions | INDEX row + body: stdlib curses + numbered/non-TTY fallback; snapshot path always preserved; panels computed on real events & cached (no per-keystroke I/O); red lines still need explicit human approval (CHG-20260617-02/03, CHG-20260703-04/05) | row + section present |
| 4 | Document split threshold | header line: "when entries reach 30, split into `entries/` (one file per entry) and keep this INDEX" | line present |

### Structure document updates
- [ ] None — `directory.md` already lists `docs/knowledge/` (CHG-20260706-01) and describes it as INDEX-first; entry population needs no tree change.

### Risk & rollback
- Risk: low — pure documentation; no runner code, no governance logic, no data.
- Rollback: revert `knowledge.md` to the zero-entry bootstrap state.

## Structure change summary
`docs/knowledge/knowledge.md` gains three KN pattern entries + INDEX rows and a split-threshold note.
No other file touched. No code.

## Status
Accepted (see docs/acceptance/ACC-20260713-02.md) — Pass (self-verified, low-risk docs-only).
