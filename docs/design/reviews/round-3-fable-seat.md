Working tree verified clean at `051287f` before and after review; nothing was edited. Every claim below was checked against the files at that commit, not against the record's description of them.

---

# Round 3 review — CHG-20260823-11 (fable-seat)

**Files read in full:** `docs/changes/CHG-20260823-11.md` (434 lines), `git show bb9d83f` (the round-2 diff, all 8 files), `docs/design/reviews/README.md` and all four seat verdicts, `docs/design/review-round-1-brief.md`, `docs/design/review-round-2-brief.md`, `src/ai_sdlc_runner/graph.py` (whole), `engine.py` and `policy.py` (all cited regions), `docs/design/console-mockup.html` (node data, config tabs, endpoint table).

---

## T1 — Did round 2's corrections introduce new defects? **Verdict: YES — two hard ones, one silent weakening, one pattern. Not "none found."**

**1. A false citation, inside the correction that discloses the priming.** The round-2-added risk-note paragraph says the round-1 brief handed both seats the suspend/resume alternative, "(`docs/design/review-round-2-brief.md` quotes it; the original is `review-round-1-brief.md`, Q2)" — `docs/changes/CHG-20260823-11.md:255-256`. The original is real: `docs/design/review-round-1-brief.md:38-40` ("e.g. the walk returning a suspended state the caller resumes"). But **the round-2 brief does not quote it anywhere**. I read `docs/design/review-round-2-brief.md` in full (73 lines); it lists "the 'ends the process' claim" among R1's items and never reproduces or references the Q2 worked example. A correction whose entire subject is the integrity of an evidence chain contains a pointer to evidence that is not where it says it is. Introduced by `bb9d83f`.

**2. An attribution overclaim, in the round that corrected an attribution overclaim.** "What round 2 did not overturn: … both agreed the 'Not claimed' section is honest" — `docs/changes/CHG-20260823-11.md:388-390`. fable-seat's committed verdict does say that (`docs/design/reviews/round-2-fable-seat.md:85`). codex-seat's committed verdict does not: its only "honest" statement is "The split verdict and 'does not pass' handling are internally honest" (`docs/design/reviews/round-2-codex-seat.md:82`), citing the then-current record's lines 237 and 242 — which I checked against `git show 5aed9b0:docs/changes/CHG-20260823-11.md`: line 237 is the round-1 verdict table header and 242 is the "Outcome: does not pass" paragraph, not the "Not claimed" section. codex's file never mentions the "Not claimed" section at all. So "both agreed" is one seat's agreement promoted to two — the exact species round 2's defect #2 corrected ("I overstated the attribution"), reproduced by the round that corrected it. The record's own new rule convicts it: "Where a summary and a verdict disagree, the verdict is right" (`docs/design/README.md`, added in the same commit).

**3. Task 12's inference-refusal clause silently dropped "role".** Round-1 wording: "inferring it from a node's id, **role** or branch shape is refused". Round-2 wording: "inferring the mode from a node's id or branch shape is refused" (`git show bb9d83f`, task-12 hunk; current `docs/changes/CHG-20260823-11.md:209`). No written reason appears anywhere in the record or the diff. fable-seat's round-2 required change #3 offered exactly two remedies: add a mode value that avoids reading `role`, **or** exempt the seat path "with a written reason" (`round-2-fable-seat.md:126`). The correction did the first (`seat_panel`/`model_panel` added) and then also took the second's permission without its condition. Consequence: the engine today keys the entire seat path off `node.role == "seat"` (`engine.py:686`, `engine.py:725`); under the narrowed clause, an implementation that adds the mode field as decorative data and keeps role-keyed dispatch violates nothing task 12 refuses. A refusal clause was weakened inside a commit titled as a tightening, silently.

**4. Correction-by-delegation (pattern, weaker).** Round 2's task-12 tightening converts three demanded decisions into demands that someone else decide: "referent type is **stated**" (the record does not state it), "every one of the 23 existing nodes **is assigned** a mode" (the record assigns none), cross-rule "which kinds may be a panel" (the rule is named, not given) — `docs/changes/CHG-20260823-11.md:209`. The record's own maxim, written eleven lines below by the same commit: "If the record does not decide this, the builder decides governance" (`:229`). Related latent tension: task 11's new opt-out ("or this record says in writing why the console ships happy-path-only", `:190`) is satisfiable in a way that contradicts the uncorrected screen requirement "The whole flow, every node" (`:62`), and nothing notes the collision.

**What I checked and did not find:** every line-level code claim the round-2 corrections introduced is true — `engine.py:719-721` (TERMINAL sets `halted_at`; verified, so "distinguishable from finished" genuinely does not exist today), `engine.py:572`, `engine.py:631-643`/`668-673`/`575-591` (confirmations counted, spent, unspent reported), `policy.py:649-651` (raise on unknown seats), `policy.py:655` (per-seat veto), `graph.py:114-118` (`lead_review` branches pass/fail only), no run-id in `RunReport`/`RunConfig` (`engine.py:150-240`, verified by reading both dataclasses). The seat-verdict tallies match ("fail" + 6 changes; "pass with changes" + 7). Task 17's both-directions rewrite covers codex's prose-pass objection because it is outcome-stated, not mechanism-stated (`engine.py:424-435` scans every field; a word-backstop trip on the filename would fail the "must not halt" direction). The Approver contradiction is genuinely gone — the only remaining "Approver" text is historical narration (`:312-314`, `:359`).

## T2 — Is task 12 buildable as written? **Verdict: NO — two competent builders would produce incompatible graphs at three named points.**

1. **The `main`/`follows` referent.** The done-when requires the referent type be "stated (role or node id)" and does not pick one (`docs/changes/CHG-20260823-11.md:209`). The only interaction spec uses one of each: `main:"lead"` is a role (`console-mockup.html:576`), `follows:"engineer_build"` is a node id (`console-mockup.html:577`). Builder A declares both roles; builder B both node ids; both "state" their choice; the graphs are incompatible and both pass the done-when.
2. **The 23-node mode assignment.** Required, not supplied — and the record's own vocabulary does not map onto the closed set: §1's table classifies nodes as "verdict / dispatch / follows" (`:80`), and "verdict" is not one of `single`/`seat_panel`/`model_panel`/`pool`/`follows`. Is `pm_confirm` statically `model_panel` (a verdict node that *may* fan out, count from `/config/nodes`) or `single` (one model configured today)? Nothing says whether mode is the *interpretation rule* for N configured models or a claim about N itself. Builder A marks every verdict node `model_panel`; builder B marks everything `single` except `lead_review`/`engineer_build`/`engineer_selfverify`. Both defensible, incompatible.
3. **"Which kinds may be a panel."** The cross-rule must exist in `graph.validate()` (`graph.py:159-206` is a real home) but its content is not given — and the record's own "What is not settled" admits a work-producing node with several models has no rule (`:400-404`). One validator admits `model_panel` on any role-bearing node, another restricts to DECISION + `answer_decides`. Both satisfy "enforces named cross-rules."

What round 2 genuinely fixed here: `seat_panel` vs `model_panel` closes fable's round-2 hole #1 (the old set could not express the seat panel without reading `role`), and "every node assigned / cross-rules / no id-or-branch-shape inference" are real, watchable requirements. The shape is right; the decisions inside it are still the builder's.

## T3 — Are task 1's two written-answer questions answerable from the record? **Verdict: Q1 yes, Q2 no.**

**Q1 (confirmations vs. new mechanism):** answerable. The record contains everything a decider needs: the existing mechanism's exact behaviour and locations (counted `engine.py:631-643`, spent `engine.py:668-673`, unspent-reported `engine.py:575-591` — all verified), the new path's required properties (task 1's done-when, `:180`), and the audit stakes (`:222-227`). The record poses it as a decision and both branches are compatible with everything else written. Nothing must be invented to decide; it must only be decided.

**Q2 (who mints the run id):** not answerable without invention. The record supplies one fact — no run-id concept exists in `RunReport` or `RunConfig` (verified, `engine.py:150-240`) and "the journal directory is currently the only durable run identity" (`:228-229`) — but no candidate minting authorities, no binding rule between id and journal directory, no uniqueness or persistence requirement. A builder would have to invent: whether `POST /run` (server layer), `walk` entry (engine), or the journal directory name (derived) mints it; how it binds to the journal so a resumed run keeps its identity; and where it lives in the report so "wrong-run answers are refused" is checkable. Each invention is governance.

## T4 — Is prose review still converging? **Verdict: NO — the defect altitude is falling while the introduction rate is not, and that is the polishing signature.**

The trajectory, from the committed verdicts: round 1 found roughly twelve substantive defects (six shared, six single-seat, per the record's own enumeration at `:290-340`, checkable now against `reviews/round-1-*.md`) — design-level: unenforceable independence, undecidable pool/panel data, silent flattening at `engine.py:572`, an attachment filename permanently halting every run. Round 2 found that three of nine corrections were defective, two reproducing the disease they treated, plus one genuinely new design finding (the `PolicyError` crash → task 20) and the two task-1 prerequisite questions. **Round 3 — what I found that round 2 created: two false claims about repository content (T1.1, T1.2), one silent weakening (T1.3), one delegation pattern (T1.4). Zero new design-level information.** Every code-level claim now in the record is true; what is being generated each round is narration about the review process itself, and the narration layer is producing errors of exactly the species being corrected — a false evidence pointer inside the priming disclosure, an attribution overclaim inside the round that fixed one. Round 1's defects were about the design; round 2's were about the corrections; round 3's are about the corrections to the corrections. The record is two meta-layers deep and each layer is minting its own errors.

Meanwhile, everything substantive that remains open (T2's three points, T3's Q2) is a *decision*, not a discovery — and a fourth prose round cannot validate a decision; it can only re-describe the absence of one. Building task 12 forces every one of those decisions into `graph.py` and `graph.validate()`, where a test can see them fail. **NEXT: build task 12** — with the T2 decisions written into the record as part of that build, not before it as a round 4.

**The steering check the caller asked for:** yes, this brief steers T4 toward "build." It leads with the record's own pre-written escape clause, frames this round as "that test," and the trigger condition — "if round 3 *splits* on defects introduced by round 2" — is observable by no seat, only by the author aggregating both, which puts the declaration of the outcome back in the corrected author's hands. And "build" is the outcome that ends review pressure on the author's record. I flag that plainly. My answer does not rest on the clause: it rests on the defect trajectory I verified independently (real, cited, and dominated by round-2-introduced narration errors), and I note the anti-invention warnings in T1's framing pushed toward "none found," which I did not deliver — the findings above are real. The structural mitigation that makes author-aggregation tolerable now exists: the verdicts are committed whole, and the record's own rule says the verdict beats the summary.

## T5 — The single thing that, if wrong, costs the most later.

**Task 1's question 1: whether a resumed gate decision is a spend of the existing `confirmations` mechanism or a second mechanism beside it** (`docs/changes/CHG-20260823-11.md:222-227`; the existing ledger at `engine.py:631-643`, `668-673`, `575-591`). Every other open item fails loud — an incompatible graph fails `validate()`, a missing run id fails the first wrong-run test, a decorative mode field is visible in a diff. Two overlapping approval ledgers with slightly different audit semantics fail *silent*: a gate opens on an approval one ledger spent and the other never saw, in a production run, and no after-the-fact audit can reconstruct who approved what — which is the halt guarantee, the one property the whole record grades itself high-risk on, degrading invisibly. The record itself names this "the quiet-governance-bug shape this repository keeps documenting itself shipping." It is a one-paragraph decision and it is not yet written.

---

**ROUND 3: fail**
**NEXT: build task 12**

What must change — folded into the task-12 build and its record sweep, not a round 4:

1. Strike or correct the false citation at `docs/changes/CHG-20260823-11.md:255-256`: `review-round-2-brief.md` does not quote the primed alternative; the only source is `review-round-1-brief.md:38-40`.
2. Correct `:388-390` to what the committed verdicts support: fable-seat called the "Not claimed" section honest; codex-seat's "internally honest" covers only the split-verdict handling (`reviews/round-2-codex-seat.md:82`). "Both agreed" is not substantiated.
3. Restore "role" to task 12's inference-refusal clause, or exempt the seat path explicitly with the written reason fable-seat's round-2 change #3 required — not silently.
4. In building task 12, decide in the record what round 2 only demanded be decided: the referent type of `main` and `follows` (one type, both fields, or the mixed scheme the mock-up uses — named); the mode of each of the 23 nodes; the content of "which kinds may be a panel"; and whether `single` may carry `main`/`follows` at all.
5. Before task 1 starts (unchanged from the record's own gate, now with Q2 sharpened): answer question 1 in writing, and for question 2 the record must name the minting authority and the id-to-journal binding — the constraint alone is not a decision.
